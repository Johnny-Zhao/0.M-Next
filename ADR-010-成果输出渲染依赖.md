# ADR-010 — 成果输出渲染依赖准入(Apache POI + PDFBox)

- 状态:**草案(待人批准并提交)** —— 这是 AG-501/AG-502 的依赖准入 spec-change;Codex 在本 ADR + `ci/deps-allowlist.yaml` 合入 main 前**不得引用**这些库。
- 日期:2026-06-15
- 关联:`docs/12-output-rendering.md` §5;任务卡 T-V33-708b(Office 渲染器);承 ADR-008(多架构/离线准入)、ADR-009(标准借鉴等级)

## 背景

阶段9 成果输出的"输出半边"中,708a 用 JDK 内置实现 HTML/Markdown/CSV 渲染(零依赖)。但**真·Office 交付物(docx/xlsx/pdf)无法用内置库生成**,必须引入渲染库。这触发 AG-502(禁止未准入依赖)+ ADR-008(多架构/离线)门,需专项裁定。

## 决策

为 **T-V33-708b 的 `RenderAdapter` Office 实现**准入以下库(均 **Apache-2.0**,纯 Java、无 native,满足 ADR-008 amd64/arm64 与离线):

| 用途               | 库(Maven 坐标)                         | 许可       |
| ------------------ | -------------------------------------- | ---------- |
| docx / xlsx(OOXML) | `org.apache.poi:poi-ooxml`(连带 `poi`) | Apache-2.0 |
| pdf                | `org.apache.pdfbox:pdfbox`             | Apache-2.0 |

- 仅用于 `packages/engines`(或独立 output 渲染模块)内的 Office 渲染适配器;**不得渗入 kernel/shared**。
- 版本固定(由 708b PR 在 BOM/父 POM 锁定),纳入 `pnpm verify` 与离线介质。

## 备选与否决

- **OpenPDF / iText**(pdf):**否决** —— OpenPDF 为 LGPL/MPL、iText 为 AGPL/商业,触碰 E2 许可红线(你的离线/分发约束)。PDFBox(Apache-2.0)干净。
- **不引库、用 HTML→外部转 PDF**:否决 —— 依赖外部工具链,违"基础能力不可降级"(§1135)且不离线友好。

## 影响

- **正面**:docx/xlsx/pdf 成果输出落地;输出快照(§1488)对真 Office 产物可算 hash、可追溯。
- **代价/风险**:POI 传递依赖较多(xmlbeans、commons-compress、commons-io 等)——**708b PR 须列全传递依赖并逐一过 allowlist/ADR-008**(纯 Java,无 native,风险低);包体增大(可接受,后台重任务 §1134)。
- **回滚**:Office 渲染器是 SPI 适配器,可单独移除而不影响 708a 的零依赖管线与内核。

## 待人执行

1. 审本 ADR;2. 合入 `ci/deps-allowlist.yaml` 的对应条目(下方草案);3. 二者提交进 main 后,T-V33-708b 方可下发(AG-501)。
