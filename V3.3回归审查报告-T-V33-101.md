# T-V33-101 回归审查报告

- 审查对象:分支 `feat/T-V33-101-kernel-commands-batch1`(8 个提交,ead9a09 之后),46 个文件、+2165 行
- 审查方式:从分支提交内容(git archive)逐项静态核验;沙箱无法运行 Maven/Testcontainers,运行期结论以用户本机 `pnpm verify` 输出为准(见 §6)

## 一、审查结论

**修改后进入用户验收。** 这是 Codex 迄今质量最高的一次交付:范围零越界、契约符合度高、测试覆盖了全部指定场景、AG-405 写入纪律完全生效(8 个细粒度提交、零截断、全部源文件完好)。仅 1 个实质问题(覆盖率门禁未生效)+ 1 个低级偏差,均为一行级修复。

## 二、逐项核验

| 核验项 | 实测 | 判定 |
|---|---|---|
| diff 范围 = 封闭清单 | 46 文件全部位于 kernel/server/shared、tests/contracts、ci/、pom、lockfile;零越界 | ✅ |
| 依赖 = 准入清单 | pom 新增逐项比对:flyway×2、postgresql、starter-jdbc/web/test、ulid-creator、testcontainers、jacoco——与任务卡逐字一致,无多余;ci/deps-allowlist.yaml 已建 | ✅ |
| Flyway V1 | 8 张表齐全;command_log 主键 (workspace_id, idempotency_key);outbox 含 sequence 与 (status,created_at) 部分索引;种子类型+字段定义 | ✅ |
| 事务边界(§1.2/AG-201) | 处理器 @Transactional 单事务:主数据+field_value_history+command_log+event_outbox 同帧;事务内零出站 | ✅ |
| 幂等(§6) | replay 同 hash 返原结果、异 hash 抛 KERNEL-409-IDEMPOTENCY-CONFLICT 并回原 commandId | ✅ |
| 字段级版本 [R-1](§5) | expectedFieldVersion 优先、缺省回落对象版本;冲突 details 含 conflictingFields(yourValue/currentValue/changedBy/changedAt)与 suggestion;值未变不发事件不加版本 | ✅ |
| P0 workspace 前置 | CommandSupport 查 workspace 状态,缺失抛 KERNEL-404-WORKSPACE-NOT-FOUND | ✅ |
| 权限前置(AG-503) | permissionChecker.check 先于一切写入;且有专项修复提交 dc2a04c"enforce permission precheck order" | ✅ |
| 事件信封 | 13 字段齐全;FieldChanged 携 before/after/version;fixture 样例眼检 13 必填字段无缺失 | ✅ |
| 错误码(AG-311) | 实现所用码均已登记(RULE-422-REQUIRED 按"RULE-422 前缀类"登记口径接受) | ✅ |
| 审计(AG-321) | 全部写路径带 created_by/updated_by;actor 取自 X-Actor-Id,禁止载荷传入 | ✅ |
| 测试 | 17 个 @Test:scenarioOne/Two/Eleven(契约 §9 #1/#2/#11)显式命名、幂等重放零副作用、失败零残留(原子性×2)、权限 verify×2、信封序列化↔fixture 闭环;**零 sleep**(AG-504) | ✅ |
| AG-405 | 8 个 Conventional Commits 按建议顺序推进;全分支文件类型抽查无截断 | ✅ |

## 三、问题清单

| 编号 | 严重 | 位置 | 问题 | 修复 | 阻断 |
|---|---|---|---|---|---|
| P-101-1 | **中** | 根 pom.xml | `<jacoco.minimum>0.00</jacoco.minimum>`——覆盖率门禁形同虚设,任务卡验收标准 #2(kernel ≥80% 且 jacoco 门禁)未满足 | kernel 模块 pom 覆写该属性为 0.80(其余模块可维持现状) | 是 |
| P-101-2 | 低 | CommandController 第 45 行 | 未知 commandType 抛 KERNEL-400-SCHEMA-INVALID,任务卡明确要求 KERNEL-400-UNKNOWN-COMMAND(该码已在 yaml 登记) | default 分支改用 UNKNOWN-COMMAND | 是(一行) |
| P-101-3 | 低(本地环境,非分支内) | 用户工作区 | HEAD 停在名字被截断的**空分支** `feat/T-V33-101-kernel-commands-b`,且工作区多个文件已损坏(Workspace.java 实测为二进制)——分支提交内容完好,仅本地检出态污染 | 见 §6 清理命令 | 否(但须清理) |

观察项(不阻断,记入阶段5 设计输入):FieldChanged 的聚合粒度为 `objectId:fieldCode`、sequence 复用版本号——满足契约"同聚合单调递增",但同一命令产生的 FieldChanged 与 ObjectUpdated 分属不同聚合,跨聚合无序;阶段5 读模型投影按对象重组时序时须依赖 correlationId+version 而非跨聚合 sequence。

## 四、架构红线检查

AG-100~110(kernel 零越界依赖、命令唯一入口)✅;AG-201(事务内零重操作)✅;AG-204/AG-106 不适用(无 AI);AG-321/322/324 ✅;AG-501/502/507/508 ✅;关系一等对象、视图隔离不适用(本批无关系/视图)。**零红线违规。**

## 五、给 Codex 的二次修复指令(T-V33-101-fix,同分支,仅以下两项)

1. `packages/kernel/pom.xml`:在 properties 覆写 `<jacoco.minimum>0.80</jacoco.minimum>`;本地跑 `mvn -pl packages/kernel verify` 确认覆盖率达标并把 jacoco 摘要贴入 PR——若实际覆盖率不足 80%,补测试至达标,**不得调低阈值**。
2. `CommandController` default 分支错误码改为 `KERNEL-400-UNKNOWN-COMMAND`(信息文案不变),对应集成测试断言同步更新。
禁止触碰其他任何文件。AG-405 纪律照旧。

## 六、给用户的操作与验收建议

1. **先清理本地检出态**(P-101-3,在 E:\0.M-Next 执行):
   ```
   git checkout -f feat/T-V33-101-kernel-commands-batch1
   git branch -D feat/T-V33-101-kernel-commands-b
   git status   ← 应 clean
   ```
2. 把 §5 两条指令发给 Codex;其完成后**在本机跑 `pnpm verify` 并把输出发我**——沙箱跑不了 Maven/Testcontainers,这是本轮唯一缺失的运行期证据,验收以它为准。
3. 验收演示建议:用任务卡的演示链(POST 建对象→改字段→版本冲突→幂等重放)对照集成测试断言走一遍;重点看版本冲突响应里的 conflictingFields 是否如契约 §5.3 呈现。
4. 通过后合并顺序:先合 `chore/T-V33-001-v33-convergence` 入 main,再合本分支,然后打 tag `v0.3-kernel-batch1`。
