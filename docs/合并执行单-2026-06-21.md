# 合并执行单(2026-06-21)

> 在 `E:\0.M-Next`(守 main)逐条执行。三张真待合:**P1(web)、REC-D、AI-1b**,文件互不相交、无交叉冲突。**IMP-A 已在 main,跳过只做收尾。** 每张:先 `git merge main` 拉平 → verify → 对检查清单 → 合。

---

## 0. 前置 · git 状态对账(先做,1 分钟)

```bash
cd E:\0.M-Next
git checkout main && git pull
git branch --merged main          # 看哪些分支其实已合(IMP-A 应在内)
git log --oneline -15             # 确认 IMP-A / 各卡的提交是否已在 main
```

判据:main 已含 `V18__import_task.sql` + `ImportService/ImportController` + `import-mapping` 契约 → **IMP-A 确已合**。

## 1. IMP-A 收尾(不合,删 stale 分支)

```bat
:: Windows cmd:用 findstr 代替 grep
git branch --merged main | findstr imp-a   :: 若列出 → 已合
:: 或 git 自带过滤(免管道):
git log --oneline -i --grep=import
:: 确认已合后:
git worktree remove E:\mnext-imp           :: 移除落后的 worktree(如用 worktree)
git branch -d feat/T-V33-imp-a             :: 删分支
```

⚠️ **不要**在 imp-a 分支上 commit 它显示的"两个 docs/tasks 删除"——那是分支落后于 main 的假象,提交会误删 main 现有新文档。

## 2. 逐张合并(一次一张,每张合完下一张再 rebase)

### 2.1 P1 — feat/T-V33-web-1?（实为 feat/T-V33-web-p1,web）
```bash
git checkout feat/T-V33-web-p1
git merge main                              # 拉平基线
corepack pnpm --filter @m-next/web test     # ← 补这行 vitest 汇总,要全绿
corepack pnpm verify                        # 全绿
```
检查清单(合前确认):
- [ ] 仅动 `packages/web`;`packages/views` 源 0 改;无后端/契约/迁移 diff
- [ ] 新依赖仅 `dockview@6.6.1` + `@xyflow/react@12.11.0`,锁精确版本
- [ ] 写经 `CommandClient`(updateFields/createRelation);改即重算闭合
- [ ] web vitest 全绿(本次补的那行)
```bash
git checkout main && git merge --no-ff feat/T-V33-web-p1
corepack pnpm verify                        # 合后 main 再验
```

### 2.2 REC-D — feat/T-V33-rec-d(ViewQueryController,只读)
```bash
git checkout feat/T-V33-rec-d
git merge main
corepack pnpm verify && node scripts/check-no-skipped.mjs
```
检查清单(已审过,确认即可):
- [ ] 仅 2 文件:`ViewQueryController.java` + `RecommendationMethodIntegrationTest.java`
- [ ] 纯读侧(无命令/写路径);无契约/迁移改动
- [ ] wpm 读最新 `decision-wpm` run、排序键 `score`、无 run→REC-409、空交集→空、`details=method=wpm`
- [ ] `check-no-skipped` Skipped:0;针对性测试 3/0/0
```bash
git checkout main && git merge --no-ff feat/T-V33-rec-d
corepack pnpm verify
```

### 2.3 AI-1b — feat/T-V33-ai-1b(确认写入闭环)
> **先把 `git diff --stat main` + 测试汇总发我审,再合。** 命门见下。
```bash
git checkout feat/T-V33-ai-1b
git merge main                              # 若 V19 与他人撞号 → 改 V20(本批无人用 V19,应无撞)
corepack pnpm verify && node scripts/check-no-skipped.mjs
```
检查清单(我 review 重点):
- [ ] **REVIEWER 门**:VIEWER/AUTHOR 调 ConfirmAiChange → 403;REVIEWER → 200
- [ ] **确认时重新预检**(不凭旧 verdict);**BLOCKED 项跳过不写**、item=SKIPPED
- [ ] **经命令重放写入**(`KernelCommandService`,Actor.user)→ 天然落审计/版本
- [ ] **命门(AI-1a 反向)**:REVIEWER 确认后 `fieldSnapshot()` **不再为空**;set=CONFIRMED
- [ ] 幂等:重复 Confirm 不重复写;已 REJECTED→AI-409
- [ ] 契约/夹具随卡(AG-406);错误码 `AI-` 前缀;`check-no-skipped` Skipped:0
```bash
git checkout main && git merge --no-ff feat/T-V33-ai-1b
corepack pnpm verify
```

## 3. 通用门禁 / 冲突处置

- **每张合前** `git merge main` 拉平;**合后** main 上 `corepack pnpm verify` 全绿 + `check-no-skipped` **Skipped:0**(真绿,非 `-DskipTests` 打包尾)。
- 预期**无交叉冲突**(三张文件不相交)。若 `error-codes.yaml` / `check-contracts.mjs` 真冲突 → **保留双方条目**(同此前 IMP-A×AI-1a 的处置),改完重验。
- 迁移撞号:若两张都建 `V19` → 后合者改 `V20`(本批仅 AI-1b 可能用 V19,应无撞)。
- 任何"夹带的越界文件 / docs 删除 / 非本卡改动" → 不提交,回报。

## 4. 收尾

```bash
git push origin main
git branch -d feat/T-V33-web-p1 feat/T-V33-rec-d feat/T-V33-ai-1b   # 已合的删
git tag v1.48 ... v1.50                                              # 按你的版本号习惯打标
```

> 顺序建议:**0 对账 → 1 IMP-A 收尾 → 2.1 P1 → 2.2 REC-D →(发我审)2.3 AI-1b**。每合一张,下一张 `git merge main` 再拉平。
