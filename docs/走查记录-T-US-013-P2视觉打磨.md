# 走查记录 T-US-013 · P2 视觉打磨

Spec-Ref: `docs/design/design-new/project/同源 主版本页面集.dc.html(7a/8c/9b/2b/9a/5b 六屏)`、设计系统 §01-§03、交接规格 §03/§05/§09、`docs/tasks/T-US-012` 审查记录①②。

## A. 必修项摘要

- [x] A-1 已修:插件详情操作分支调整为 `已启用=停用 / 已安装未启用=启用 / 未安装=安装`;补 store 与 VM 断言。
- [x] A-2 已修:preview P2 组件区 SlotCard 静态示例改为 instantiated / activated / empty 三态。
- [x] A-3 已修:canvas / template / sim 三处 React Flow 增加 `proOptions={{ hideAttribution: true }}` 隐藏水印。
- [x] A-4 已查:协议标签、仿真时刻/倍速、矩阵计数/售价、分析数值、插件版本/形式计数、配置单价格等使用 `us-data` 或 `UsMonoTag`。
- [x] A-5 已查:sim 相位、槽位状态、矩阵列、分析正负值、脱敏提示均有文字/徽标伴随。
- [x] A-6 已查:菜单/弹窗/弹层/PlayBar/Scope 段控沿用 button 与全局 focus-visible;Esc 关闭由既有 Modal/Popover 处理。
- [x] A-7 已查:全局 `prefers-reduced-motion` 已覆盖动画与 transition;FlowEdge/PulseRing/拖拽 hover 可静态降级。

## B. 逐屏走查

| 屏 | 项 | 状态 | 记录 |
|---|---|---|---|
| 7a | 点阵底、节点卡默认/选中、字段行、来源与文档角标 | 已修 | 选中节点强化 2px primary + glow;节点过渡走 token。 |
| 7a | 贝塞尔连线、协议标签、流动态 | 已修 | 默认关系线调整为灰阶;流动态保留琥珀虚线与文字标签。 |
| 7a | 对齐工具条、右键菜单、检查器、提示胶囊、运行入口 | 符合 | 既有结构与交互保持;未改写数据流。 |
| 8c | 槽位三态、违例槽、结构连线与标签 | 已修 | empty/activated/instantiated 边线规格收敛;违例仍有原因文字。 |
| 8c | 库面板、拖拽态、配置单 RefChip | 已修 | 库筛选计数补 `us-data`;RefChip 与合计 live 保持。 |
| 9b | 回放头部、HOW 运行态、节点相位、流动线 | 已修 | React Flow 水印隐藏;相位徽标与时刻文字保留。 |
| 9b | PlayBar、网络段控、时间线、指标瓦片 | 符合 | 数值走 `us-data`;reduced-motion 可静态降级。 |
| 2b | 配置行、列头计数、行头、记录卡 | 已修 | 卡片 hover/dragover 增强;矩阵底部胶囊文案保持 2b 原文。 |
| 2b | 停产列、拖拽可见性、点击 focus | 符合 | 停产卡 0.75;权限策略迁移后行为不变。 |
| 9a | 口径 chip、标题、来源、重新分析骨架 | 符合 | 不加功能;骨架沿用 token surface。 |
| 9a | 因素条、下钻表、洞察卡、操作行、TRACE | 已修 | 下钻正负值补双向文字色;数值保持 `us-data`。 |
| 5b | 三栏布局、顶栏、左栏计数、插件卡徽标行 | 符合 | 216/1fr/330 布局与 live 计数保持。 |
| 5b | 详情四段、SCOPE、操作行、USES 底注 | 已修 | 已停用插件可重新启用;不产生业务 ChangeEvent。 |
| 跨屏 | 侧栏语境底注、ShareDialog、脱敏、preview P2 | 已修 | ShareDialog 链接不含公网 URL;preview 三态已补。 |

## 自检

- [ ] `corepack pnpm verify:web`
- [ ] `node scripts/check-us-tokens.mjs`
- [ ] `node scripts/check-no-skipped.mjs`
