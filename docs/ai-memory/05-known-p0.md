# 剩余 P0（阻断 v0.1 发布的问题）

更新：2026-07-02

## 未消灭

1. **P0-5 docx 不是文档**（最大缺口）：DocxRenderAdapter 输出"一行 Output+objectId 平表"；导出范围按单一 objectType 平面快照，与文档树脱节。→ 卡 T-V01-4a/4b/4c。
2. **P0-2 校核不联动**：改参数后需手动点「重新校验」。→ 卡 T-V01-3（进行中）。
3. **30 分钟启动不可复现**：无亲测过的从零启动文档。→ 卡V 验收脚本。

## 已消灭（留档防复发）

- 入口断链（首页硬编码室内 workspace）→ 卡1 修复
- 示例无数值参数/比选/红灯 → 卡2 修复
- 发布基线混乱(HEAD 在特性分支、worktree 堆积) → 卡0 修复

## 观察项（尚未定级）

- .git/packed-refs 拷贝损伤疑点：本机 git 正常但需跑过一次干净的 `git fsck`
