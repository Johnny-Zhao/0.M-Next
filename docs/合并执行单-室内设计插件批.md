# 合并执行单 —— 室内设计插件批(F1 / F4 / ③ / D-INTERIOR)

决策 B(见 `docs/设计稿-领域插件机制.md`)。目标:补三个扩展点 + 出首个可装卸领域插件。**E:\0.M-Next 守 main;Codex 在 worktree 分支提交不合并,由人验证后 `--no-ff` 合入。**

## 卡与分支
| 卡 | 分支 | 域 | Docker e2e | 依赖 | 状态 |
|---|---|---|---|---|---|
| F4 DimensionRegistry | `feat/T-V33-plugin-f4` | web | 否(vitest) | 无 | ✅ 已完成已提交 `b71dbc4`,待合 |
| F0 模板 withdrawn 状态(迁移+转换) | `feat/T-V33-plugin-f0` | kernel+server | 是 | 无 | 待发(**人已授权动 schema**) |
| ③ /views/templates | `feat/T-V33-view-templates-a` | server | 是 | 无(查询需排除 withdrawn,建议 F0 后起) | 分支已建 |
| F1 ProfileLoader+装卸 | `feat/T-V33-plugin-f1` | server | 是 | **F0 在 main**(uninstall 调 withdraw/restore) | 分支已建,曾阻塞在 F0 |
| D-INTERIOR 首个插件 | `feat/T-V33-plugin-interior` | domains+server+web | 是 | **F1 且 F4 已在 main** | 未建(待 F1+F4 合后起) |

> **变更说明**:F1 执行时正确命中红线(模板无 withdrawn 状态),已拆出前置卡 **F0**(人授权的迁移/契约)。F1 现依赖 F0。

## 并行 / 串行(已据 F0 重排)
- **F4**:已完成,先合(前端、零后端)。
- **第一波(并行起)**:**F0** 与 **③** 可同时发(都 server Docker,**验证错峰**;文件低冲突:F0 动 kernel 迁移+meta 转换,③ 在 ViewQueryController/Dtos——但③ 的目录查询要排除 withdrawn,**建议 F0 先合、③ 基于含 F0 的 main 起**,避免回头改查询)。
- **第二波**:F0 合入 main 后发 **F1**(uninstall 调 F0 的 withdraw/restore)。F1 分支从**含 F0 的 main** 起。
- **第三波**:F1、F4 都在 main 后发 **D-INTERIOR**(`install` 要 F1、注册风/光要 F4)。从**含 F0+F1+F4 的 main** 起。

## 合并顺序(逐张:人验证 → `--no-ff`)
1. **F4**(前端、最安全)先合。
2. **F0**(迁移+转换)合(合前 `git merge main` 拉平)。
3. **③**(基于含 F0 的 main)合。
4. **F1**(基于含 F0 的 main)合。
5. **D-INTERIOR** 最后合(基于 F0+F1+F4+③ 的 main)。

> 每张合前:分支先 `git merge main` 拉平基线,再在本机跑该域验证,绿了才 `git merge --no-ff <branch>`。

## 每张完成时 Codex 必须回报
- `git diff --stat main`(确认封闭文件清单、无越界、无 core 业务逻辑/契约/迁移 diff)。
- 测试汇总行:server 看 **`Skipped:0`** + check-no-skipped;web 看 `pnpm --filter @m-next/web test` 通过。
- F1/D-INTERIOR 额外贴**装卸 E2E** 的关键断言行(卸后目录不含 + 老工作空间仍可用 + 重装恢复)。

## 验证命令(人在本机 / Codex 跑)
- 前端(F4、D-INTERIOR 前端部分):`corepack pnpm install` → `corepack pnpm verify` → `pnpm --filter @m-next/web test`。
- 后端(F1、③、D-INTERIOR 后端):Maven 构建 + 起 Docker 跑对应 e2e;**逐张错峰**,看汇总 `Skipped:0`。
- 合并后在 main 复核:`git diff --stat main~1 main` + 重跑该域验证。

## 红线复述(四张通用)
- **不改 core 业务逻辑/命令/事件契约/求值逻辑;不加业务迁移**(F1 的 withdraw 复用现有状态,否则停下回报)。
- 扩展点(F1/F4)是**人发起的 core 基建**;领域(D-INTERIOR)**经 F1 装载 + F4 注册,core 一行业务逻辑不改**。
- **可装卸**:F1/D-INTERIOR 的卸载**非破坏可逆**,绝不硬删模板/工作空间/用户数据。
- 不臆造 DSL(规则表达不了 → 拆或停)、不新增依赖(AG-502)。
- **任一卡命中"需改契约 / 需新迁移 / 定义命令取不到 service / 清单需改构建才能被读" → 停下回报,不夹带。**

## 环境备忘(沿用)
- sandbox git 损坏 → 一律 Codex 在 Windows 本机跑 git/maven/pnpm;人验证靠**读 main 文件 + bundle/产物变化**确认是否真合入(勿假设已合)。
- postgres `Connection refused` 刷屏 + "Surefire kill self fork JVM" = testcontainers 拆容器的善意噪声;`@charset`/661kB chunk = vite 善意告警。
- Windows 无 `grep` → 用 `findstr`/`Select-String`。
- `git worktree remove` 被 vite/Codex 占用失败 → `--force` + `git worktree prune`。

## 完成态(本批 DoD)
四张全绿合入 main 后:HOME 向导经 `/views/templates` 真列出"室内设计" → 选→配→实例化 → 进工作台画户型 → 派生(面积/窗地比)+ 规则灯(采光/通风/热)+ 切风/热/光维度 全来自插件;**卸载插件后目录消失、老工作空间仍可用、重装恢复**。即"领域=可装卸插件"跑通。
