# 任务卡 T-US-009 — 同源 P2 批2:模板与实例化 8c(槽位/库面板/实例化闭环)

- 状态:**可下发**(纯前端、零新依赖、全 Mock)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(8c 屏)、设计系统 §02(节点卡/槽位卡)/§03(槽位状态)、交接规格 §02(NodeCard/SlotCard/LibraryPanel)/§05(画布·violated 槽位)/§06(拖拽实例化)/§08-⑨(槽位校验时机:实例化即时+保存全量,首版取即时)、docs/tasks/T-US-008(审查结论)` + 自检输出段
- 序列位置:P2 五卡之 **批2**;复用 008 画布底座;TPL 校验与 006 规则引擎在本卡合拢

## 背景与关键决策(必读)

008 已合入:RF 画布底座、updateViewConfig/inverseView、删除闭环。本卡做 8c —— 「模板存抽象槽位+约束,不存具体记录」的核心叙事:槽位是抽象的主板/CPU,库里才是具体型号,拖入即绑定,字段随库更新。

**决策 1:装机数据域进 seed,但不污染主线。** 新 ObjectType `hardware_products`(硬件产品库,组「供应链」;字段:name/part_type(主板|CPU)/chipset/form_factor(enum ATX|mATX)/socket/vrm/price(CNY)/cores?)+ 7 对象(6 主板对照 8c 屏:ROG Maximus Z890 Hero ¥5,299 / Strix Z890-E ¥3,999 / TUF Z890-Plus ¥2,599 / Prime Z890-P ¥1,999 / TUF B860-Plus ¥1,599(B860)/ Prime B860M-A ¥1,299(B860·mATX);+ CPU Intel Core Ultra 7 265K `#C-102` LGA1851/20C/¥2,899)。**Expression 增可选 `space?: 'main'|'workshop'`**:首页与侧栏 WHAT 默认只列 main(保持 6 卡不变);装机 3 表达(装机方案·Z890 高配(canvas+模板)/装机方案·B860 性价比(canvas+模板)/装机配置单·Z890(doc))标 workshop,当前路由位于 workshop 表达时侧栏列 workshop 表达 + 「模板 TEMPLATES」段。相关 VM/测试同步(首页 6 卡断言不回归)。
**决策 2:SlotBinding 从「值载体」升级为「对象绑定」,TPL 规则迁移。** 006 的 `slotBindings.values.form_factor` 临时载体重构为 `SlotBinding { id, templateId, slotId, exprId, objectId: string | null }`;TPL-001/003 规则改读**绑定对象的字段**(live):B860 性价比表达的主板槽绑定 `Prime B860M-A`(对象 form_factor='mATX')→ TPL-003 警告的真实来源;9c「换用 ATX 型号」fix 改为**换绑** objectId 到首个符合约束的 ATX 主板。规则与既有测试迁移逐条在 commit message 说明。`SceneTemplate.slots` 扩:`SlotDef { id, abstractType, label, constraints: {field, op:'eq'|'gte'|'lte', value}[] 或保留字符串数组+结构化并存, shownFields: string[], connectsTo?: slotId[] }`(连线由模板结构派生:CPU—主板 实线(两端已实例化)/主板—内存、主板—显卡 虚线灰);`装机方案 V1` 模板定 5 槽:CPU(已绑 #C-102)/电源(已绑占位对象或标记内置✓)/主板(**激活待实例化**)/内存(待)/显卡(待)——顶栏琥珀灯「N/M 槽位待实例化」**live 派生**(设计稿「2/5」为示意,以数据为准)。
**决策 3:实例化 = 点击为主 + 原生拖拽增强。** 库卡点击 → 绑定到当前激活槽位(点击槽位可切换激活;无激活时取首个未绑定且约束匹配的槽);同时实现 HTML5 draggable(库卡拖起 → 源卡显示 Mono「拖拽中…」态 + 槽位 dragover 高亮(底色加深+glow)→ drop 绑定;DragGhost 简化用浏览器默认+源卡态,不做 -2° 自绘浮标)。**拖拽超预算可降级为仅点击,PR 声明**(弹性条款)。绑定/换绑 = data 轨事件(「实例化槽位 主板 → Prime Z890-P」)+ 活动流 + TPL 规则自动重跑(交接规格 08-⑨ 首版取「实例化即时校验」)。
**决策 4:「生成配置单 DOC」接同一条阻断管线。** 按钮墨底:存在校验 error(全局)或仍有未实例化槽位 → 禁用 + tooltip 原因(呼应 9c 脚注「错误会阻断生成配置单/分享」);可用时点击 → 跳转 `装机配置单·Z890`(doc 表达,seed 预置 DocModel:配置单表格行 = 各槽位(RefChip 引用绑定对象的名称/价格)+ Mono 合计行(live 求和))+ Toast。「另存为模板」= 占位 Toast。

## 目标

`/us/expr/exp-build-z890?form=canvas` 按 8c 完整还原:模板画布(槽位卡三态+已实例化节点+结构连线)、右 316 库面板(约束过滤 chips + 6 主板卡 + 点击/拖拽实例化)、左栏 workshop 表达列表 + 模板区、顶栏模板标签与待实例化琥珀灯、生成配置单闭环;TPL 校验迁移为真对象绑定并保持 9c 修复链路可用。

## 涉及文件(封闭清单)

**A. 模型与种子**

- `model/kernel.ts` — `SlotDef` 扩(label/constraints 结构化/shownFields/connectsTo);`model/view-layer.ts` — `Expression.space?`、`SlotBinding` 重构(决策 2)。
- `seed/demo-seed.ts` — 决策 1 全量(hardware_products 类型+7 对象/3 workshop 表达+ViewDef/装机方案 V1 五槽模板/slotBindings:Z890 表达 CPU+电源已绑、主板内存显卡空;B860 表达主板槽绑 B860M-A(TPL-003 载体);装机配置单 DocModel);权限矩阵补 workshop 资源列(王芸/李晓 edit,陈默/周然 readonly)。
- `state/workspace-store.ts` — `bindSlot(bindingId 或 slotId+exprId, objectId, actor)` / `unbindSlot`(data 轨事件+活动流);`updateSlotBinding` 迁移/删除(决策 2);共置测试。
- `validation/rules.ts` — TPL-001/003 改读绑定对象字段;`validation-store` fix `TPL-003 换用 ATX` = 换绑(决策 2);测试迁移。

**B. 模板画布(新目录 `template/`,复用 canvas 基建)**

- `template/template-view-model.ts`(纯函数,共置测试)— 模板+绑定+workspace → RF 节点(**槽位卡三态**:instantiated=白实卡+来源徽标 `硬件产品库 #C-102`+「已实例化」+绑定对象字段行(shownFields:芯片组/供电/价格 Mono);activated=2px 虚线 primary+外发光+底 primary-bg 半透明+「拖入实例化」徽标+「抽象:主板」+约束行 Mono+显示字段行;empty=1.5px 虚线灰半透明+「抽象:内存 ×2」+约束行;violated=琥珀边+原因(B860 表达))+ 结构连线(实线绿+socket 标签 / 虚线灰+DDR5/PCIe 标签,由 connectsTo 与两端绑定态派生)+ 待实例化计数 N/M + 库面板 VM(激活槽约束 → 符合约束 N/按 chipset 分组计数/每卡 匹配|不匹配|已绑定 态)。
- `template/template-canvas.tsx` — RF 装配(nodeTypes: slotCard;节点拖动仍写 view config 复用 008);工具栏(选择 active/「添加槽位」占位 Toast/连线占位;右「另存为模板」线框占位+「生成配置单 DOC」墨底(决策 4));点击槽位 = 激活(本地 UI 态);槽位 drop 目标(决策 3);左下提示胶囊(8c 文案);只读身份禁用实例化与生成。
- `template/slot-card.tsx` — 三态+violated 渲染(设计系统「节点卡/槽位卡」规格);dragover 态。
- `template/library-panel.tsx` — 右 316(底 topbar):头「库 · 主板 LIBRARY」+ Mono「硬件产品库 › 主板 · N」;筛选 chips(符合约束 N(墨激活)/Z890 · n/B860 · n);卡列表(名称 600/Mono 规格行 chipset·form_factor·vrm/价格 Mono 右;selected=2px primary+阴影;不匹配约束=置灰+「不符约束」小注;已绑定=「已在图中」);点击/拖起 → 决策 3;底注(8c 文案)。
- `shell/app-sidebar.tsx` — workshop 上下文:WHAT 列表按当前表达 space 过滤;「模板 TEMPLATES」段(书本图标+名称+「使用中」徽标(有绑定引用时));底注切换(8c 文案)。**仅在 workshop 路由下变化,main 路由零回归。**
- `pages/expr-page.tsx` — workshop canvas 表达 → TemplateCanvas(替代 CanvasView);顶栏 breadcrumbTail Mono「模板:装机方案 V1」+ 琥珀灯「N/M 槽位待实例化」(全实例化转绿「槽位已全部实例化」);HOW 行 forms=视图+配置单 DOC(点配置单 tag → 跳配置单表达)。
- `us-components.css` — 追加 `.us-slot-*`、`.us-library-*`、`.us-tplbar-*`;随组件同 commit。

## 行为要求(逐条可测)

1. 三态渲染与 seed 一致(CPU/电源已实例化、主板激活、内存显卡空);连线实/虚与标签按绑定态派生;顶栏琥珀灯计数 live。
2. 实例化:点击库卡(约束匹配)→ 主板槽转白实卡+字段行(live 读对象)、data 轨事件+活动流、计数 -1、TPL 重跑;点击不匹配卡 → 拒绝 + Toast 原因;换绑(对已实例化槽再点其他匹配卡)→ 覆盖绑定 + 事件。
3. 拖拽增强(若未降级):拖起源卡显「拖拽中…」、目标槽 dragover 高亮、drop 等效点击绑定;Esc/取消拖拽无写入。
4. 约束过滤:激活主板槽(LGA1851/ATX)时「符合约束」计数 = ATX Z890/B860 主板数;mATX 的 B860M-A 显示不匹配;切换 chips 过滤列表。
5. TPL 迁移:B860 表达主板槽绑 B860M-A → TPL-003 warning(读对象 form_factor);9c「换用 ATX 型号」→ 换绑首个匹配 ATX → warning 消失;TPL 相关既有断言迁移后全绿。
6. 生成配置单:未全实例化 → 禁用+tooltip「仍有 N 个槽位未实例化」;全实例化但全局有校验 error → 禁用+校验原因;两者皆无 → 点击跳转配置单 doc(表格行 RefChip live 值+合计正确)。
7. 字段随库更新:在表格页改 Prime Z890-P 价格 → 模板画布已绑槽字段行与配置单合计同步(引用 justSynced 联动)。
8. main 线零回归:首页仍 6 卡;主线侧栏无 workshop 表达;`exp-portal` 画布(008)行为不变。

## 测试要求(vitest 共置,纯逻辑优先)

约束匹配纯函数(eq/gte/尺寸上限/多条件);template-view-model(三态派生/连线派生/计数/库面板分组与匹配态);workspace-store bindSlot/unbindSlot(事件+活动流+换绑覆盖);TPL 规则迁移(B860 违例→换绑后通过);生成配置单可用性纯函数(未实例化 N/校验 error 两分支);配置单合计派生;Expression.space 过滤(首页 6 卡断言、workshop 列表断言);008/006 既有测试零回归(迁移项逐条注明)。

## 验收标准(机器可判 + 视觉 + 手工)

1. `corepack pnpm verify:web` 全绿(本机权威);tokens 门禁 0 违规;
2. 手工重点链:打开 Z890 高配 → 点击/拖入 Prime Z890-P 实例化主板 → 计数 2/5→…递减 → 依次实例化内存显卡(seed 为内存/显卡补 2-3 个可选对象,含至少一个不匹配项)→ 生成配置单解锁 → 跳转配置单看 live 值与合计 → 回表格改主板价 → 配置单合计变;切 B860 表达看 violated 槽 → 9c 换绑修复;
3. 视觉:1440×900 对照 8c(左栏模板区/槽位三态与激活发光/库面板 chips 与六卡/拖拽中态/提示胶囊/顶栏模板标签+琥珀灯/工具栏按钮组);
4. `git diff --stat main` 限 `packages/web/src/unisource/**`;每步一 commit;CSS 随组件同 commit。

## 禁止事项

禁止实现:「添加槽位」「另存为模板」「连线」编辑(占位)、模板 CRUD、B860 表达的完整画布布置(仅需其槽位绑定支撑 TPL 剧本,画布可复用同模板简排)、仿真(010)/矩阵/分析/插件(011/012)、-2° 自绘 DragGhost 浮标、跨库换源(「换一套数据源整图复用」仅底注文案)。禁止新增依赖;禁止 import workbench/views;禁止整文件重排 CSS;禁止改 P0/002 CSS 段;禁止 localStorage 业务数据(AG-102);选择联动零写入(AG-209);禁止触碰 unisource 之外的代码。完成后停止等待审查。
