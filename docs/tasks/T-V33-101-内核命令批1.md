# 任务卡 T-V33-101 — 阶段1 批1:数据内核命令(CreateObject/UpdateFields)

- 状态:**已下发,待用户批准后开工**(按总控计划 Q-5 约定,阶段1 任务卡不预先授权)
- 分支:`feat/T-V33-101-kernel-commands-batch1`(单分支,AG-401)
- PR 要求:`Spec-Ref: D.1-CreateObject, D.1-UpdateFields, 6.11-数据内核, 8.9-热路径, 契约§3.1/§3.2/§5/§6/§7` + **写后自检输出段(AG-405,缺失即拒)**
- 执行者:Codex。**写入纪律 AG-405 全程强制**:新建文件每段 ≤40 行分段追加、写后即验贴输出;修改既有文件只用最小补丁。

## 背景依据

contracts/数据内核命令与事件契约.md v1.1(实现蓝本)、contracts/schemas/*(机器契约)、packages/shared/contracts/error-codes.yaml(错误码登记)、AGENTS.md(全部 AG 规则)。kernel 当前仅有 Workspace 桩。

## 目标

实现契约前两条命令的可运行纵切:HTTP 命令入口 → 权限预检挂点 → 校验 → 主数据+版本+event_outbox 同事务写入 → 幂等保障。**只做 CreateObject 与 UpdateFields**,为批2(关系命令)留好结构。

## 涉及文件(封闭清单)

- `packages/kernel/src/main/java/com/mnext/kernel/api/**`:命令 DTO(CreateObjectCommand/UpdateFieldsCommand/CommandResult)、事件 DTO(EventEnvelope/ObjectCreated/FieldChanged/ObjectUpdated)、`KernelCommandService` 接口、`PermissionChecker` 接口
- `packages/kernel/src/main/java/com/mnext/kernel/internal/**`:命令处理器、仓储(JDBC)、幂等(command_log)、Outbox 写入、`AllowAllPermissionChecker`(临时实现,类注释标注 `// TODO 阶段4 权限矩阵替换`)
- `packages/kernel/src/main/resources/db/migration/V1__kernel_batch1.sql`:Flyway 迁移
- `packages/server/src/main/java/com/mnext/server/**`:`POST /workspaces/{workspaceId}/commands` 端点(仅受理上述两命令,其余 commandType 返回 KERNEL-400-UNKNOWN-COMMAND)、全局错误响应映射(error-codes.yaml 口径)
- `packages/kernel/src/test/**`、`packages/server/src/test/**`:单测与集成测试(Testcontainers PG16)
- `tests/contracts/fixtures/events/valid/`:**新增 1 个由实现真实序列化产出的 FieldChanged 信封样例**(借既有 contracts 门禁做事件契约校验)
- `packages/kernel/pom.xml`、`packages/server/pom.xml`、根 `pom.xml`(依赖与 jacoco)
- **新增** `ci/deps-allowlist.yaml`:登记当前全部直接依赖(补 AG-502 缺口)
- `.github/workflows/ci.yml`:test job 确保跑 maven test(若已覆盖则零改动)

## 本卡准入依赖(AG-502,逐项已核:纯 JVM/官方 arm64 兼容/许可合规)

spring-boot-starter-web、spring-boot-starter-jdbc、org.postgresql:postgresql(BSD-2)、org.flywaydb:flyway-core + flyway-database-postgresql(Apache-2.0)、com.github.f4b6a3:ulid-creator(Apache-2.0)、spring-boot-starter-test、org.testcontainers:postgresql/junit-jupiter(MIT,test scope)、org.jacoco:jacoco-maven-plugin。**此外一律禁止。**

## 数据结构变化(Flyway V1,表名/字段按契约 4.7 与 §5/§6/§7)

`workspace`、`object_type`、`field_def`(本批为只读种子数据,迁移内插入 1 个演示类型+3 个字段定义;模板管理属阶段2)、`data_object`(id/workspace_id/object_type_id/status/version/created_by/updated_by/时间戳)、`data_field_value`(唯一键 object_id+field_def_id;value JSONB;version;审计列)、`field_value_history`(历史版本行,不可变)、`command_log`(唯一键 workspace_id+idempotency_key;payload_hash;result_snapshot JSONB;decided_at)、`event_outbox`(契约 §7.1 全字段 + sequence,(status,created_at) 部分索引)。

## 行为要求(逐条可测,均引契约条款)

1. 命令信封校验与响应结构 = 契约 §1.4/§1.5;actor 取自请求头 `X-Actor-Id`(临时认证,类注释标注阶段4 替换),**禁止**从载荷读;
2. 事务边界 = §1.2:单事务内完成主数据+历史+command_log+outbox;事务内零 HTTP 出站(AG-201);
3. CreateObject = §3.1:类型/字段码校验、必填校验挂点(本批仅必填,规则 DSL 属阶段3)、ObjectCreated + 每个非空字段 FieldChanged(before=null);
4. UpdateFields = §3.2 + §5:字段级 expectedFieldVersion 优先判定 [R-1],值未变不发事件、不加版本;冲突响应含 details.conflictingFields 与 suggestion(§5.3 结构);
5. 幂等 = §6:同键同 payloadHash 重放返回首次结果 idempotentReplay=true 零副作用;同键异 hash → KERNEL-409-IDEMPOTENCY-CONFLICT 并返回原 commandId;
6. Outbox = §7.1:信封 13 字段齐全,sequence 按聚合递增;**本批不实现投递器**(批4),只保证落表正确;
7. 错误码仅用 error-codes.yaml 已登记码;审计列非空(AG-321)。

## 测试要求(AG-504:禁止 sleep;Testcontainers 同步断言)

- 内核命令单测覆盖率 ≥80%(jacoco 门禁),必含异常路径/权限调用断言/幂等键(11.1);
- 契约 §9 场景测试:#1 并发同字段(后提交者 409+conflictingFields)、#2 并发不同字段(双双成功,对象版本 +2)、#11 幂等键复用异载荷;
- 事务原子性:命令失败时 data_object/outbox/command_log 零残留;成功时四表同帧可见;
- PermissionChecker 被调用的 verify 断言(AG-503);
- 真实序列化的 FieldChanged 信封写入 fixtures 后 `pnpm contracts:check` 通过(事件契约闭环)。

## 验收标准(机器可判)

1. `pnpm verify` 全绿(含 maven test);2. jacoco kernel 包行覆盖 ≥80%;3. `pnpm contracts:check` 通过且 fixtures 新增 1 个事件样例;4. 集成测试演示完整链:POST 创建对象 → 改字段 → 版本冲突 → 幂等重放,断言全过;5. `git diff --stat` 限于封闭清单;6. PR 含 AG-405 写后自检输出与上述命令输出;7. ArchUnit/architecture:check 通过(kernel 零越界依赖)。

## 禁止事项

禁止实现:关系命令(批2)、ChangeState/Archive/SoftDelete/Batch(批3)、Outbox 投递器与 RabbitMQ(批4)、OpenAPI 生成(批4)、规则 DSL、AI、读模型、任何 backlog 条目。禁止修改:contracts/**、ADR/**、AGENTS.md、docs/**、packages/{views,web}/**、docker-compose.yml。禁止引入准入清单外依赖。禁止改动既有 Workspace/Identifier 桩的公共签名(可扩展)。完成后停止,等待 Claude 审查。
