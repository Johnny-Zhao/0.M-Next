# 任务卡 T-V33-102 — 阶段1 批2:关系命令(CreateRelation/UpdateRelation/Unlink)

- 状态:**已批准,可开工**(随 v0.3-kernel-batch1 验收通过下发)
- 分支:`feat/T-V33-102-relation-commands`(从最新 main 切出,AG-401)
- PR 要求:`Spec-Ref: D.1-CreateRelation/UpdateRelation/Unlink, 契约§3.4/§3.5/§3.7/§9, ADR-004, 8.11-红线2` + AG-405 写后自检输出段(缺失即拒)
- 依据:contracts/数据内核命令与事件契约.md(实现蓝本)、ADR-004(关系表+双向复合索引+闭包表)、AGENTS.md 全部 AG 规则

## 目标

实现关系作为一级数据对象的三条命令,复用批1 的命令骨架(信封校验/权限挂点/幂等/Outbox 同事务);附带一项工程链修正(jacoco 接电)。

## 涉及文件(封闭清单)

- **新增** `packages/kernel/src/main/resources/db/migration/V2__kernel_batch2.sql`
- `packages/kernel/src/main/java/com/mnext/kernel/api/**`:CreateRelationCommand/UpdateRelationCommand/UnlinkCommand DTO,RelationCreated/RelationUpdated/RelationUnlinked 事件
- `packages/kernel/src/main/java/com/mnext/kernel/internal/**`:三个 Handler、关系仓储方法、闭包表维护
- `packages/server/src/main/java/com/mnext/server/CommandController.java`(switch 增三个 case,最小补丁)
- 对应测试文件(kernel/server)
- `tests/contracts/fixtures/events/valid/`:新增 1 个真实序列化的 RelationCreated 信封样例
- **工程链修正(本卡批准)**:`package.json` 的 `"test"` script 中 `run-maven.mjs test` 改为 `run-maven.mjs verify`(使 jacoco:check 真正执行——批1 验收记录的观察项 1)
- **禁止新增任何依赖**(批1 准入集已够用)

## V2 迁移内容(按契约 4.7/ADR-004)

`relation_type`(id/workspace 模板归属/code/source_type/target_type/direction/cardinality/semantics(weak|strong)/hierarchical bool);`data_relation`(id/workspace_id/relation_type_id/source_id/target_id/status/version/created_by/updated_by/时间戳)+ **三组索引**:(workspace_id,source_id,relation_type_id)、(workspace_id,target_id,relation_type_id)、防重**部分唯一索引**(relation_type_id,source_id,target_id) WHERE status='ACTIVE';`relation_history`;`relation_closure`(ancestor_id,descendant_id,depth,主键二元组)。种子:2 个关系类型——`depends_on`(弱语义,多对多)、`decomposes_to`(层级类 hierarchical,1 对多)。

## 行为要求(逐条可测,引契约条款)

1. **CreateRelation = §3.4 全量**:端点存在/同 workspace/状态非 VOID/DELETED → KERNEL-422-ENDPOINT-INVALID(不可见与不存在同码,不泄露存在性 [R-9]);事务内对两端按 **id 升序加行锁** [R-5];类型/方向/端点类型匹配;防重 → KERNEL-409-DUPLICATE-RELATION(返回已存在 relationId);基数超限 → KERNEL-422-CARDINALITY-VIOLATION(返回基数定义与现值);hierarchical 类型同事务维护闭包表,成环 → KERNEL-409-CYCLE-DETECTED(返回环路径);
2. **UpdateRelation = §3.5**:fields 更新走字段语义;**端点变更按 Unlink+CreateRelation 语义整体重校验**(查重/基数/环);事件 RelationUpdated 的 before/after 含端点;
3. **Unlink = §3.7**:relationId 或三元组定位;status → UNLINKED(保留行+历史);重复解除幂等成功;hierarchical 解除时同事务删除对应闭包行;`acknowledgeImpact` 字段实现但派生数据判定**本批固定返回"无派生"**(类注释 `// TODO 阶段3 规则引擎接入派生判定`),禁止实现任何生成/规则逻辑;
4. 幂等/审计/Outbox 同批1 机制复用;事件 sequence 沿用聚合版本约定(RelationXxx 聚合 = relationId)。

## 测试要求(jacoco 0.80 门禁本批起真实生效;AG-504 禁 sleep)

集成测试必含(对应契约 §9):防重(#8 双发同关系,后者 409+已存在 id)、基数超限、**层级成环**(A→B→C 后建 C→A,断言 409-CYCLE 且零写入)、端点不存在与端点 DELETED 同返回 ENDPOINT-INVALID(#9 不泄露存在性)、并发同端点对加锁串行(两线程同时建同关系,恰一成功)、Unlink 幂等 + 闭包行清除、UpdateRelation 端点变更重校验、幂等键重放零副作用。单测覆盖 Handler 异常路径。RelationCreated 样例进 fixtures 后 `pnpm contracts:check` 通过。

## 验收标准(机器可判)

1. `pnpm verify` 全绿(**含本批接电的 jacoco:check,kernel ≥0.80**);2. `pnpm contracts:check` 通过且 fixtures 新增 RelationCreated;3. 集成测试演示链:建两对象→建关系→重复建(409)→建层级链→成环(409)→Unlink→重 Unlink(幂等);4. `git diff --stat main` 限封闭清单;5. PR 含 AG-405 自检输出与全部验收命令输出;6. architecture:check 通过。

## 禁止事项

禁止实现:ChangeState/Archive/SoftDelete/Batch(批3)、Outbox 投递器(批4)、规则引擎/派生数据生成、深度遍历查询 API(读模型属阶段5)、任何 backlog 条目。禁止触碰:contracts/**、AGENTS.md、ADR/**、docs/**、V1 迁移文件(迁移不可变,只能新增 V2)、packages/{views,web}/**。禁止新增依赖。每步一 commit,完成后停止等待审查。
