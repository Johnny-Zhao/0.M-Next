# 任务卡 T-V33-S1 — 收尾批1:质量基线(黄金路径 E2E + 文档 + AG 载体审计)

- 状态:**可下发**(无内核契约门、无新依赖、无迁移;从最新 main 切出即可开工)
- 分支:`feat/T-V33-S1-quality-baseline`(从最新 main 切出,AG-401)
- PR 要求:`Spec-Ref: 说明书 §11.2 关键闭环验收用例、§10.1 首验场景, AGENTS.md 全量 AG 规则` + AG-405 自检输出段
- 依据:在扩更多功能前固化质量基线;承 MVP-0 + 阶段6/7/9 全部已合能力
- 对应:收尾(非新功能)——把"全链路可跑通 + 文档可上手 + 红线有载体"钉死

## 目标

不加新功能,**固化质量基线**:① 一条**黄金路径 E2E 集成测试**串起全链;② **README/快速上手**文档;③ **AG 规则 → CI/测试载体**审计表(标出有无对应自动检查)。**纯增量:不改任何既有产线逻辑。**

## 涉及文件(封闭清单)

- **新增** `packages/server/src/test/java/com/mnext/server/GoldenPathE2ETest.java`(Testcontainers,`mnext.outbox.enabled=false` 去噪):一条测试走完——
  1. 元命令建对象类型/字段定义/关系类型(201);
  2. 建对象 + 关系(M1 命令);
  3. 查 `/views/objects`、`/views/tree`、`/views/matrix`、`/views/relations`(读模型可见);
  4. 建三级评审批注 + 查询(R01);
  5. JSON `export → preview → apply`(702/707):改一字段 + 加一对象,经命令回写、读模型刷新;
  6. 捕快照(704)→ `/outputs` 渲 html(708a)→ 取产物含数据;
  7. 断言全程主数据经命令入口、读模型最终一致。
- **新增/更新** `README.md`:一键校验(`pnpm verify`)、模块结构(shared/kernel/engines/server/views/web)、CQRS 读写链、两个扩展点(`ExchangeAdapter`/`RenderAdapter` SPI + ServiceLoader)、如何开发适配器插件(指向 docs/11、docs/插件-交换适配器开发指南.md)。
- **新增** `docs/AG-载体审计.md`:表格逐条列 AGENTS.md 的 AG-1xx/2xx/3xx/4xx/5xx 红线 → 其**自动载体**(`architecture:check`/`architecture:test`/`contracts:check`/spotless/checkstyle/jacoco/具体架构测试类/集成测试),无载体者标注"⚠️ 仅人工/待补"。
- **不改** 任何 `src/main` 产线代码、迁移、契约、AGENTS。

## 行为要求(逐条可测)

1. **E2E 真跑通**:`GoldenPathE2ETest` 在 Docker 起着时端到端通过(Skipped:0 时);各步断言到位(读模型可见、回写经命令、输出含数据)。
2. **只读不改产线**:E2E 只调既有公开端点/命令,不新增产线类、不改既有逻辑。
3. **文档可上手**:README 让新人据其 `pnpm verify` 跑通并理解扩展点。
4. **审计完整**:AG 载体表覆盖 AGENTS.md 现有全部 AG 条目;⚠️ 项如实标注(便于后续补 CI)。

## 测试要求(jacoco ≥0.80;AG-504 禁 sleep)

`GoldenPathE2ETest` 为主交付;不得用 sleep(用轮询/同步投影或既有测试辅助等待读模型一致,参照现有集成测试做法)。其余为文档,无新单测要求(E2E 计入 server 覆盖)。

## 验收标准(机器可判)

1. `pnpm verify` 全绿(贴 jacoco;Docker 起着、Skipped:0,E2E 真执行);2. `pnpm architecture:check` 通过;3. `git diff --stat main` 限封闭清单(`src/main` 零改);4. AG 载体审计表条目数 = AGENTS.md AG 条目数,⚠️ 项有清单;5. PR 含 AG-405 自检。

## 禁止事项

禁止:改任何 `src/main` 产线代码/迁移/契约/AGENTS、加新依赖、加新功能、改既有测试语义(仅新增 E2E)、引入 sleep。E2E 只读既有接口、不新增产线行为。每步一 commit,完成后停止等待审查。

## 给 Codex 的落盘自检(防截断)

新增 `.java`/`.md` 落盘后:大括号/语法完整;spotless:apply、prettier --write 对新增 md 不报错;编译过再跑测试。禁止提交语法不完整文件。每步一 commit,完成后停止等待审查。
