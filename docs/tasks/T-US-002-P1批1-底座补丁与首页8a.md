# 任务卡 T-US-002 — 同源 P1 批1:底座补丁 + HOW 形式行 + 首页 8a

- 状态:**可下发**(纯前端、零新依赖、全 Mock、不触后端)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(8a 屏)、交接规格 Handoff Spec.dc.html §02/§06、docs/tasks/T-US-001(审查结论)、AGENTS.md AG-101/102/209` + 自检输出段
- 序列位置:P1 剧本主线 5 卡之 **批1**;后续 003(表格+分屏)/004(文档)/005(AI 导入+对话)/006(校验+权限+剧本端到端)全部依赖本卡

## 背景(接 T-US-001,必读)

T-US-001 已合入:内核形状模型(15 实体+ObjectTypeDef)、唯一 Mock 种子(剧本时间线)、workspace/selection/changeset 三 store、页面骨架已全部改读 store。**审查发现三个缺口 + P1 需要两块新底座**,必须在首屏之前补齐,故本卡 = 底座补丁 + 第一块业务界面(首页 8a)+ 全表达页共用的 HOW 形式行。

审查缺口(原样修):
1. seed 缺 8b 权限矩阵(T-US-001 卡要求了但未实现);
2. 关系写路径(createRelation/updateRelationField/unlinkRelation)不产生 ChangeEvent/活动流;
3. `changeset-store` 对不存在的 changeSetId 抛异常,与 `ChangeSetResult` 返回值契约不一致。

P1 新底座:
4. SessionStore(当前身份 + 权限求值 + 越权写转审批)—— 没有它,003 的表格编辑、006 的权限页都无法按剧本工作;
5. FormRow(HOW 描述形式行)—— 除首页/设置页外所有屏共用(交接规格 §02 FormRow/FormTag/AddFormButton,G 级)。

## 目标

首页 8a 按设计稿完整还原并全量走 store;FormRow 落地并接入表达页/表格页(URL `form=` 双向同步);SessionStore + 演示身份切换器可用;三个审查缺口修复。**不做表格编辑/文档/分屏/AI 界面(003–005),不做校验/权限页 UI(006)。**

## 涉及文件(封闭清单)

**A. 底座补丁**

- `model/kernel.ts` — 追加:`PermLevel = 'admin'|'edit'|'owner'|'readonly'|'none'`;`PermissionMatrix = Record<MemberId, Record<string /*resourceCode: objectType code 或 expression id*/, PermLevel>>`。不改既有实体。
- `seed/demo-seed.ts` — 追加 `permissions: PermissionMatrix`,按 8b 屏:王芸 admin/admin/edit/owner;李晓 edit/readonly/readonly/edit;陈默 readonly/edit/edit/readonly;周然 readonly/none/none/readonly(资源列:产品规格库/渠道销量表/渠道经营看板/S3 规格书);AI 无固定行(跟随发起人)。
- `state/workspace-store.ts` — 关系三个写操作补:产生 `ChangeEvent`(track='data',target=relation,`inverse: null`,undo 仍仅支持字段事件——在事件上注明)+ 活动流条目;`getPermissions()` 选择器。
- `state/changeset-store.ts` — `confirmAll/reject/acceptItems` 对不存在 id 返回 `{ ok:false, reason:'找不到变更集…' }`,不再 throw(内部 `requireChangeSet` 改查找判空);补单测。
- `state/session-store.ts`(新)— 当前身份(默认 wangyun,含 AI 代理发起人概念)+ `switchMember(id)`;`can(memberId, resourceCode, action: 'read'|'editData'|'editView'|'admin')` 按 PermissionMatrix 求值(edit/owner/admin 可 editData;readonly 仅 read+引用;none 不可见值);`requestWrite(...)`:有权 → 直接走 workspace-store 写路径;越权 → 组装 `ChangeSet(source:'manual', actor=当前身份, items=[该写入])` 经 changeset-store.submit 转 pending,返回 `{ queued: true, changeSetId }`。共置单测。
- `shell/role-switcher.tsx`(新)— 顶栏头像点击弹出成员列表切换当前身份(含「同源 AI(代理)」说明行,不可选为身份);**开发演示工具**,视觉低调(复用 ContextMenu 样式规格:白底 shadow.float),不属于设计稿组件清单。

**B. HOW 形式行(G 级,交接规格 §02/§06)**

- `shell/form-row.tsx`(新)— 44px 行:mono「描述形式 · HOW」标签 + `FormTag` 列表(active=墨底+金图标,inactive=描边;点击切换 → URL `form=` 同步)+ `AddFormButton`(虚线绿,点击开 `AddFormMenu`:内置六形式 + seed 中已启用插件的 forms + 分隔 + 「管理插件」→ `/us/settings/plugins`,菜单 shadow.float)+ 右侧上下文槽(children)。
- `us-components.css` — **仅允许追加**新段落:`.us-formrow-*`、`.us-formmenu-*`、`.us-home-*`、`.us-rolesw-*`;禁止修改既有规则(视觉回归风险)。颜色/字体一律 var(--us-*)。
- `pages/expr-page.tsx` — 接入 FormRow(当前表达的 viewIds→FormTag;切换写 searchParams);`pages/source-page.tsx` — 接入 FormRow(表格 active + 右注「同一份数据,换任意形式描述」,对照 1a 屏)。

**C. 首页 8a(对照主版本页面集 8a 屏逐块还原)**

- `pages/home-page.tsx` 重写 — **无侧栏**(8a 为全宽布局):
  - 专属顶栏 52px(底 topbar 色):Logo+「同源」+「空间:智能硬件团队」(store)|右:搜索框 260px(占位「搜索表达、数据源、字段…」+ Mono ⌘K,仅样式)、「AI 导入」按钮(IconSpark 绿字 → `/us/import`)、绿实心「新建表达」(点击 Toast「P2 提供」)、RoleSwitcher 头像。
  - 主体 `grid 1fr/356px` gap16 padding20/24,底 canvas 色:
    - 问候行:「早上好,{当前身份名}」20/700 + 副文「今天已有 **N 项数据变更**(琥珀),M 处引用全部同步」(N=pending ChangeSet 数,M=fieldRefs 数,store 派生)。
    - 「表达 WHAT TO SAY · 6」段 → `HomeExprCard`×6(3 列):标题 13/700 + MonoTag 组(defaultForm 对应 tag 为 active 墨底金)+ 底部头像 + lastActivity 文案;点击 → `/us/expr/:id?form=default`。
    - 「统一数据源 DATA · 4」段 → `HomeSourceTile`×4(4 列):绿网格图标 + 库名 12.5/700 + Mono「N 记录 · 被 K 表达引用」(N=该类型对象数,K=fieldRefs/表达绑定派生)+ 状态灯行(产品规格库=琥珀「今日 改 2 · 全部已同步」、渠道销量表=绿「自动刷新 5min」、合同台账=绿「今日 增 1(AI 导入)」、客户信息库=灰+整卡 opacity .75「未被引用」);点击 → `/us/source/:code`。
    - 右列 356px:`TodoBanner`(琥珀横幅:「**1 项待确认**」+ 74% 文案 + 墨底「去确认」→ `/us/import`;无 pending AI 变更集时整块隐藏)+ `ActivityFeed` 卡(头「最近变更 ACTIVITY」+「全部」;5 条 store 活动流:22px 头像 + 富文本摘要 + 轨道标签(数据轨琥珀 chip/表达轨蓝灰 chip)+ 时间,虚线分隔;底注「数据源改动(琥珀)与表达改动(蓝灰)同一条流水,可追溯。」)。
- `pages/home/`(可选子目录)拆 `home-expr-card.tsx / home-source-tile.tsx / todo-banner.tsx / activity-feed.tsx`,均只吃 props/store 选择器。

**D. 文档**

- `docs/前端实施计划-同源主版本页面集.md` — H 节 P1 拆分表更新为 002–006 五卡序列(引用本卡序列位置表)。

## 行为要求(逐条可测)

1. FormRow:URL `?form=` 与 active FormTag 双向同步(直接改 URL 也生效);AddFormMenu 列出 内置6 + 已启用插件形式 + 管理插件入口;Esc/点外关闭。
2. 权限求值:`can('chenmo','prd_spec','editData')===false` 且 `can('chenmo','sales_channel','editData')===true`(资源 code 以 seed 实际为准);`requestWrite` 越权 → changeset-store 出现新 pending(source='manual', actor='chenmo'),workspace 零写入;有权 → 直接落库产生 ChangeEvent。
3. 关系写事件:createRelation/updateRelationField/unlink 各产生 1 条 ChangeEvent(track='data', inverse=null)+ 活动流条目;字段事件的 undo 行为不回归。
4. changeset-store 错误契约:三方法对未知 id 返回 `{ok:false}`,不 throw。
5. 首页派生数字全部可溯源 store:表达 6/数据源 4/待确认 N/引用 M/各库记录数;剧本终态下 待确认=2(ai+manual)、产品规格库显示 8 记录。
6. RoleSwitcher 切到陈默后:首页问候语变化;(为 003 预置)`session.current` 全局可读。
7. 跳转链路:表达卡→对应表达页(form 正确);数据源卡→表格骨架页;「去确认」→ `/us/import`。

## 测试要求(vitest 共置,纯逻辑优先,不渲染组件)

session-store:can() 矩阵(≥6 断言,覆盖 admin/edit/owner/readonly/none)、requestWrite 有权直写/越权转 pending(断言 workspace 零事件);workspace-store:关系三写各出事件+活动流;changeset-store:未知 id 三态返回 ok:false;首页派生选择器(若抽成纯函数 `deriveHomeVm(state)`)的数字断言;FormRow 的 form 参数往返(纯函数 `nextFormSearch(...)`)。

## 验收标准(机器可判 + 视觉)

1. `corepack pnpm verify:web` 全绿;`node scripts/check-us-tokens.mjs` 0 违规;
2. 视觉走查:浏览器 1440×900 对照 8a 屏逐块核对(顶栏构成/三列表达卡/四列数据源卡/右列横幅+活动流/客户信息库 75% 透明/一切数值日期等宽 font.data);
3. 手工链路:首页 →「去确认」→ import 骨架页;切陈默 → 问候语变;表达卡/数据源卡跳转正确;
4. `git diff --stat main` 限封闭清单;每步一 commit,PR 附自检输出。

## 禁止事项

禁止实现:表格编辑/分屏(003)、文档 RefChip(004)、AI 导入四步与对话抽屉(005)、校验/权限页 UI(006)、搜索框真实搜索、「新建表达」真实创建。禁止新增依赖;禁止改既有 primitives/壳的视觉与既有 CSS 规则(只许追加新段落);禁止 localStorage 存业务数据(AG-102);SelectionRef 相关约束不变(AG-209);禁止触碰 unisource 之外的代码与 `contracts/**`、`architecture/**`。完成后停止等待审查。
