# 任务卡 T-V33-103 — 阶段1 批3:ChangeState/Archive/SoftDelete/BatchCommand

- 状态:**已批准,可开工**(随 v0.4-relation-batch2 验收通过下发)
- 分支:`feat/T-V33-103-state-batch-commands`(从最新 main 切出,AG-401)
- PR 要求:`Spec-Ref: D.1-ChangeState/Archive/SoftDelete/BatchCommand, 契约§3.3/§3.6/§3.8/§3.9/§5.5, 4.6-状态机` + AG-405 自检输出段
- 依据:contracts/数据内核命令与事件契约.md(蓝本);复用批1/批2 命令骨架

## 目标

补齐内核写路径的剩余四条非 AI 命令,落地 4.6 状态机与 5.3 删除语义映射(Delete=SoftDelete、Archive=废止 VOID;Remove 仍非内核命令)。本批完成后,契约 10 条命令仅剩 AI 两条(阶段4)。

## 涉及文件(封闭清单)

kernel api(四个命令 DTO + StateChanged/Archived/SoftDeleted/BatchCommitted 事件)、kernel internal(四个 Handler + 仓储扩展)、**新增** `V3__kernel_batch3.sql`(仅在确需时:如 data_field_value 缺 status 列则补列;不建新表)、CommandController(四个 case,最小补丁)、对应测试、fixtures 新增 1 个 Archived 信封样例。**禁止新增依赖。**

## 行为要求(逐条可测)

1. **ChangeState = §3.3**:状态机 DRAFT→PENDING_CONFIRM→CONFIRMED→ISSUE→TO_FIX→FIXED→FILED,任意受控状态→VOID;fromState 必须等于当前态(与 expectedVersion 双保险)→ 否则 KERNEL-409-STATE-TRANSITION-INVALID(details 含当前态与允许迁移集);targetType 支持 object 与 relation;**fieldValue 状态本批不实现**——targetType=fieldValue 返回 KERNEL-400-SCHEMA-INVALID 并注明"本批次不支持",类注释 `// TODO 阶段3规则联动时实现字段值状态`;进入 CONFIRMED 的规则前置检查留挂点(本批仅必填级,TODO 阶段3);
2. **Archive = §3.6 全量 [R-2]**:任意受控状态→VOID;重复废止幂等成功(零事件);relationPolicy 默认 reject——存在活动关系返回 KERNEL-422-ACTIVE-RELATIONS(details 关系清单前 50 条+总数);relationPolicy=unlink 且活动关系 ≤50 → 同事务逐条解除并发 RelationUnlinked(同 correlationId/causationId,hierarchical 同步清闭包);>50 → KERNEL-413-CASCADE-TOO-LARGE;targetType=artifact 本批返回 SCHEMA-INVALID+"本批次不支持"(TODO 阶段7);`archive.referenced` 权限挂点:输出快照尚不存在,引用判定固定 false,`// TODO 阶段7 快照引用检查`;
3. **SoftDelete = §3.8**:仅 DRAFT 目标的创建者或 `admin.softdelete` 权限码;→ DELETED 终态;活动关系处理同 Archive 的 relationPolicy 机制;重复删幂等成功;audit_log 留痕沿用既有审计列(独立 audit_log 表属后续批次,不在本批);
4. **BatchCommand = §3.9 [R-3][R-8]**:子命令 ≤200、预估写行 ≤2000(估算规则:CreateObject=1+字段数,UpdateFields=字段数,关系/状态类=1;超限 KERNEL-413-BATCH-TOO-LARGE);禁止嵌套(KERNEL-400-NESTED-BATCH);`all_or_nothing` 单事务,任一失败整体回滚且**零事件零 command_log 子项**;`partial` 逐子命令独立事务,子幂等键 `<批次键>#<index>`,响应 results[] 逐项 {index,status,error?,events[]};整批结束发 BatchCommitted(成功/失败计数与索引);子命令复用既有 Handler,**禁止复制校验逻辑**;
5. UpdateFields/CreateRelation 对 VOID/FILED/DELETED 目标的拒绝(批1/批2 已实现)补回归断言,确认状态机终态与写入口径闭环。

## 测试要求(jacoco ≥0.80 门禁生效;AG-504 禁 sleep)

必含场景:非法状态迁移(CONFIRMED→DRAFT 拒绝);任意态→VOID;VOID 重复 Archive 幂等;**带 3 条活动关系的对象:reject 默认拒绝并列清单 → unlink 策略级联解除+事件计数=4(Archived+3×RelationUnlinked)同 correlationId**;51 条关系断言 CASCADE-TOO-LARGE 且零残留(契约 §9 #3);SoftDelete 非草稿被 PERM-403 拒;batch all_or_nothing 中途失败整体回滚零事件;**batch partial 第 2 项失败后第 3 项继续,重试整批仅补第 2 项**(契约 §9 #4);batch 嵌套与超限拒绝。

## 验收标准(机器可判)

1. `pnpm verify` 全绿(贴 jacoco 段);2. `pnpm contracts:check` 通过且新增 Archived 样例;3. 集成演示链:建对象→建关系→Archive(reject)→Archive(unlink)→batch partial 部分失败与重试;4. `git diff --stat main` 限封闭清单;5. PR 含 AG-405 自检输出;6. architecture:check 通过。

## 禁止事项

禁止实现:SubmitAIChangeSet/ConfirmAIChangeSet(阶段4)、Outbox 投递器(阶段4)、规则 DSL、audit_log 独立表、快照/输出、任何 backlog 条目。禁止触碰:contracts/**、AGENTS.md、ADR/**、docs/**、V1/V2 迁移文件(已合并,不可变)、packages/{views,web}/**。每步一 commit,完成后停止等待审查。
