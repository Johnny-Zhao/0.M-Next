# 任务卡 T-US-011 — 同源 P2 批4:矩阵 2b + 分析 9a(拖卡写字段/洞察钉回看板)

- 状态:**可下发**(纯前端、零新依赖、全 Mock)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(2b/9a 屏)、设计系统 §02(MatrixBoard+RecordCard/KpiCard·BarChartCard BI 卡族)、交接规格 §01(matrix 出口:卡片点击→源记录;ana 出口:生成子看板→新 BI/钉到看板→bi)/§02(MatrixBoard「行×列=两个枚举字段;拖卡跨列=写字段;列头计数;停产列卡片 75% 透明」)/§06(矩阵卡拖过列=写状态字段=目标列值,卡片落位+Toast 可撤销)、docs/tasks/T-US-010(审查结论)` + 自检输出段
- 序列位置:P2 五卡之 **批4**;交付后仅剩 012(插件 5b + P2 收官)

## 背景与关键决策(必读)

画布三卡(008/009/010)已合入。本卡做剩下两个描述形式:**矩阵**(2b,「矩阵即描述形式:拖动卡片跨列 = 修改状态字段,表格与所有文档引用同步更新」)与**分析**(9a,「分析建立在统一数据源之上,结论可溯源到记录级」)。两屏共同点:都不是新数据,是同一份数据的另一种「说法」。

**决策 1:产品库补「负责人」字段,矩阵两轴全 live。** `product_specs` 增 `owner`(dataType `person`,值=memberId,列名「负责人」);seed 按 2b 屏分派:王芸=S3/猫眼 E1/网关 G2,李晓=S3 Lite/门磁 M1,陈默=D2 Pro/D2,周然=指纹挂锁 P1。矩阵行=owner、列=lifecycle **由 ViewDef.config 声明**(`{ rowField:'owner', colField:'lifecycle', cardFields:['price','docRefs'], summary:'count' }`,存 `view-inventory-matrix`),VM 对任意「两枚举/person 字段」组合通用;列头计数、行头「N 项」、卡片售价全 live;卡片「关联文档数」= fieldRefs 中该对象去重 doc 表达数(live,S3 应为 3)。**注意:** grid 视图(1a)会自动多出「负责人」列(列定义源自 ObjectTypeDef),相关 VM/AVG/测试断言同步更新并在 commit message 注明。
**决策 2:拖卡跨列 = 走 grid 同一条写管线(权限语义一致)。** 卡片 HTML5 drag → 目标列 drop → `sessionStore.requestWrite`(同 1a 编辑):王芸/李晓 直接写(`updateField(lifecycle)`,data 轨事件 + Toast「已移至『在售』」带**撤销**动作=undo(eventId) + 引用 justSynced);陈默(product_specs readonly)→ 转审批 queued manual ChangeSet + Toast「已转审批」+ 8b PENDING+1(卡片**不落位**);周然全禁拖。**列头/单元格 dragover 高亮;停产列卡片 opacity 0.75(规格原文);`prefers-reduced-motion` 无落位动画。** 卡片点击(非拖拽)→ `/source/product_specs?focus=<objectId>`(复用 008 focus 机制,交接规格出口)。
**决策 3:分析 = seed 静态报告 + 两个真出口。** 9a 的数字叙事(因素拆解 -3.4%/-1.2%/+2.5%/合计 -2.1%、下钻 线上直营 -5.2% 31%/京东 -1.8% 24%/线下经销 +0.9% 12%、洞察主因/佐证)是分析结果的 Mock 快照,**作为 `AnaReport` 结构进 seed**(view-layer 定型:scopeLabel/question/sourcesLabel/factors(label/deltaText/widthPct/tone:'change'|'change-soft'|'primary'|'ink')/drill 行/insights 富文本段(纯文本+Mono 值分段));「重新分析」= 800ms 骨架 → 同一结果 + Toast(Mock 固定,fake timers 可测)。**两个出口做真:** seed 预置**隐藏** KPI 卡(`visible:false`):`客单价 Δ(净)-2.1%`(sourceLabel 分析)与 `主机口径 +3.1%`/`配件口径 -5.3%` 两张;「钉到看板」= `setKpiVisible(净值卡, true)`(**既有方法**,view 轨事件+活动流)→ 跳 `?form=bi` + Toast;「生成子看板:主机 / 配件口径」= 点亮两张口径卡 → 同跳转;已点亮再点 → Toast「已在看板」幂等;都可在 7a 版本面板按 view 轨恢复(隐藏回去)。
**决策 4:插色不新增。** 矩阵列头徽标复用 StatusPill 既有四态(us-components.css §「StatusPill — 数据枚举」);分析因素条 tone 全部映射既有 token(change/change-bg/primary/ink),**tokens 门禁 0 违规是硬线**。

**010 审查遗留顺手补(必做):** 仿真运行态下侧栏底注换为「仿真读取节点字段(协议、延迟、功耗)— 数据一改,回放结果随之变化。」;`SimScenario.metrics` 静态字段已无消费,删除(seed/类型/断言同步)。

## 目标

`/us/expr/exp-inventory?form=matrix` 按 2b 完整还原(配置行/四列徽标+计数/负责人行/记录卡拖拽写字段+权限分支/停产列透明/点击定位源记录);`/us/expr/exp-dashboard?form=ana` 按 9a 完整还原(口径 chip/问题行+重新分析/因素条/下钻表/洞察卡/生成子看板+钉到看板真出口/底注);两页 HOW 行与侧栏语境底注就位。

## 涉及文件(封闭清单)

**A. 模型与种子**

- `model/view-layer.ts` — `AnaReport` 定型(决策 3);`SimScenario.metrics` 删除(顺手补);
- `seed/demo-seed.ts` — product owner 字段+八对象赋值(决策 1);`view-inventory-matrix` 的 config(决策 1);`anaReport`(挂 `view-dashboard-ana` config 或顶层,注明);3 张隐藏 KPI 卡;
- `state/workspace-store.ts` — 若 `setKpiVisible` 签名需补(actor/summary)微调,零新方法优先;共置测试补断言。

**B. 矩阵(新目录 `matrix/`)**

- `matrix/matrix-view-model.ts`(纯函数,共置测试)— config+workspace → 列(枚举序=ObjectTypeDef enumValues;徽标态;计数)×行(person 值→成员头像/姓名/「N 项」)×单元格卡片(名称/售价 Mono/文档数 live);dim 列派生(`停产`);拖拽合法目标派生(同列=noop);
- `matrix/matrix-board.tsx` — 配置行(`数据:产品规格库 | 行:负责人 | 列:状态 | 卡片字段:售价 · 关联文档 | 汇总:记录数`,Mono 值)+ 网格主体 + 决策 2 全部交互 + 底部提示胶囊(2b 底注原文);
- `matrix/record-card.tsx` — 卡(名称 600/售价 Mono/文档角标);dragging/dragover 态;停产 opacity;
- `pages/expr-page.tsx` — form=matrix → MatrixBoard(替换骨架);exp-inventory chrome(「已同步 · 3 篇关联文档」灯按 live 文档引用数派生)。

**C. 分析(新目录 `ana/`)**

- `ana/ana-view-model.ts`(纯函数,共置测试)— AnaReport+workspace → 视图数据(出口按钮态:已点亮→「已在看板」);
- `ana/ana-view.tsx` — 顶行(口径 chip「近 30 天 · 全部渠道」+ 问题 H「为什么 7 月平均客单价下降 2.1%?」+ 来源 Mono「渠道销量表 + 产品规格库」+「重新分析」(骨架 800ms));「贡献度拆解 / 客单价 Δ · 按因素」横条组(条宽 widthPct/值 Mono/tone 映射);「下钻 · 渠道变动 Top」三列表(渠道|客单价 Δ|配件占比,值 Mono,负值 danger 正值 primary);「洞察 INSIGHTS」卡(主因/佐证,¥210/+3.1%/¥1,299→¥1,199 等 Mono 分段);操作行(「生成子看板:主机 / 配件口径」线框 +「钉到看板」绿实心,决策 3);底注(9a 原文);
- `pages/expr-page.tsx` — form=ana → AnaView(替换骨架);
- `shell/app-sidebar.tsx` — 语境底注:matrix 路由「矩阵把状态字段变成看板列,拖动即写回数据源。」/ana 路由「分析结论可固化为洞察卡,钉回看板或写入周报。」/sim 运行态(顺手补文案);
- `us-components.css` — 追加 `.us-matrix-*`、`.us-ana-*`;随组件同 commit。

## 行为要求(逐条可测)

1. 矩阵渲染与 seed 一致:4 列(研发中 1/预售 2/在售 4/停产 1)× 4 行(王芸 3/李晓 2/陈默 2/周然 1),S3 卡售价 ¥1,199(live)、文档数 3;停产列卡 0.75 透明。
2. 拖卡:王芸把 D2 从「在售」拖到「停产」→ lifecycle 写入、data 轨事件、Toast 带撤销(undo 后卡片回列)、列计数与 1a 表格同步、S3 规格书里引用该字段处 justSynced(若有);同列拖放 = 无写入。
3. 权限分支:切陈默拖任意卡 → 卡不动、queued ChangeSet、Toast 转审批、8b PENDING+1、王芸批准后卡片移列;切周然 → 卡片不可拖(cursor/禁用态)。
4. 点击卡片(非拖拽)→ 表格页对应行 focus 闪烁。
5. 分析页静态渲染与 9a 一致(因素条/下钻/洞察全 Mono 规范);「重新分析」骨架后结果复现,无业务写入。
6. 钉到看板 → KPI「客单价 Δ(净)」出现在 bi(跳转可见)、view 轨事件+活动流、版本面板可恢复(卡隐藏回去);再点 → 幂等 Toast;生成子看板 → 两张口径卡同机制。
7. 校验/分享零牵连:矩阵写 lifecycle 触发校验重跑但 11 规则口径不变(FLD-002 枚举合法性对拖拽值恒成立);resetDemo 全复原(含 KPI 隐藏态)。
8. 顺手补生效:sim 侧栏底注/metrics 字段删除。
9. 零回归:1a 表格(新增负责人列断言更新)、bi 看板、008–010 各卡行为不变;首页仍 6 卡。

## 测试要求(vitest 共置,纯逻辑优先,fake timers)

matrix-view-model(行列派生/计数/卡片字段 live/文档数派生/dim 列/enum 序);拖拽写入(updateField 分支+undo 回滚断言/queued 分支零主数据写入/同列 noop);matrix config 通用性(换一对字段仍可派生,防硬编码);ana-view-model(出口态派生/幂等);setKpiVisible 断言(view 轨+恢复往返);重新分析 fake timers(骨架→结果,无写入);seed(owner 赋值/隐藏 KPI/AnaReport 结构);既有测试零回归(负责人列与 metrics 删除的影响逐条注明)。

## 验收标准(机器可判 + 视觉 + 手工)

1. `corepack pnpm verify:web` 全绿(本机权威);tokens 门禁 0 违规;
2. 手工重点链:矩阵拖 D2 → 停产 → 撤销 → 切陈默拖卡转审批 → 8b 批准 → 卡移列 → 点卡跳表格 focus;分析页钉到看板 → bi 看新卡 → 版本面板恢复 → 生成子看板两卡 → resetDemo;
3. 视觉:1440×900 对照 2b(配置行/列徽标四态/行头像/卡片/停产透明/提示胶囊)与 9a(口径 chip/问题行/因素条四色/下钻表/洞察卡/双按钮/底注);
4. `git diff --stat main` 限 `packages/web/src/unisource/**`;每步一 commit;CSS 随组件同 commit。

## 禁止事项

禁止实现:矩阵行/列字段切换 UI(config 驱动即可,通用性靠测试保证)、卡片内编辑、矩阵新建记录、真实分析计算引擎(AnaReport=Mock 快照)、AI 指令条(ana 页面集无此元素)、子看板独立表达/路由(出口=点亮预置卡)、插件(012)。禁止新增 npm 依赖;禁止 import workbench/views;禁止发明新色(决策 4);禁止整文件重排 CSS;禁止改 P0/002 CSS 段;禁止 localStorage 业务数据(AG-102);选择联动零写入(AG-209);AI/越权写入必须走 ChangeSet(AG-106/204,矩阵审批分支同源);禁止触碰 unisource 之外的代码。完成后停止等待审查。
