# 任务卡 T-US-004 — 同源 P1 批3:文档视图 1b + 引用体系(RefChip 五态/@弹层/DataPanel)

- 状态:**可下发**(纯前端、零新依赖、全 Mock、不触后端)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(1b 屏)、设计系统 §02(引用芯片/Toast)、交接规格 §02(DocEditor+DataTableBlock+DataPanel/RefChip)/§05(RefChip 状态)/§06(RefInsertPopover/定位到显示处)、docs/tasks/T-US-003(审查结论)` + 自检输出段
- 序列位置:P1 五卡之 **批3**;005(AI 导入/对话)复用本卡的引用计数与文档;006 剧本端到端依赖「文档引用全联动」

## 背景与关键决策(必读)

T-US-003 已合入:DataGrid 编辑链路(权限/Toast/撤销)、justSynced 10s 回落、RefChip 两态、分屏 1c。本卡把文档做成真正的「表达」:正文里的每个值都是活引用,并补齐引用生命周期(插入/低置信/悬空/重绑)。

**决策:本卡不引入 Tiptap,正文用「结构化 blocks 只读渲染」。** 理由:① web 包零新依赖红线(Tiptap 在 views 包,web 未声明;跨包深 import 违反 AG-101);② P1 演示剧本不需要自由编辑正文文本,需要的是「引用是活的」;③ 真富文本编辑(schema/自定义 node/协同)是接真后端后的工作,届时在 views 包的 Tiptap 底座上做。工具栏按设计稿视觉还原,编辑类按钮为禁用占位,「@引用字段」「数据表格」为真交互。此决策记入实施计划 F 节修订。

T-US-003 审查遗留(本卡顺手修,允许修改 003 引入的 `.us-splitview/.us-refchip/.us-ltoggle/.us-grid` CSS 段;**P0/002 的段仍禁改,且严禁再整文件重排属性顺序**):
1. 分屏布局改回设计稿三列 `492px 14px 1fr`,「变更记录」移回左列下方(左列 grid rows: 工具条/表格/变更记录);中缝改墨色圆钮 + 金色双向箭头(纯装饰);
2. RefChip 改纯值展示(字段名移入 title/tooltip)+ 虚线下划线;justSynced 态加 ✓ 图标(IconCheck,色 change-icon);
3. LayoutToggle 改「分屏对照」文字 + 迷你拨杆开关(26×15 轨 + 12px 白点,ON=primary,OFF=toggle-off,对照设计系统「开关」规格),不再是两颗按钮;
4. DataGrid 补 40px 勾选框列(勾选=selection toggle,选中行实心绿勾)+ 编辑中行琥珀左沿(`tr[data-editing]` inset 3px change 色);
5. 分屏右侧文档替换为本卡新 DocView(自然修复缺元行/表格头问题)。

## 目标

文档页 `/us/expr/exp-spec-doc?form=doc` 按 1b 屏完整还原:三栏(表达侧栏 264 / 文档主区 / DataPanel 300);正文 blocks 只读渲染,内嵌 RefChip 全部为活引用;@引用字段完整交互(弹层/键盘/插入产生视图轨事件);RefChip 补齐五态;DataPanel(记录卡/字段引用列表/定位到显示处);分屏右侧复用同一 DocView。

## 涉及文件(封闭清单)

**A. 模型与种子**

- `model/view-layer.ts` — 增文档模型:`DocBlock = { kind:'meta'|'h1'|'h2'|'paragraph'|'dataTable'; … }`,paragraph 内容为 `DocInline[]`(`{kind:'text',text}` | `{kind:'ref',refId}`);`DocModel { docNo, template, binding:{objectId}, authorLine, blocks }`。`FieldRef` 不改形状(五态已定义)。
- `seed/demo-seed.ts` — ① 为 `exp-spec-doc` 增 DocModel(对照 1b 屏文案:元行 `SPEC-2026-018 / 模板:产品规格书 V2 / 绑定:产品规格库 › 智能门锁 S3`;H1「智能门锁 S3 产品规格书」;作者行「王芸 · 产品部 | 更新于今天 09:12」;三节 H2「一、定位与定价 / 二、关键参数 / 三、续航与可靠性」;正文引用 型号/售价/防护/上市日期/续航 等;第二节后内嵌 dataTable block:型号/建议零售价/电池续航/防护等级)。② fieldRefs 扩充:S3 规格书达到 **11 处引用**(售价保持恰 3 处全局引用,勿破坏既有 `syncedRefs=3` 断言;如需调整断言在同 commit 内说明);新增 1 条 **dangling** 引用:`exp-weekly` → S3 的已删字段 `presale_gift`(对象无此字段),与 9c 的 REF-002 剧本一致;③ 新增 1 条 **lowConfidence** 引用:S3 规格书 → `launch_date`(呼应 74% 剧本)。首页「M 处引用」数字随之变化,更新相关测试断言。
- `state/workspace-store.ts` — 新写操作 `addFieldRef(exprId, objectId, fieldCode, label)`:fieldRefs 追加(state='fresh')、产生 ChangeEvent(**track='view'**,target=field,inverse=null)+ 活动流(表达轨,「插入字段引用 …」)——这是视图轨事件的第一个真实来源;`rebindFieldRef(refId, newFieldCode)`:更新指向、state→'fresh'、view 轨事件 + 活动流。共置测试。

**B. 文档视图(目录 `doc/` 扩展)**

- `doc/doc-view-model.ts`(纯函数,共置测试)— DocModel + workspace 状态 → 渲染 VM:inline ref 解析为 {值文本(经 formatCellValue)、state、chipDomId(`ref-<refId>`)};统计 `refCount/allFresh/justSyncedCount/danglingCount`;HOW 右侧文案(全最新=绿「✓ N 处引用 · 全部最新」/有 justSynced=琥珀「刚刚同步 K 处引用」/有 dangling=砖红「1 处引用悬空」)。
- `doc/ref-chip.tsx` — 补三态:`inserting`(琥珀 1.5px 内描边 + @ 前缀,底 change-bg)、`lowConfidence`(琥珀底 + 尾随 Mono `74%` 置信标)、`dangling`(danger-bg-2 底 + 删除线 + 虚线下划线 danger;**可聚焦,点击/回车打开重绑弹层**);顺手修:纯值 + 虚线下划线 + justSynced ✓(见背景)。
- `doc/doc-view.tsx` — 1b 主区:富文本工具栏 44px(`正文 Body ▾`/B/I/U/S/两种列表 = 禁用占位;「数据表格」= 滚动定位到 dataTable block;「@引用字段」= 激活态按钮,触发插入流程;右侧:评论/历史占位按钮 + Mono 字数(由 blocks 文本长度计算));纸面 768px 居中(canvas 灰底,padding 40/72,font.doc 衬线,行高 2,H1 32/700 且绑定对象名下加绿虚线);元行/作者行/H2/段落/内嵌数据表格块(头「数据表格 · 来自 产品规格库」+ 同步态标签,行值全部 RefChip)按 DocModel 渲染;`compact` 模式(600px、缩小 padding)供分屏复用。
- `doc/ref-insert-popover.tsx` — @插入流程(交接规格 §06 RefInsertPopover):点击「@引用字段」→ 在第三节段落末出现 inserting 态 ghost chip(`@` + 已键入文本)+ 308px 弹层(白底 shadow.float):头部「@引用字段 · 产品规格库 › 智能门锁 S3」+ Mono 键入回显;选项 = 绑定对象全部字段按键入过滤(纯函数 `filterFieldOptions(fields, query)` 共置测试),首项预选(inset 2px 0 0 primary);底部 Mono 键位注「↑↓ 选择 ↵ 插入引用 esc 取消」;键盘 ↑↓ 移动、Enter 插入(→ `addFieldRef`,chip 转 fresh,Toast「已插入引用」)、Esc 取消(ghost 消失);点外关闭。
- `doc/rebind-popover.tsx` — dangling 重绑简版:列出绑定对象可选字段(同过滤组件复用),选择 → `rebindFieldRef` → chip 转 fresh + Toast;底注「悬空引用也可在校验中心批量处理(006)」。
- `doc/data-panel.tsx` — 右栏 300px(底 topbar):头「数据面板 DATA」;记录卡(`产品规格库 · 记录 #001 / 智能门锁 S3` + 按钮「定位到显示处 · N」「切换记录」(占位 Toast));「字段 · 在文档中的引用」列表(每行:字段名 / 值 Mono / 引用计数徽标或置信药丸(lowConfidence 显 74% 琥珀,dangling 显砖红「悬空」));「插入其他字段…」入口(触发 @流程);底部同步行(绿「✓ 同步状态:全部最新 · 刚刚」/ 琥珀变体)。「定位到显示处」点击 → 滚动到文档内首个对应 chip 并闪烁高亮 2 次(CSS keyframes,prefers-reduced-motion 降级)。
- `pages/expr-page.tsx` — form=doc 单栏 → DocView(768);split → SplitView 右侧换 DocView compact + DataPanel 不出现(分屏无右栏,按 1c);HOW 右侧文案接 doc-view-model 派生。
- `split/split-view.tsx` — 应用顺手修 1(布局回三列、变更记录回左列下方、中缝圆钮);右侧改用 DocView compact。
- `us-components.css` — 追加 `.us-doc-*`、`.us-refpop-*`、`.us-datapanel-*`,并按顺手修条款修改 003 的段落。

## 行为要求(逐条可测)

1. 文档正文全部值来自 store(改表格 → 文档 chip 值即变):分屏左表改续航 → 右文档 chip 值+琥珀✓,10s 回落;单栏文档同样活。
2. @插入:完整键盘流(↑↓/↵/esc);插入后 fieldRefs+1、产生 **view 轨** ChangeEvent、活动流出现表达轨条目、HOW 右侧计数 +1;Esc 取消零写入。
3. dangling:seed 中 `presale_gift` 引用渲染为悬空态;点击重绑到存在字段 → 转 fresh、view 轨事件、计数修正;HOW 右侧文案随 dangling 存在与否切换砖红/绿。
4. lowConfidence:launch_date 引用显示 74% 置信标;不影响值展示。
5. DataPanel:字段列表计数与文档实际 chip 数一致(纯函数断言);「定位到显示处」滚动 + 闪烁;「插入其他字段…」与工具栏 @ 同一流程。
6. 字数统计 = blocks 文本长度派生(Mono 显示),非写死。
7. 顺手修验证:分屏三列 492/14/1fr、变更记录在左列下方;RefChip 纯值+虚线下划线;LayoutToggle 为拨杆;DataGrid 勾选列 + 编辑行琥珀左沿。
8. 权限:周然(S3 规格书 readonly)身份下,「@引用字段」入口禁用(readonly 可读可引用?按 002 语义 readonly 允许「在表达中引用字段」——保持可用,但插入产生的是 view 轨写,需 `can(member, exprId, 'editView')` 判定:周然对 exp-spec-doc 为 readonly → 禁用并 tooltip「只读成员不可编辑表达」)。

## 测试要求(vitest 共置,纯逻辑优先,fake timers)

doc-view-model:ref 解析/计数/HOW 文案三态;`filterFieldOptions`(「防」→ 含 rating 的过滤结果、空串全量、无命中空数组);workspace-store:addFieldRef(track='view'、fieldRefs+1、活动流表达轨)、rebindFieldRef(指向更新、state fresh);字数派生;popover 键盘 reducer(若抽 `popoverReducer(state, key)`:↑↓ 环绕、↵ 产出选择、esc 复位);既有 003 测试全部不回归(seed refs 扩充后同步修正受影响断言,逐条在 commit message 说明)。

## 验收标准(机器可判 + 视觉)

1. `corepack pnpm verify:web` 全绿(本机为准);`node scripts/check-us-tokens.mjs` 0 违规;
2. 手工剧本:打开 S3 规格书 → 全绿「11 处引用」;分屏改续航 → 文档琥珀✓ → 10s 回落;@插入「防护认证」类字段 → 计数 12 + 活动流表达轨;点悬空引用(Q3 周报文档不在本卡,用 seed 注入到 S3 文档的 dangling 演示项或经 preview 页断言)→ 重绑 → 转绿;
3. 视觉走查:1440×900 对照 1b 屏(三栏结构/工具栏构成/纸面版式与衬线/元行与作者行/内嵌表格块/@弹层 308px 结构与键位注/DataPanel 结构);对照 1c 复核顺手修;
4. `git diff --stat main` 限封闭清单;每步一 commit;CSS 与组件同 commit。

## 禁止事项

禁止实现:Tiptap/contentEditable 自由文本编辑(见背景决策)、评论与历史真实功能、切换记录真实功能、AI 导入与对话(005)、校验中心 UI(006)、@弹层的拖拽插入(交接规格 DataTreeRow 拖拽属后续)、Q3 周报等其他表达的完整文档(只做 S3 规格书 + 分屏复用)。禁止新增依赖;禁止 import `@m-next/views`;禁止整文件重排 CSS;禁止改 P0/002 的 CSS 段与 primitives 视觉;禁止 localStorage 业务数据(AG-102);选择联动零写入(AG-209);禁止触碰 unisource 之外的代码。完成后停止等待审查。
