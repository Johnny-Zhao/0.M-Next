# 任务卡 T-US-012 — 同源 P2 批5(收官):插件中心 5b + 清尾六项 + P2 走查

- 状态:**可下发**(纯前端、零新依赖、全 Mock)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(5b 屏)、交接规格 §01(plug:入口 AddFormMenu「管理插件」/设置;出口:启用后回 AddFormMenu;data 插件注册表)/§02(PluginCard/DetailPanel)/§07(插件=静态注册表全 Mock)/§08-②(ShareDialog 缺稿自实现)/§09(DragGhost)、docs/tasks/T-US-007(C-1 保留项)/T-US-009/T-US-011(审查记录项)` + 自检输出段 + **《P2 走查》文档**
- 序列位置:P2 五卡之 **批5(收官)**;交付后 14 屏全亮,进入 P3(打磨/联调预备)前的自由点

## 背景与关键决策(必读)

008–011 已合入,14 屏只剩插件中心还是骨架;另有历卡审查记录的六个尾巴。本卡两件事:把 5b 做完 + 清尾,再交一份 P2 走查文档收口。

**决策 1:PluginDef 升级为注册表形状,旧两条 seed 替换为设计 6 条。** `PluginDef` 扩:`{ id, name, version, vendor, industry: '制造业'|'建筑工程'|'医疗健康'|'金融'|'法务合规'|'教育科研', tagline(卡片副文), formsProvided: { code, name, desc }[], contract: { reads: string[], writes: string[], writeNote?: string }, scope: 'all'|'group', scopeGroupLabel?, installed: boolean, enabled: boolean, updateTo?: string, beta?: boolean, usedByExprIds: string[] }`。seed 按 5b 屏 6 条:三维架构图 v2.3·同源官方·制造业(已启用,可更新 v2.4,含 2 形式:三维装配视图/爆炸图;contract 读 部件层级·树/尺寸·数值/材质·文本,写回 装配关系 — 与表格/文档同步;usedBy `exp-portal`)/工程甘特 Gantt+ v1.8·筑云科技·建筑工程(已启用,3 形式)/财务三表 FinSuite v3.1·数衡·金融(未装,3 形式)/病历结构化 MedForm v1.2·康码·医疗健康(未装,2 形式)/条款对照 LexGrid v2.0·律衡·法务合规(已启用,2 形式)/BIM 模型视图 v0.9·筑云科技·建筑工程(未装,Beta)。**旧 `plug-retail`/`plug-hardware` 删除,`AddFormMenu` 从 `plugin.forms` 迁移到 `formsProvided`**(枚举 enabled 插件的形式,label `插件名 · 形式名`,点击 = 占位 Toast「插件形式挂载留待正式版」;「管理插件」入口既有保留);相关断言迁移注明。左栏计数(全部/已启用/有更新/行业组)**全部 live 派生**(设计稿「24」为示意)。
**决策 2:插件状态机进 workspace-store,但不进业务双轨。** `setPluginState(id, patch: { installed?, enabled?, version?, updateTo?, scope? }, actor)`:安装=「安装」按钮一步 `installed+enabled` + Toast;停用 = enabled:false + Toast(详情底注「被 N 个表达使用…停用不删数据,形式标签将置灰」保留展示);「更新到 v2.4」= version 升级 + updateTo 清除 + Toast。**插件注册表不是业务数据:不产生 ChangeEvent、不进活动流**(与 AG-106 的 AI 写入路径无关,加注释说明),但走 store 广播(AddFormMenu/计数即时联动),resetDemo 复原;共置测试。
**决策 3:三栏 5b 布局(FullLayout)。** 顶栏(`设置 › 插件中心` + Mono PLUGINS + 搜索「搜索插件 / 描述形式…」(过滤名称/副文/形式名,本地态)+「开发者文档」线框占位 +「提交插件」墨底占位);左 216:状态项(全部插件 N(默认选中墨底)/已启用 N/有更新 N)+「按行业 · INDUSTRY」六行业(计数)+底注「插件 = 行业表达形式包。数据不动,换一套『怎么描述』。」;中栏卡列表(名称 600 + Mono `vX.Y · 厂商`、副文、徽标行:已启用(绿)/安装(按钮)/行业 tag/「含 N 种描述形式」Mono/「可更新 vX.Y」琥珀/Beta;选中卡 2px primary;底注「安装的描述形式出现在每个表达的『添加形式』菜单;同一数据源,不同行业各说各的话。」);右 330 详情(选中插件:标题 + Mono `vX.Y · 厂商 · 行业` + 琥珀「更新 vX.Y」链;「提供的描述形式 · FORMS」名称+描述列表;「数据契约 · CONTRACT」读取/写回条目(写回带注)Mono;「启用范围 · SCOPE」段控(全部空间可用/仅 {组名},写 scope);操作行(「停用」/「安装」/「更新到 vX.Y」按状态);底注 usedBy(live 解析表达名))。
**决策 4:清尾六项(全部必做,各自独立 commit)。**
1. **拖拽添加节点 + DragGhost 简版**(008 决策 4 遗留):7a「从数据源添加」弹层条目 draggable(拖起源条目「拖拽中…」态)→ 画布 drop → 添加节点(初始坐标=drop 位置换算,`screenToFlowPosition`)+ 既有 view 轨写入;点击添加路径不变;reduced-motion 无过渡。
2. **ShareDialog 简版**(缺稿 08-②,T-US-007 C-1 保留):校验通过后点分享 → 460px UsModal「分享 Share」:只读链接框(Mono 假链接 + 「复制」占位 Toast)+ 提示行「打开时字段值按查看者的数据源权限脱敏显示」+ 关闭;errors>0 时按钮禁用逻辑不变;标注「正式稿后调整」注释。
3. **脱敏「···」**(设计 DEV NOTES 脱敏承诺):permissions 补 `customers` 列(王芸 admin,李晓/陈默/周然 none);`/source/customers` 表格对 none 身份:全部值单元格显示 Mono「···」+ 禁编辑 + 工具栏提示条「字段值按你的数据源权限脱敏显示」;grid VM 层实现(`maskValues` 派生,共置测试);8a 数据源卡 0.75 透明既有不动。
4. **配置单 RefChip 视觉**(009 记录③):template-config-doc 值列改用 RefChip(fresh 态,点击无操作或跳源记录),合计行保持 Mono。
5. **矩阵两项**(011 记录):`canDragMatrixCard` 挪入 session-store(命名如 `canDragCards`,注释「演示身份策略:陈默=数据只读可转审批;周然/AI=纯只读禁拖,超出权限矩阵表达力」);矩阵底部胶囊文案对齐 2b 原文「矩阵即描述形式:拖动卡片跨列 = 修改『状态』字段,表格与所有文档引用同步更新。」(侧栏底注短句保留)。
6. **preview 页补 P2 组件区**:SlotCard 三态/PlayBar/MatrixRecordCard/AnaFactor 条/PluginCard 各一静态示例(展示用,不接 store)。

## 目标

`/us/settings/plugins` 按 5b 完整还原(三栏/搜索过滤/状态与行业过滤/安装-停用-更新闭环/详情四段/AddFormMenu live 联动);清尾六项落地;交付《P2 走查》文档;14 屏无骨架页。

## 涉及文件(封闭清单)

- `model/view-layer.ts` — PluginDef 扩(决策 1);
- `seed/demo-seed.ts` — 6 插件注册表(决策 1)/customers 权限列(清尾 3);
- `state/workspace-store.ts` — `setPluginState`(决策 2,共置测试);
- `state/session-store.ts` — `canDragCards` 迁移(清尾 5,原 matrix-actions 引用与测试同步);
- **新目录 `plugins/`** — `plugins-view-model.ts`(+测试:过滤/计数/详情派生/formsProvided 联动)、`plugins-view.tsx`、`plugin-card.tsx`、`plugin-detail.tsx`;
- `pages/plugins-page.tsx` — 替换骨架(FullLayout 装配);
- `shell/form-row.tsx` — AddFormMenu 迁移 formsProvided(决策 1);
- `canvas/add-node-popover.tsx` + `canvas/canvas-view.tsx` — 拖拽添加(清尾 1);
- `shell/workspace-header.tsx`(或既有 Share 按钮所在)+ 新 `shell/share-dialog.tsx` — 清尾 2;
- `grid/grid-view-model.ts` + `grid/data-grid.tsx` — 脱敏(清尾 3);
- `template/template-config-doc.tsx` — RefChip(清尾 4);
- `matrix/matrix-actions.ts` + `matrix/matrix-board.tsx` — 清尾 5;
- `pages/preview-page.tsx` — 清尾 6;
- `us-components.css` — 追加 `.us-plugins-*`、`.us-share-*`、脱敏与拖拽态小段;随组件同 commit;
- **`docs/验收-同源P2走查.md`**(交付物,见下)。

## 行为要求(逐条可测)

1. 插件页渲染 6 卡与 seed 一致;左栏计数 live(全部 6/已启用 3/有更新 1/行业计数);点状态或行业过滤中栏;搜索「甘特」「爆炸图」均命中对应卡(名称/形式名两路)。
2. 安装 FinSuite → 卡转已启用、左栏已启用 4、AddFormMenu 立即出现其 3 个形式项;停用三维架构图 → AddFormMenu 该组消失、详情底注仍显示 usedBy 文案;「更新到 v2.4」→ 版本变 v2.4、「有更新」计数归 0;全程 changeEvents 零增长(断言);resetDemo 复原。
3. SCOPE 段控切「仅 产品中心」→ scope 写入并保持(store 内存态)。
4. 拖拽添加:从弹层拖 M1 到画布空白处 → 节点出现在 drop 附近 + view 轨事件 + 版本面板可恢复;点击添加路径不回归。
5. ShareDialog:修复全部校验错误后点分享 → 弹窗(链接 Mono/复制 Toast/脱敏提示行);有错误时仍禁用。
6. 脱敏:切李晓/陈默/周然开 `/source/customers` → 值全「···」、单元格不可进入编辑、提示条出现;王芸看真值可编辑;VM 测试断言 mask 分支。
7. 配置单值列 = RefChip fresh 态,改主板价后仍 live(009 行为 7 不回归)。
8. 矩阵:胶囊文案 = 2b 原文;canDragCards 迁移后三分支行为与 011 测试等价。
9. 14 屏检查:所有路由无 PageSkeleton 残留(preview 除外,它是工具页)。
10. 零回归:P0–P2 既有测试全绿;首页 6 卡;AG-102(插件状态不进 localStorage)/AG-209 不回归。

## 交付物:《docs/验收-同源P2走查.md》

步骤化(≥14 步,每步:操作/预期/对应屏),覆盖:①7a 多选对齐+版本恢复 → ②删除 E1 闭环+resetDemo → ③8c 点击/拖拽实例化+TPL 换绑 → ④生成配置单(RefChip+合计 live)→ ⑤9b 回放四同步+弱网 → ⑥改协议重放时刻变 → ⑦2b 拖卡直写+撤销 → ⑧陈默拖卡转审批+8b 批准落位 → ⑨卡片点击 focus → ⑩9a 重新分析+钉到看板+版本恢复 → ⑪5b 安装/停用/更新+AddFormMenu 联动 → ⑫拖拽添加节点 → ⑬脱敏页+ShareDialog → ⑭全局 resetDemo 回初始。**Codex 按此自测并在 PR 勾选。**

## 测试要求(vitest 共置,纯逻辑优先)

plugins-view-model(过滤三路/计数/详情/AddFormMenu 派生);setPluginState(安装/停用/更新/scope + changeEvents 零增长断言 + reset 复原);grid maskValues(none→「···」+禁编辑/admin→真值);canDragCards 迁移等价断言;拖拽添加坐标换算纯函数(若抽出);seed 插件注册表结构断言;AddFormMenu 迁移后既有断言更新注明;全量零回归。

## 验收标准(机器可判 + 视觉 + 手工)

1. `corepack pnpm verify:web` 全绿(本机权威);tokens 门禁 0 违规;
2. 手工按《P2 走查》全 14 步走通;
3. 视觉:1440×900 对照 5b(三栏/卡徽标行/详情四段/双底注);ShareDialog/脱敏/拖拽态抽查;
4. `git diff --stat main` 限 `packages/web/src/unisource/**` + 走查文档;每步一 commit(清尾各自独立);CSS 随组件同 commit。

## 禁止事项

禁止实现:插件真实挂载形式到表达(AddFormMenu 点击=占位 Toast)、插件市场远端/评分/截图、开发者文档与提交插件真页面(占位)、复制到剪贴板真实现(占位 Toast 即可,避免权限差异)、脱敏的字段级细粒度(库级即可)、-2° 自绘 DragGhost。禁止插件状态产生 ChangeEvent/活动流(决策 2);禁止新增依赖;禁止 import workbench/views;禁止整文件重排 CSS;禁止改 P0/002 CSS 段;禁止 localStorage 业务数据(AG-102);选择联动零写入(AG-209);禁止触碰 unisource 之外的代码。完成后停止等待审查。
