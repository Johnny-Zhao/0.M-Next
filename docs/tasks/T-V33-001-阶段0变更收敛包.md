# 任务卡 T-V33-001 — 阶段0 变更收敛包

- 状态:**已批准,可开工**(总控计划确认表 Q-1~Q-5 全部确认,2026-06-12)
- 分支:`chore/T-V33-001-v33-convergence`(单分支,AG-401)
- PR 要求:`Spec-Ref: 7.15, F.4, F.5, D.2-错误码体系, 11.1-契约测试;ADR-009`(AG-402)
- 前置(用户已承诺先行执行):`git init` + 全量首提交 + tag `v0.2-baseline-v33`

## 背景依据

《V3.3回归审查报告》P-01(AGENTS.md 截断:现 129 行,止于 AG-505 中段,AG-506 与自检清单丢失)、P-02(AG-507/508 缺失)、P-03(AG-502 编号过期);契约 §8 错误码表(AG-311 引用的 error-codes.yaml 尚不存在);11.1"契约测试进 CI"。

## 目标

1. AGENTS.md 恢复完整并补齐 AG-507/AG-508;
2. 错误码登记表 error-codes.yaml 落地;
3. 契约 Schema 校验成为 CI 门禁。

## 涉及文件(封闭清单,git diff 不得超出)

修改:AGENTS.md、.github/workflows/ci.yml(lint job 追加一行)、package.json(追加 `contracts:check` script)、README.md(One-command verification 段追加一行说明)。
新增:packages/shared/contracts/error-codes.yaml、scripts/check-contracts.mjs、tests/contracts/fixtures/(≥10 个 JSON 样例)。

## 子任务 1:修复 AGENTS.md

1a. 补全 AG-505 句尾,恢复为:

> - AG-505:**禁止硬编码公网 URL**(AI 端点、镜像源、CDN、遥测);AI 端点只能读 `AI_GATEWAY_URL` 等环境变量;镜像引用一律 `${REGISTRY_PREFIX}/...`(9.6.2;ADR-008)。CI:代码与镜像外呼扫描 + compose/Helm lint。

1b. 在其后恢复 AG-506(丢失原文如下,逐字恢复):

> - AG-506:**禁止直连基础设施。** PostgreSQL/OpenSearch/MinIO/RabbitMQ 的驱动与 SDK 只允许出现在对应封装层(kernel/internal/persistence、SearchGateway、StorageClient、OutboxRelay/TaskDispatcher);其他模块一律经封装接口。CI:依赖扫描(驱动依赖声明位置白名单)。

1c. 追加 AG-507、AG-508(《V3.3回归审查报告》第五节原文):

> - AG-507:**禁止把外部标准文件(ReqIF/XMI/STEP/FMU 等)作为事实源直读直写**;导入必须解析为内部对象/字段/关系/制品映射,输出由适配器生成(依据 F.5/7.15.3)。CI:适配器代码扫描"绕过内核命令直接以标准文件为读写目标"模式。
> - AG-508:**C 级标准/工具(以 ADR-009 §C 级清单为准)禁止进入 MVP 依赖与主流程**;引入前必须新增专项 ADR 并通过 ADR-008 准入。A 级标准适配器必须声明支持子集并维护 golden files,适配器变更必须跑样本回归(F.4-1/3/4)。CI:deps-allowlist 与 C 级清单比对;适配器目录存在 golden/ 样本集检查。

1d. AG-502 文本中"由 ADR-001~008 派生"改为"由 ADR-001~009 派生"。

1e. 文末恢复"附:提交前自检清单"(丢失原文,第 4 条按术语表扩容微调):

> ## 附:提交前自检清单(代理每次提交前逐条核对)
>
> 1. 本次变更落在哪个 package?该 package"不得承担"什么(§1.1/§1.3)?
> 2. 是否在热路径/事件处理器中加入了重操作或全量查询(AG-201/202/209/211)?
> 3. 写操作是否走命令入口、审计字段是否必填、事件是否在注册集合内(AG-110/301/321/322)?
> 4. 新依赖是否在 allowlist、命名是否在附录A 对照表 20 项内(AG-502/§3.1)?
> 5. 分支命名、Spec-Ref、测试等待方式是否合规(AG-401/402/504)?
> 6. `pnpm verify` 是否全绿?

## 子任务 2:error-codes.yaml

按契约 §8 登记:KERNEL-\* 全部 19 项 + PERM-403-FIELD-DENIED + RULE-422(前缀类,registered as prefix)+ AI-409-CHANGESET-EXPIRED、AI-409-CHANGESET-ALREADY-DECIDED、AI-422-ITEM-PRECHECK-FAILED。每条字段:`code`、`httpStatus`、`title`(中文含义)、`responseRequirement`(契约 §8"返回要求"列)。**只登记,不发明新码、不改语义。**

## 子任务 3:check-contracts.mjs + CI

- 用 ajv(2020 方言;ajv + ajv-formats 为 devDependencies,MIT、纯 JS、无 native,**本任务卡已准入**)编译两份 schema;
- fixtures ≥10 例:正例 ≥5(每命令族至少 1 个 + 1 个合法事件),反例 ≥5,必含:缺 expectedObjectVersion 的 UpdateFields、嵌套 BatchCommand、无目标的 Unlink、未注册 eventType、缺 dataVersion 的 CheckCompleted;
- 正例全过、反例全拒,否则退出码非 0;
- ci.yml lint job 追加 `node scripts/check-contracts.mjs`;package.json 加 `"contracts:check"`。

## 数据结构 / API / Event / Schema / ErrorCode 变化

数据结构:无。API:无。Event:无。**Schema 文件禁止任何改动**。ErrorCode:仅登记现有,零新增语义。

## 测试要求

check-contracts.mjs 即测试本体;CI amd64 跑通;`pnpm verify` 全绿;禁止 sleep(AG-504)。

## 验收标准(全部机器可判)

1. `wc -l AGENTS.md` ≥160 且文件以自检清单结尾;
2. `grep -c "AG-50[1-8]" AGENTS.md` = 8;
3. AGENTS.md 含"ADR-001~009 派生";
4. `node scripts/check-contracts.mjs` 退出码 0;任删一个 fixture 必填字段后退出码非 0;
5. error-codes.yaml 与契约 §8 逐条对应(Claude 审查比对);
6. CI 绿;
7. `git diff --stat v0.2-baseline-v33` 仅含"涉及文件"清单内路径。

## 禁止事项

禁止触碰 contracts/schemas/_.json、ADR/_、docs/backlog-v33.md、packages/{kernel,engines,views,web,server}/src、docker-compose.yml、Dockerfile.\*;禁止引入 ajv/ajv-formats 以外任何新依赖;禁止实现任何 backlog 条目;禁止重构或格式化未涉及文件;禁止修改既有 AG-100~211/301~324/401~404/501~504 的语义。
