# T-V33-604 — 矩阵单元格连/断关系（仅前端，as-built）

状态:**已完成并合入 main**（merge `cab3db5`，tag `v1.11-stage6-matrix-cell-edit`）。

## 目标

在 603 只读矩阵视图基础上，让非终态单元格可**连接**（空格→建立关系）/**断开**（命中格→解除关系），全程经命令入口，纯前端，零后端改动。

## 封闭文件清单（实际合入 = 6 个前端文件）

- `packages/views/src/api/command-client.ts`（新增 `createRelation`→`CreateRelation`、`unlink`→`Unlink` 客户端方法，走通用 `/commands` 端点）
- `packages/views/src/api/clients.test.ts`（新增方法测试）
- `packages/views/src/matrix/matrix-view.tsx`（编辑态、保存/冲突/只读逻辑）
- `packages/views/src/matrix/matrix-view.test.tsx`（连/断/409冲突/终态只读/未注入只读 等 9 例）
- `packages/web/src/app.tsx`（一行：给 `MatrixView` 注入既有 `commandClient`）
- `packages/web/src/styles.css`（编辑态样式）

零 server / engines / 迁移 / 依赖。

## 交互与红线

- AG-110：写仅经 `CommandClient`；`createRelation`/`unlink` 对应**既有**命令类型，**不触发 AG-301**。
- 向后兼容：`commandClient?` 可选；未注入则 `canEditMatrixCell` 返回 false，退回 603 只读。
- AG-209：选择联动仍是 `selection.select` 内存态，零写。
- 终态对象（ARCHIVED/CONFIRMED/DELETED/…）单元格不可编辑。
- `KERNEL-409-*` → 复用 `conflict/conflict-dialog`，不自造冲突 UI。
- AG-202/203：矩阵查询分页有界（PAGE_SIZE 50）。

## 落地说明（重要）

604 的并行 Codex 会话曾**越界改动 L0 仿真代码**（`SimulationRunRepository`/`Runner`/新增 `FailingSimulationEngine` 等），触发 `architecture:check` 的 **AG-107（server must not assemble simulation code）**。处置:**弃用整条污染分支**，仅将上述 6 个前端文件外科式摘取到基于权威 main（含 L0/L1）的干净分支后合入。教训已沉淀为并行纪律：子任务必须严守封闭清单，禁止跨车道改动。

## 门禁

合并后 main 上 `pnpm verify` 全绿（AG-107 不再报警，证明仿真污染已甩除）。
