# 任务卡 T-US-007 — 同源 P1 视觉打磨与走查(不加功能)

- 状态:**可下发**(纯前端、零新依赖、零功能变更)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(8a/1a/1b/1c/6a/6b/9c/8b 八屏)、设计系统 §01-§03、交接规格 §03(Token)/§08-④(对比度)/§09(DEV NOTES)` + 自检输出段 + **《视觉走查记录》勾选表**
- 序列位置:P1 收官后的打磨轮;完成后进入 P2 画布批

## 背景(必读)

P1 六卡(001–006)已合入,剧本全链路可演。本卡目标是把「功能对」提升到「长得对」:对照设计稿逐屏核对并修复偏差。**本卡不加任何功能、不改任何 store 行为**;允许的代码改动仅限:us-tokens.css(对比度条款)、us-components.css(全文件可调,但 diff 必须只含视觉修复,禁止重排无关段落)、组件 JSX 的结构/类名微调、新增小图标;若视觉需要 VM 输出微调(如文案格式),同步更新对应测试并在 commit message 说明。

**CSS 修改权限(本卡特例):** 历卡「只追加」限制解除,允许修改任何 `.us-*` 段落以修复视觉;但每处修改必须对应下方清单编号,PR 里逐条勾选。

## A. 全局必修项

1. **对比度上调一档(交接规格 08-④,规格已授权)** — `us-tokens.css`:`--us-text-faint`(#98937F)与 `--us-text-disabled`(#B4AF9F)各加深一档(oklch 就近取色,注释登记新旧值与依据);全局肉眼复查小字可读性,不得顺带改其他 token。
2. **字段类型图标弃 emoji** — 1a 表头的 📅/👤/📄 换成 12px 线性 SVG 小图标(primitives/icons.tsx 新增 IconCalendar/IconPerson/IconDoc,currentColor,列头灰 Mono 风格与 Aa/# 一致);emoji 跨平台渲染不稳。
3. **FormTag 激活态补金色形式图标** — 设计稿 HOW 行 active tag = 墨底 + 金色小图标 + 文字;为六种内置形式补 12px 图标(grid=网格/doc=文档/canvas=相连节点/matrix=井格/bi=柱图/ana=放大镜勾),active 时金色(--us-gold),inactive 不显示图标。
4. **等宽字体全站复查** — 一切数值/¥金额/日期/型号/ID/百分比必须 `us-data`(font.data);重点抽查:6a 差异树旧新值、6b KPI 数值与柱图数值、9c 对照表、8b 审批卡旧新值、活动流时间。发现遗漏逐处修。
5. **状态色伴随文字/图标复查** — 增/改/删/错误/警告任何仅用颜色传达处补文字或图标(交接规格 DEV NOTES)。
6. **焦点可达性抽查** — 弹层(@插入/重绑/AddFormMenu/RoleSwitcher)内 Tab 顺序合理、Esc 一致;RefChip dangling 可聚焦已有,复查其余交互件 focus-visible 描边未被覆盖。

## B. 逐屏核对清单(对照 1440×900 设计稿,逐条勾选:符合 / 已修 / 偏差保留并说明)

**8a 首页**:顶栏 52px 构成与间距(Logo22/搜索260+⌘K/AI导入绿字/新建表达绿实心);问候 20/700 + 副文琥珀强调;表达卡 3 列(标题 13/700、MonoTag 组当前形式首位墨底金、底部头像+动作文案 11px muted、卡 hover 边+阴影);数据源卡 4 列(绿网格图标 26px、库名 12.5/700、Mono 计量 10.5、状态灯行三色、客户信息库整卡 0.75);右列 356(待办横幅琥珀底/边、墨底「去确认」;活动流 22px 头像、双轨 chip 琥珀/蓝灰、时间 Mono 右对齐、虚线分隔、底注)。
**1a 表格**:HOW 行右注文案;工具栏(记录集 chip 绿网格图标、筛选 chip 激活 primary-bg、搜索 180、墨底新建);表头 surface-head + 类型图标(A 项 2);行高 40、选中青绿左沿+实心勾、编辑行琥珀左沿+单元格 2px 内描边;末行「+ 新建记录」灰行(设计有,若缺补上,点击占位 Toast);统计条 34(行数/已选、AVG Mono、右绿灯「本表被 N 篇文档引用 · 上次同步 刚刚」);侧栏当前库徽标「打开中」绿底白字。
**1b 文档**:工具栏按钮图标化程度(B/I/U/S 至少按钮态统一、禁用 40%);纸面 768、padding 40/72、正文衬线 16/行高 2、H1 32/700 绑定名绿虚线下划线;元行/作者行(绿「N 处引用 · 全部最新」);内嵌表格块(头部来源标注+同步标签、grid 180px/1fr 行);@弹层 308(头部路径、选中项 inset 2px primary、底部键位 Mono);DataPanel 300(记录卡双按钮、字段行 值Mono+徽标、底部同步行);lowConfidence 74% 琥珀标、dangling 砖红删除线。
**1c 分屏**:492/14/1fr;左小表条头「产品规格库 · 网格 · 全部产品」;中缝墨圆钮+金双向箭头居中;右 DocView compact 600;变更记录在左列下方(最新琥珀点/其余灰点、旧值删除线→新值);编辑单元格「确认写入」名牌气泡(若 003 未做名牌,本卡补静态样式版:编辑态单元格上方琥珀小气泡「{当前身份} · 确认写入」);同步 Toast 392 墨底金图标+查看变更/撤销。
**6a AI 导入**:4 步进度器(完成=墨实心圆、当前=绿实心圆、待办=空心圈,连线);左卡三页签(文件页签置灰)、原文 Mono 高亮 span(绿/琥珀两 tone)、底部 AI 指令条墨 1.5px 描边+星标;右卡语义匹配三 chips(置信 % 绿/琥珀)、差异树(分组头 Mono 路径、行内 DiffBadge、旧值删除线、74% 行琥珀强调边+「请确认」、已写入行 ✓)、底部操作条(统计 + 逐项审核 + 绿实心确认写入,禁用带原因)。
**6b AI 对话**:三栏(中 BI + 右 360);对话头(星标绿+COPILOT Mono+上下文右注);气泡圆角 12/12/3/12 与 12/12/12/3;ActionCard(改=琥珀底+方角「改」章、增=青绿底「增」章、pending=琥珀边+待批文案、undone 置灰);操作行三链接;建议 chips 描边胶囊;输入条墨描边+纸飞机;typing 三点;KPI 卡(数值 Mono 23/600、delta 绿/红、AI 卡琥珀描边+外发光+右上 AiBadge);柱图(条圆角、三档色、值 Mono)。
**9c 校验**:顶栏(VALIDATE Mono 尾标、绿点自动增量文案、黑立即运行+播放三角);左 216 规则组(选中 surface-selected、组计数红/琥珀);中栏卡(错误 danger 边+红「!」圆、警告 change 边+⚠、选中 2px+glow、Mono 组·规则码、动作按钮排布);通过折叠条虚线边+绿勾+展开;三计数徽标(红/琥珀/绿底);右 330 详情(对照表权威绿/缓存红底、来源行小字、影响范围行、FIX 三按钮推荐绿、脚注)。
**8b 权限**:说明横幅盾图标+粗体首句;矩阵(列头图标、行头像24+姓名600+部门灰、徽标四级样式:管理墨底白/编辑·所有者 primary-bg 深绿/只读 canvas 灰/无 eol 浅灰;AI 行跨列斜体说明;陈默行琥珀底+左沿);右 320 详情(头像32、Mono 部门·email、能力清单绿勾/红叉+灰字、待审批卡琥珀边+Mono 旧→新+双按钮、脚注)。
**壳与全局**:侧栏 264(Logo 区/分段开关白浮片/搜索米底/脚注虚线上边);顶栏 48/52 两档;Inspector 316 页签;Toast 出现层级(不遮弹层);<1280 占位页;preview 页各组件示例与最新样式一致。

## C. 补充项(低优先,时间允许则做)

1. **ShareDialog 简版**(缺稿 08-②,自行按设计系统实现,标注「正式稿后调整」):点击已解锁的分享按钮 → 460px Modal:「分享 Share」标题 + 只读链接框(Mono 假链接 + 「复制」按钮占位 Toast)+ 成员权限提示行(「打开时字段值按查看者权限脱敏」)+ 关闭;有校验错误时按钮仍禁用(既有逻辑)。DeleteConfirmModal 不做(删除入口在 P2 画布)。
2. 活动流摘要富文本化:人名/字段名/值加 600 字重分段(若需 VM 输出结构调整,同步测试;成本高则保留纯文本并在走查记录注明)。

## 交付物

1. 代码修复(按 A 必修 + B 清单);
2. **`docs/走查记录-T-US-007视觉打磨.md`** — B 清单逐条三态勾选(符合/已修/偏差保留+理由),A 项附修改摘要(token 新旧值);
3. `corepack pnpm verify:web` 全绿;`node scripts/check-us-tokens.mjs` 0 违规;`git diff --stat main` 限 `packages/web/src/unisource/**` + 走查记录。

## 禁止事项

禁止任何功能/行为变更(store/路由/交互逻辑不动,除 C-1 ShareDialog 简版与 1c 名牌静态样式);禁止新增依赖;禁止改 P2 范围页面(canvas/matrix/tpl/sim/ana/plugins 的骨架页不动);禁止发明新色(对比度调整走 oklch 就近并登记);禁止整文件重排 CSS(修改必须逐段对应清单编号);禁止触碰 unisource 之外的代码。每步一 commit(建议按屏分 commit),完成后停止等待审查。
