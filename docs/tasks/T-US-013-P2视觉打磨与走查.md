# 任务卡 T-US-013 — 同源 P2 视觉打磨与走查(不加功能)

- 状态:**可下发**(纯前端、零新依赖、零功能变更(A-1/A-2 豁免见下))
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(7a/8c/9b/2b/9a/5b 六屏)、设计系统 §01-§03、交接规格 §03(Token)/§05(状态矩阵)/§09(DEV NOTES/动效表)、docs/tasks/T-US-012(审查记录①②)` + 自检输出段 + **《P2 视觉走查记录》勾选表**
- 序列位置:P2 收官后的打磨轮(对应 P1 后的 T-US-007);完成后进入 E.3 契约预研(另行安排,非 Codex 卡)

## 背景(必读)

P2 五卡(008–012)已合入,14 屏全亮、《P2 走查》14 步可走。本卡把 P2 六屏从「功能对」提到「长得对」:对照设计稿逐屏核对并修复偏差。**本卡不加功能、不改 store 行为**,唯二豁免:

- **A-1(012 记录①,功能微修豁免):** 插件详情操作按钮分支改为 `enabled ? 「停用」 : installed ? 「启用」 : 「安装」`(「启用」= `setPluginState({enabled:true})` + Toast);当前已停用插件按钮误显「停用」且无恢复入口。同步补一条 store/VM 断言。
- **A-2(012 记录②):** preview 页 P2 组件区补 SlotCard 三态静态示例(instantiated/activated/empty,不接 store)。

**CSS 修改权限(本卡特例):** 历卡「只追加」限制解除,允许修改任何 `.us-*` 段落以修复视觉;但每处修改必须对应下方清单编号,PR 逐条勾选;禁止无关重排。

## A. 全局必修项

1. 插件详情按钮分支(豁免条款,见上)。
2. preview 补 SlotCard 三态(豁免条款,见上)。
3. **React Flow 水印**:若画布右下有 RF attribution 残留,以 `proOptions={{ hideAttribution: true }}` 统一隐藏(canvas/template/sim 三处;MIT 许可允许,注明);设计稿无此元素。
4. **等宽字体复查(P2 面)**:缩放百分比、节点坐标/尺寸、协议标签、仿真时刻与倍速、指标值、矩阵计数与售价、因素条数值、下钻表数值、插件版本号/形式计数、配置单价格与合计 —— 一切数值/ID/时刻必须 `us-data`;逐处抽查,发现遗漏即修。
5. **状态色伴随文字/图标复查(P2 面)**:节点 sim 四相位、槽位四态、violated 原因、边流动态、矩阵列徽标、因素条正负、脱敏提示 —— 任何仅靠颜色传达处补文字/图标(DEV NOTES)。
6. **焦点可达性抽查(P2 交互件)**:右键菜单(含子菜单)、DeleteConfirmModal、添加节点弹层、PlayBar、网络段控、SCOPE 段控、ShareDialog —— Tab 顺序合理、Esc 一致、focus-visible 描边未被覆盖;拖拽件(库卡/矩阵卡/弹层条目)保持可聚焦。
7. **prefers-reduced-motion 全面复查**:FlowEdge/PulseRing/对齐归位 150ms/justSynced 回落/骨架闪烁/拖拽落位 —— 全部静态降级仍可用(交接规格 §09 动效表逐行勾选)。

## B. 逐屏核对清单(对照 1440×900 设计稿,逐条勾选:符合 / 已修 / 偏差保留并说明)

**7a 视图画布**:点阵底 22px 密度与色阶;节点卡三态(默认细边+卡阴影/选中 2px primary+glow+四角手柄/字段行状态药丸+Mono 售价);来源徽标与文档角标按 visibility;贝塞尔连线灰阶+中点协议标签胶囊;对齐工具条(墨底圆角/图标+9px 标签/金字「移除」/分隔);右键菜单(定位子菜单结构/移除次级/删除 danger 红字+动态计数副文);检查器三面板逐块(SHOWN 勾选列表/样式面板色板与滑块/版本流双色点+琥珀高亮行+恢复绿链);左下提示胶囊;右下运行仿真墨底金三角;右上缩放 Mono。
**8c 模板**:槽位卡三态边线规格(激活 2px 虚线 primary+外发光+底半透明 vs 空 1.5px 虚线灰);「拖入实例化」「已实例化」徽标;绑定对象字段行(芯片组/供电/价格 Mono);结构连线实绿(socket 标签)/虚灰(DDR5/PCIe);库面板(头部 Mono 计量/chips 墨激活/卡名称 600+规格 Mono+价格右对齐/不匹配置灰小注/已绑定「已在图中」/拖拽中态);顶栏模板标签+琥珀灯;工具栏按钮组;违例槽琥珀边+原因(B860)。
**9b 仿真**:顶栏副题「回放中 · 场景…」;HOW 右「仿真 · 运行中」;节点相位(事件源徽标恒显/运行中琥珀+PulseRing 尺寸对稿/完成✓+Mono 时刻);流动线虚线密度与速度(0.9s);PlayBar(底部中央墨底/播放暂停/Mono 进度/轨道/倍速 chip/循环/停止);参数面板四段(场景禁用下拉/网络段控/时间线当前行琥珀+末行回放结束/指标瓦片 2×2);提示胶囊文案。
**2b 矩阵**:配置行五段 Mono 值;列头 StatusPill 四态+Mono 计数;行头(头像 24+姓名 600+「N 项」Mono);记录卡(名称 600/售价 Mono/文档角标);停产列卡 0.75;dragover 列高亮;底部胶囊 2b 原文;卡片 hover 态。
**9a 分析**:口径 chip;问题标题字号字重;来源 Mono;重新分析按钮与骨架样式;因素条(四 tone 宽度对稿/值 Mono 右对齐);下钻表(表头灰 Mono/负值 danger 正值 primary);洞察卡(主因/佐证结构/Mono 分段);操作行双按钮(线框+绿实心/「已在看板」态);TRACE 底注。
**5b 插件**:三栏 216/1fr/330;顶栏(搜索占位文案/开发者文档线框/提交插件墨底);左栏状态项激活墨底+行业组+底注;插件卡徽标行顺序(已启用绿/安装按钮/行业 tag/含 N 种 Mono/可更新琥珀/Beta);选中卡 2px primary;详情四段间距与 kicker Mono;SCOPE 段控;操作行;USES 底注。
**跨屏**:侧栏语境底注三条(sim/matrix/ana)切换正确;ShareDialog 460 宽与内部间距;脱敏「···」Mono 与提示条样式;配置单 RefChip 行高与合计行;preview 页 P2 区与最新样式一致。

## C. 补充项(低优先,时间允许则做)

1. 矩阵卡跨列落位过渡 150ms(reduced-motion 降级;纯 CSS,不改写入逻辑)。
2. 版本面板 KPI 事件(inverseKpi)恢复链路的文案统一(「恢复」语义与 field/view 轨一致)。

## 交付物

1. 代码修复(A 必修 + B 清单);
2. **`docs/走查记录-T-US-013-P2视觉打磨.md`** — B 清单逐条三态勾选(符合/已修/偏差保留+理由),A 项附修改摘要;
3. `corepack pnpm verify:web` 全绿;`node scripts/check-us-tokens.mjs` 0 违规;`git diff --stat main` 限 `packages/web/src/unisource/**` + 走查记录。

## 禁止事项

禁止任何功能/行为变更(store/路由/交互逻辑不动,除 A-1/A-2 豁免);禁止新增依赖;禁止发明新色(一切取既有 token);禁止整文件重排 CSS(修改逐段对应清单编号);禁止改 P0/002 段落语义(视觉修复不在此限,但须对应编号);禁止触碰 unisource 之外的代码。每步一 commit(建议按屏分 commit),完成后停止等待审查。
