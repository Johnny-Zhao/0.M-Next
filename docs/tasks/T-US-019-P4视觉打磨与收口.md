# 任务卡 T-US-019 — P4 视觉打磨与收口(仿 007/013 phase 收官)

- 状态:**可下发**(P4 016–018 已合并;**纯 unisource 打磨 + 走查**,无新契约、无新内核调用)
- 性质:phase 收官打磨卡——集中修 016–018 审查记录的非阻断 nit + backend 各面视觉一致性 + 全量 P4 走查执行。**无新功能**
- PR 要求:`Spec-Ref: docs/tasks/T-US-016~018(各审查结论的 nit 段)、docs/验收-同源P4全量走查.md、docs/design/design-new/project/设计系统 Design System.dc.html + 交接规格 Handoff Spec.dc.html(视觉权威)` + 自检输出段
- 序列位置:P4(014–018)之后的收官打磨,承 T-US-007(P1)/T-US-013(P2)节奏

## 背景与关键决策(必读)

016–018 逐张审查通过,过程中记了一批**非阻断 nit**(都折"顺手修"),且 backend 模式新出了几处面(9c 内核权威面板、6a 内核 aiChanges 旁路、导出 UI)。本卡按 007/013 的收官惯例一次性收口:**只做视觉打磨、语义/一致性修正、补测与走查执行,不改任何行为契约、不接新内核、不加功能**。已明确 **不做** 017d / updateRelationField 接线(经核为空接线,YAGNI)。

## 涉及文件(封闭清单)

**视觉打磨**
- `packages/web/src/unisource/validation/validate-view.tsx` —— 9c「内核校验(权威)」面板:`data-tone` 与角标按 `kernelResults` 最高严重度派生(含 error→danger、仅 warn→warning、否则 ok/中性),**不再恒 `warning`+✓**;摘要外壳由无 `onClick` 的 `<button>` 改为非交互元素(div/section),去误导性可点样式。
- `packages/web/src/unisource/import/import-view.tsx` —— 6a「内核 AI 变更集(权威)」旁路:间距/标签/空态与脚本面板对齐,`kernelBusy` 态呈现一致。
- `packages/web/src/unisource/template/template-canvas.tsx` —— 导出区(导出 DOCX/PDF/CSV):按钮排布、`outputs.busy`「导出中」态、`availability` 禁用提示与既有工具条风格一致。
- `packages/web/src/unisource/us-components.css` —— 上述面板/按钮样式,**append-only、token 取色**;`reduced-motion` / 对比度 / 等宽字体在新面上补齐(仿 013 全查)。

**代码质量 nit**
- `packages/web/src/unisource/data/kernel-gateway.ts` —— `captureSnapshot`/`createOutput`/`getOutput` 包进 `runWrite`(`CommandFailure`→`WriteRejection`,与其余内核写面一致);**成功路径与既有返回不变**。
- `packages/web/src/unisource/boot.tsx` —— 首刷 `refreshKernelAiChanges` 的 actor 由硬编码 `"wangyun"` 改取 `sessionStore.getSnapshot().currentMemberId`。

**补测**
- `packages/web/src/unisource/state/changeset-store.test.ts` —— 补内核 `refresh/confirmKernelItems/rejectKernel` **失败路径** store 层单测(catch→Toast、`kernelBusy` 复位、不抛)。
- `packages/web/src/unisource/access/access-view.test.tsx`(或 `permission-matrix` 就近)—— 补空间角色徽标**渲染断言**(017c 只测了投影逻辑,未测渲染)。

**走查**
- `docs/走查记录-T-US-019-P4视觉打磨.md`(新)—— 执行 `docs/验收-同源P4全量走查.md`,逐屏记录 backend/Mock **双模式**结果 + `reduced-motion`/对比度扫查结论(仿 013 走查记录)。

## 行为要求(逐条可测)

1. **9c 面板着色:** 内核结果含 error → 面板 danger;仅 warn → warning;空/全 ok → 中性;摘要外壳非交互(无误导性 `<button>`)。
2. **runWrite 一致:** outputs 三方法遇内核 4xx → 抛 `WriteRejection`(与 `updateField` 等一致);`outputs-store` 的 catch 仍兜底 Toast(**行为零回归**)。
3. **actor:** 首刷内核 aiChanges 以**当前 session 成员**为 actor。
4. **补测通过:** changeset 失败路径 + 角色徽标渲染两测绿。
5. **演示/Mock 零回归:** 剧本、脱敏、本地渲染、既有视觉逐位不变;纯视觉/token 调整不改数据流与行为。
6. **reduced-motion:** 新面无强制动效违规。

## 测试要求

共置 vitest 补两处(changeset 失败、角色渲染);既有测试零回归。视觉以走查记录 + 手工截图为准。

## 验收标准

1. `corepack pnpm verify:web` 全绿(含 `tokens:check`)。
2. `git diff --stat main` 仅含封闭清单;CSS **append-only**;每步一 commit。
3. `docs/走查记录-T-US-019-P4视觉打磨.md` 落地,backend/Mock 双模式逐屏过。
4. 视觉:9c 面板按严重度着色、导出区与工具条一致。

## 禁止事项

- 只动 `packages/web/src/unisource/**` 与本卡 docs;禁碰 views/server/contracts。
- **禁改任何行为/契约:** `runWrite` 包装不改成功路径与既有错误呈现;actor 改动不碰幂等/信封;9c/6a/导出 只调视觉与语义、不改数据流。
- 禁接新内核调用;禁做 017d/updateRelationField 接线(YAGNI 已决);禁碰脚本变更集/剧本时间线/脱敏。
- 禁在 `us-tokens.css` 之外散写色值;禁新增 npm 依赖;禁 localStorage 业务数据;CSS 随组件同 commit 且只追加。

完成后停止,等待审查。
