# 任务卡 T-US-003 — 同源 P1 批2:表格视图 1a + 分屏联动 1c

- 状态:**可下发**(纯前端、零新依赖、全 Mock、不触后端)
- PR 要求:`Spec-Ref: docs/design/design-new/project/同源 主版本页面集.dc.html(1a/1c 屏)、设计系统 Design System.dc.html §02(数据表行/引用芯片/Toast)、交接规格 §02(DataGrid/RefChip/LayoutToggle)/§05(DataGrid 状态)/§06(单元格编辑提交/新建记录/LayoutToggle)、docs/tasks/T-US-002(审查结论)` + 自检输出段
- 序列位置:P1 五卡之 **批2**;批3(文档 1b)复用本卡的 RefChip;批5 的剧本端到端依赖本卡的编辑→同步链路

## 背景(接 T-US-002,必读)

T-US-002 已合入:权限矩阵 + SessionStore(can/requestWrite 越权转审批)、FormRow(URL form= 同步)、首页 8a、关系写事件、changeset 错误契约。**注意:002 的组件 CSS 段(.us-formrow/.us-formmenu/.us-rolesw/.us-home*)是审查方补写的 —— 本卡新组件的 CSS 必须随组件一起交付,漏 CSS 视为未完成。**

本卡是「统一数据源 · 改一处全联动」的第一次真实演示:在表格里改 S3 售价 → 同一 store 广播 → 分屏右侧文档里的引用芯片翻琥珀(justSynced,10s 回落)→ Toast 提供撤销。这条链路做扎实,004(文档)/005(AI)/006(剧本端到端)都是在它上面加形态。

002 审查遗留小瑕疵(顺手修):
1. 首页表达卡头像写死 `member="wang"` —— 设计稿 8a 各卡成员不同(规格书=王、门户方案/周报=陈、供货协议=李…);
2. 表达卡 MonoTag 组顺序未把当前形式排首位(设计稿激活 tag 在最前)。

## 目标

表格视图 1a 完整还原(工具栏/类型驱动列/选中与编辑态/统计条),单元格编辑经 SessionStore 权限走 workspace-store 写路径;分屏 1c 完整还原(`?form=doc&layout=split`:左小表 + 中缝同步钮 + 右简版文档 + 变更记录 + Toast 撤销);RefChip 组件落地 fresh/justSynced 两态与 10s 回落。**不做完整文档编辑器/@弹层/DataPanel(004)、AI 界面(005)、矩阵/画布(P2)、虚拟滚动(seed 仅 8 行,>50 行阈值逻辑后置)。**

## 涉及文件(封闭清单)

**A. 002 遗留修复**

- `model/view-layer.ts` — `Expression` 增 `activityMember: MemberId`(最近动态的执行人);`seed/demo-seed.ts` 按 8a 屏补值(看板=wangyun、规格书=wangyun、供货协议=lixiao、周报=chenmo、门户方案=chenmo、盘点=wangyun)。
- `pages/home-view-model.ts` — 表达卡 VM:头像取 activityMember;`forms` 排序把 defaultForm 排首位。同步更新 `home-page.tsx` 头像 member、`home-view-model.test.ts` 断言。

**B. 同步引擎补强(justSynced,交接规格 §02 RefChip/§06 单元格编辑提交)**

- `state/workspace-store.ts` — `updateField` 落库后:受影响 FieldRef 的 `state` 置 `justSynced`,启动 10s 定时器回落 `fresh`(定时器集中在 store 管理,`reset()` 清理全部;新增只读选择器 `getFieldRefsByExpr(exprId)`)。共置测试用 fake timers。

**C. 表格视图 1a(新目录 `grid/`)**

- `grid/grid-view-model.ts`(纯函数,共置测试)— 由 ObjectTypeDef.fields 派生列定义(列头带字段类型图标标记 text=Aa/number=#/date=📅/person=👤/docLink=📄,Mono 弱色);行 VM(状态药丸 tone 映射 presale/sale/dev/eol);统计条 VM:`N 条记录 · 已选 M 条`、数值列均值(seed 售价 AVG=¥547)、`本表被 K 篇文档引用`。
- `grid/data-grid.tsx` — 表头(底 surface-head)+ 行(设计系统「数据表行」规格:hover 淡米底;选中=青绿左沿 3px inset + 淡青底 + 实心勾选;编辑中=琥珀左沿 + 单元格 2px 琥珀内描边);行点击 → selection-store `set({object})`,勾选框多选 `toggle`;**双击单元格进入编辑**(input 继承 font.data 对数值列),Enter 提交 / Esc 取消;提交经 `sessionStore.requestWrite`:有权 → 落库 + Toast「已更新 · N 处引用已同步」+ 动作「撤销」(调 `workspaceStore.undo(eventId)`)/ 越权(陈默改 product_specs)→ Toast「已提交审批 · 等待管理员确认」;末行「+ 新建记录」占位行(点击 Toast「P2 提供」,不做真实创建)。
- `grid/grid-toolbar.tsx` — 「记录集:全部产品 ▾」(静态菜单占位)| 字段/「筛选:状态 ≠ 停产」(chip 可开关,开=过滤 eol 行,激活态 primary-bg)/排序/分组(后两个占位)| 右:搜索框 180px(按产品名称过滤,纯前端)+ 墨底「新建记录」。
- `grid/grid-status-bar.tsx` — 34px:左 = 行数/已选;中 = `售价 AVG ¥xxx`(Mono);右 = 引用数 + 绿灯「上次同步 刚刚」。
- `pages/source-page.tsx` — 骨架替换为 GridToolbar + DataGrid + GridStatusBar(数据 = 该 objectType 的对象);左栏 DATA 树当前库徽标「打开中」(app-sidebar 增加当前路由高亮徽标,仅此一处小改)。

**D. 分屏联动 1c(新目录 `split/` + `doc/ref-chip.tsx`)**

- `doc/ref-chip.tsx`(G 级,设计系统「字段引用芯片」)— 本卡实现两态:fresh(青绿底 primary-bg-3 + 虚线下划线)/ justSynced(琥珀底 change-bg-4 + ✓ 图标 + 外发光 glow-change);值一律 font.data;订阅 workspace-store 对应 FieldRef 状态。inserting/lowConfidence/dangling 三态留 004。
- `split/split-view.tsx` — `?form=doc&layout=split` 时表达页主区渲染:`grid 492px 14px 1fr`。左 = 复用 DataGrid 的紧凑配置(5 列:产品名称 150/状态 78/售价 96/电池续航 84/防护等级 1fr,含条头「产品规格库 · 网格 · 全部产品」);中缝 = 墨色圆钮 + 金色双向箭头(装饰);右 = 简版文档纸面(600px 白纸居中,canvas 灰底):元行 Mono(`SPEC-2026-018 / 绑定:产品规格库 › 智能门锁 S3`)+ H1(font.doc 衬线「智能门锁 S3 产品规格书」)+ 两段正文内嵌 RefChip(售价/上市日期,读 store 活值)+ 内嵌数据表格块(型号/建议零售价/电池续航/防护等级 四行,头部「数据表格 · 来自 产品规格库」+ 同步态标签:有 justSynced 引用时琥珀「1 项已更新」否则绿「✓ 自动同步」)。**完整 Tiptap 文档编辑器是 004 的活,本卡右侧为只读展示。**
- `split/change-log.tsx` — 左小表下方「变更记录 CHANGES」:读 ChangeEvents(field 轨、该库对象),渲染 `字段:旧值→新值 · 执行人 · 经AI导入?· 时间`(最新条琥珀点,其余灰点),显示最近 2–3 条。
- `shell/layout-toggle.tsx`(G 级)— 「分屏对照」迷你开关:仅当表达 forms 同时含 grid+doc 时出现在 FormRow 右槽;开 → URL 加 `layout=split`(FormRow 双 active:表格+文档),关 → 移除参数回单形式。纯函数 `nextLayoutSearch(search, on)` 共置测试。
- `pages/expr-page.tsx` — form=doc 且 layout=split → SplitView;form=doc 无 split → 004 前先渲染右侧同款简版文档(单栏 768px);FormRow 右槽挂 LayoutToggle;顶栏同步灯:存在 justSynced 引用时琥珀「刚刚同步 N 处引用」,否则绿「已同步 · 刚刚」。
- `us-components.css` — **随组件交付**,仅追加:`.us-grid-*`、`.us-splitview-*`、`.us-refchip-*`、`.us-changelog-*`、`.us-ltoggle-*`;颜色/字体全 var(--us-*)。

## 行为要求(逐条可测)

1. 列由 ObjectTypeDef 驱动(改 seed 字段定义 → 列变),非硬编码;状态列渲染 StatusPill,数值/日期/型号列 font.data。
2. 编辑链路(王芸身份):双击售价 → 改 1099 → Enter → 对象 version+1、ChangeEvent 产生、Toast 含「N 处引用已同步」与「撤销」;点撤销 → 值回 1199(version 继续 +1)。Esc 取消不产生任何写入。
3. 权限链路(陈默身份):同一操作 → 零写入,changeset-store 新增 pending(source='manual'),Toast 提示转审批。
4. justSynced:编辑提交后,该字段 FieldRef state=justSynced;fake timers 前进 10s → 回落 fresh。分屏右侧 RefChip/数据表格块的琥珀态与之联动。
5. 筛选 chip「状态 ≠ 停产」开启后行数 8→7,统计条同步;搜索「门锁」只剩 S3/S3 Lite 类匹配行。
6. LayoutToggle:仅 grid+doc 双形式表达(exp-spec-doc?视 seed 的 viewIds 而定,若无则给该表达补 grid ViewDef —— 允许的 seed 小改)显示;开关往返 URL `layout=split` 正确。
7. 分屏变更记录显示最近字段变更(含刚发生的编辑),执行人/旧新值正确。
8. 视觉:1a 的 12 列结构可简化为 seed 实际字段列,但行高 40/表头底色/选中与编辑态描边/统计条 34 必须按设计系统规格;1c 的 492/14/1fr 分栏、右纸面 600、Toast 392 必须按稿。

## 测试要求(vitest 共置,纯逻辑优先,fake timers,不渲染组件)

grid-view-model:列派生(类型图标映射)、AVG=547、筛选/搜索过滤、统计 VM;workspace-store:justSynced 置态与 10s 回落、reset 清理定时器;编辑提交纯函数(若抽 `commitCellEdit(session, ...)`):有权/越权两分支断言(事件数、pending 数、Toast payload 结构);layout-toggle:`nextLayoutSearch` 往返;change-log:ChangeEvent→VM 派生(最新在前、AI 标记)。

## 验收标准(机器可判 + 视觉)

1. `corepack pnpm verify:web` 全绿;`node scripts/check-us-tokens.mjs` 0 违规;
2. 手工剧本走查:王芸在 `/us/source/product_specs` 改售价 → Toast/统计条/活动流联动;打开 `/us/expr/exp-spec-doc?form=doc&layout=split` → 左表改电池续航 → 右侧 chip 翻琥珀 → 10s 回落;切陈默重复 → 转审批 + `/us/settings/access` 骨架页待办数变化(经 store);
3. 视觉走查:1440×900 并排对照 1a/1c 屏(列结构/选中编辑态/统计条/分栏尺寸/芯片两态/Toast);
4. `git diff --stat main` 限封闭清单(`packages/web/src/unisource/**`);每步一 commit;**新组件的 CSS 与组件同 commit 交付**。

## 禁止事项

禁止实现:Tiptap 文档编辑/@引用弹层/DataPanel/RefChip 后三态(004)、AI 导入与对话(005)、校验/权限页 UI(006)、矩阵/画布/模板(P2)、虚拟滚动与并发名牌(后置)、真实「新建记录/记录集/排序/分组」。禁止新增依赖;禁止修改既有 CSS 规则(仅追加段落);禁止改 primitives 视觉;禁止 localStorage 业务数据(AG-102);选择联动零写入(AG-209);禁止触碰 unisource 之外的代码。完成后停止等待审查。
