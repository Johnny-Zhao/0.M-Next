# 任务卡 T-V33-503 — 阶段5 批3:树视图 + 图谱视图(接入同一 SelectionRef)

- 状态:**草拟 — 待人工依赖准入后可下发**
- 分支:`feat/T-V33-503-tree-graph`(从最新 main 切出,AG-401)
- PR 要求:`Spec-Ref: docs/05-view-model.md §4/§5/§7, docs/prototype/阶段5-线框-v1.md, 契约§4-事件, AG-101/102/202/203/209, ADR-002(AntV)` + AG-405 自检输出段
- 依据:`docs/05-view-model.md`;501 的 `/views/relations`(图谱)与 `/views/tree`(树)端点;502 的 `SelectionCoordinator`/view-client
- 对应 MVP-0:第 7(树视图)+ 8(图谱视图)+ 9(多视图选中同步,补齐表格/树/图谱/详情四处联动)

## 人工前置(非 Codex)

**依赖准入**:图谱需 `@antv/g6`(ADR-002 已选 AntV;G6 为其图可视化库)。请确认加入 `packages/views/package.json` 的具体包与版本(`@antv/g6` 最新稳定;MIT/Apache,纯 JS,可经内网私服离线安装,合 ADR-008)。**树视图用纯 React 递归,无需依赖。** 确认前 Codex 不得加依赖(AG-502)。

## 目标

补齐"多视图同源"的另两类视图,使表格/树/图谱/详情**四处共享同一 SelectionRef**:树按层级关系分解、图谱按关系连边,选中任一处其余高亮/定位。视图只读 501 读模型、不碰主数据、选择零写。

## 涉及文件(封闭清单)

- `packages/views/src/api/view-client.ts`:补 `relations(workspaceId, relationType, direction, sourceId, depth)` 与 `tree(workspaceId, relationType, rootId)` 两个只读方法(若 502 未加),`depth` 前端钳制 `1..5`(AG-203)。
- `packages/views/src/tree/`:`tree-view.tsx`——读 `/views/tree`,**纯 React 递归**缩进树;按 rootId 展开、depth 受限;节点点击→`SelectionRef`(object 级);订阅 SelectionCoordinator 高亮当前选中节点。
- `packages/views/src/graph/`:`graph-view.tsx`——读 `/views/relations`,用 **G6** 渲染节点-边图;`relationType`/`direction`(out|in)/`depth(≤5)` 控件;节点点击→`SelectionRef`;订阅协调器高亮选中节点;返回集 >阈值时只渲染已返回的有界集(501 已 LIMIT)。`relations→{nodes,edges}` 映射抽为纯函数便于单测。
- `packages/web/src/app.tsx`:视图栏新增"树/图谱"入口(线框 W-1.3 占位转可用);三视图 + 详情面板共用**同一 SelectionCoordinator 实例**;切换视图不丢选择态、切空间清空。
- vitest 测试(G6 在 jsdom 难直接渲染:测 `relations→nodes/edges` 纯映射 + 选择联动 wiring,G6 实例 mock)。**禁止新增 @antv/g6 之外的依赖。**

## 行为要求(逐条可测)

1. **树(#7)**:`/views/tree?relationType=&rootId=`(relationType 须 hierarchical,501 已校验);缩进渲染、按 depth 截断;节点点击→`selection.select({entityType:'object', entityId})`(纯前端,零写)。
2. **图谱(#8)**:`/views/relations?relationType=&direction=&depth=`;`relations→nodes/edges` 映射正确(去重节点、边带 relationType);depth 前端钳 1..5(AG-203);节点点击→object 级选择。
3. **四处同步(#9,AG-209)**:表格行 / 树节点 / 图谱节点 / 详情面板四处共享同一 SelectionRef;选中任一处→其余三处高亮(图谱高亮节点、树高亮节点、表格描边行/单元格、详情刷新);**全程零写、零全工作空间查询、切空间清空、不落 storage**(仅 `ui.` 偏好如布局)。
4. **隔离(AG-101/102)**:仅经 view-client 调 `/views/*`;不 import kernel/engines/server;不把对象/关系落 localStorage/IndexedDB。
5. **有界渲染(AG-202/203)**:relations/tree 查询必带 type+direction/root+depth;大图只渲染返回的有界集,不前端全量拉取。

## 测试要求(vitest;注入 stub fetch;G6 mock;AG-504 禁 sleep)

必含:`/views/tree` 响应→树结构渲染(层级正确、depth 截断);`relations→nodes/edges` 纯映射(节点去重、边数正确);depth>5 前端被钳/拒;节点点击触发 `selection.select` 且**不产生写请求**(AG-209);跨视图选中同步(在协调器上 select 后,树/图谱/表格高亮态一致);切空间→选择清空;断言无 localStorage 写主数据。架构断言由 `architecture:check` 覆盖(views→shared,无 kernel import)。

## 验收标准(机器可判)

1. `pnpm verify` 全绿;2. `pnpm architecture:check` 通过;3. 演示:demo 种子下 → 表格/树(decomposes_to)/图谱(depends_on)三视图并存 → 在任一视图选中一个对象,其余视图与详情面板同步高亮/刷新 → 切换工作空间选择清空;4. `git diff --stat main` 限封闭清单(只动 packages/{views,web} + views/package.json 加 @antv/g6);5. PR 含 AG-405 自检输出 + 依赖准入说明(G6 版本/license/离线)。

## 禁止事项

禁止实现:文档视图(阶段6)、可配置视觉语法(BL-04)、跨用户选择广播、协同编辑、图布局算法深度定制/大图虚拟化优化(后续打磨)、失锚批注 UI、任何 backlog 条目。禁止新增 @antv/g6 以外依赖;禁止 import kernel/engines/server;禁止把主数据落 storage(AG-102);禁止选择联动发写或全工作空间查询(AG-209)。禁止触碰:后端 packages/{shared,kernel,engines,server}、contracts/**、迁移、AGENTS.md、ADR/**。每步一 commit,完成后停止等待审查。
