# 验收-同源 P2 走查

> 覆盖 T-US-008 ~ T-US-012。每步完成后在 PR 自检中勾选;若保留偏差,在备注写原因。

| # | 操作 | 预期 | 对应屏 |
|---|---|---|---|
| 1 | 打开 `/us/expr/exp-portal?form=canvas`,多选三个卡片并执行对齐,再从版本面板撤销。 | 卡片按命令对齐;撤销后回到前一版布局。 | 7a 视图画布 |
| 2 | 在画布删除 E1 卡片,确认影响提示,随后执行全局 resetDemo。 | E1 从画布、关系和引用影响提示中闭环消失;resetDemo 后恢复初始数据。 | 7a 视图画布 |
| 3 | 打开 `/us/expr/exp-build-z890?form=canvas`,点击实例化一个槽位,再拖拽库项到槽位。 | 槽位变为已实例化;TPL 检查与绑定状态同步。 | 8c 模板实例化 |
| 4 | 打开 Z890 配置单文档,修改主板价格。 | 配置单值列为 RefChip fresh 态;价格与合计 live 更新。 | 8c 配置单 |
| 5 | 打开 `/us/expr/exp-portal?form=canvas&run=1`,播放仿真并切弱网。 | 回放、节点高亮、时间轴和参数面板四处同步;弱网态有明显提示。 | 9b 仿真运行 |
| 6 | 修改 S3 与 G2 的协议字段后重放。 | 对应关系标签与回放时刻变化,旧结果不残留。 | 9b 仿真运行 |
| 7 | 打开 `/us/expr/exp-inventory?form=matrix`,拖动一张产品卡跨状态列并点 Toast 撤销。 | 字段直写成功,表格/文档引用同步;撤销后回到原列。 | 2b 矩阵 |
| 8 | 切换陈默身份拖动产品卡,再到权限页批准审批。 | 拖卡转审批;8b 待审批卡出现并可批准落位。 | 2b / 8b |
| 9 | 在矩阵点击产品卡。 | 跳转到 `/source/product_specs?focus=...`,对应行聚焦闪烁。 | 2b / 1a |
| 10 | 打开 `/us/expr/exp-dashboard?form=ana`,点重新分析并将洞察钉到看板,再撤销。 | 800ms 分析骨架出现;KPI 卡进入 BI 看板;撤销后隐藏。 | 9a 分析 |
| 11 | 打开 `/us/settings/plugins`,安装 FinSuite、停用三维架构图、更新到 v2.4。 | 左栏计数、卡片状态、详情底注和 AddFormMenu 立即联动;插件状态不增加 ChangeEvent。 | 5b 插件中心 |
| 12 | 在 7a「从数据源添加」弹层拖 M1 到画布空白处。 | 出现拖拽中态;drop 附近新增节点并写入 view 轨;点击添加路径仍可用。 | 7a 视图画布 |
| 13 | 切换李晓/陈默/周然打开 `/us/source/customers`,再修复校验错误后点分享。 | customers 值显示 Mono「···」且不可编辑;分享弹窗含只读链接、复制占位和脱敏提示。 | 1a / ShareDialog |
| 14 | 执行全局 resetDemo 并回到首页、插件页、矩阵页抽查。 | 数据、插件状态、权限、矩阵列和新增 view 轨都恢复初始态;14 屏无骨架页残留。 | 全局 |

## 自检记录

- [ ] `corepack pnpm verify:web`
- [ ] `node scripts/check-us-tokens.mjs`
- [ ] `node scripts/check-no-skipped.mjs`
- [ ] 手工按上表 14 步走通
