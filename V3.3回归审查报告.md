# V3.3 回归审查报告

- 模式:回归审查(只审查本轮修复,不提全量新任务)
- 审查对象:Codex 基于《V3.3 变更影响评估与修正计划》FIX-V33-01~03 的补丁式修复
- 审查方式:逐文件实物核对(仓库无 git 历史,无法看 diff,按"现状 vs 修复前基线"比对);重跑 `node scripts/check-architecture.mjs`(通过)与两份 JSON Schema 的 Draft2020-12 编译校验(通过);确认 packages/ 下代码与修复前逐字一致
- 前提说明:《原理.docx》仍未在仓库中,Codex 独立测试报告未提供——本报告以仓库实物为准

---

## 一、审查结论

**修改后进入用户验收。**

修复方向完全正确、无范围扩大、无后置项误实现、无代码触碰;但存在 1 个高严重度的**文件截断事故**(AGENTS.md 尾部丢失)和 1 个**FIX-V33-02 漏项**(AG-507/AG-508 未落),必须二次修复后才能验收。

## 二、逐项审查表

| 修正项 | 是否完成 | 是否符合要求 | 测试是否充分 | 文档是否同步 | 是否存在风险 | 是否阻断验收 |
|---|---|---|---|---|---|---|
| FIX-V33-01 ADR 补标准等级 + ADR-009 | ✅ 完成 | ✅ ADR-001~008 各增"标准对齐声明(V3.3)"节(抽查 001/004/007/008,内容准确,**既有"决策"节零改动**);ADR-009 含 F.2 全部 13 行矩阵裁决、C 级禁入清单、引入前置条件;README 索引 9 条并更新 V3.3 引用 | ✅ 文档项,无需新测试;架构检查通过 | ✅ | 低 | 否 |
| FIX-V33-02 AGENTS.md 增补 | ⚠ **部分完成** | AG-211 已落(含 CI 检查方式)✅;6 个术语已入对照表 ✅;头部/§0 已更新至 ADR-009 ✅。**但 AG-507、AG-508 缺失;§5"标准适配器子集+golden files"条款缺失;且文件被截断**(详见问题清单 P-01/P-02) | — | ⚠ | **高** | **是** |
| FIX-V33-03 契约补标准映射附录 | ✅ 完成 | ✅ 新增 §10:CloudEvents 映射表覆盖全部 5 个必备属性(含 specversion≠schemaVersion 的正确辨析)、AsyncAPI 通道约定、OpenAPI 3.1 生成约定、§10.4 明示"不修改信封/Schema/不代表已实现"——精确符合"借鉴≠实现"口径 | ✅ 两份 schema 文件逐项核对**零修改**,重新编译校验通过,命令枚举/$defs 完整 | ✅ | 低 | 否 |
| FIX-V33-04 backlog 登记(Claude 自办项) | ✅ 本轮已由 Claude 补办 | `docs/backlog-v33.md` 已建,9 条后置项含依据/等级/进入条件 | 不适用 | ✅ | 低 | 否 |

针对审查重点 1~13 的快速裁决:①只改了确认范围(除截断事故)✅ ②无无关重构(代码、compose、CI、依赖零变更)✅ ③遗漏项=AG-507/508(见 P-02)❌ ④无后置项误实现(无新包/新依赖/新模块,ADR-009 反而加固了禁入)✅ ⑤无"借鉴写成实现"(各处均明示等级与"不实现"边界)✅ ⑥测试:文档轮无需新增,既有校验全绿 ✅ ⑦文档/ADR 同步 ✅ ⑧未破坏已有功能——**除 AGENTS.md 自身被截断** ❌ ⑨命令事件闭环未触碰 ✅ ⑩AI ChangeSet 机制未触碰 ✅ ⑪关系一等对象原则在 ADR-004 声明中显式重申 ✅ ⑫视图/内核隔离(dependencies.json 未动,架构检查通过)✅ ⑬元模型演化/迁移/兼容性:按确认范围属后置(Q2 待答),已入 backlog BL-05,ADR-009 第 1/4 条覆盖兼容边界要求 ✅。

## 三、问题清单

| 编号 | 严重程度 | 文件位置 | 问题说明 | 修改建议 | 阻断验收 |
|---|---|---|---|---|---|
| P-01 | **高** | AGENTS.md(现 129 行,止于 AG-505 行中段 `${REGISTRY_PREFIX}/..`) | **文件尾部被截断**:AG-505 句子不完整,AG-506(禁止直连基础设施)与"附:提交前自检清单"整节丢失。属"破坏已有功能"(该文件是全部编码代理的强制约束源,缺 AG-506 意味着直连基础设施禁令失效) | 恢复 AG-505 完整文本、AG-506 全文、自检清单(6 条,其中第 4 条更新为"命名是否在附录A 对照表 20 项内") | **是** |
| P-02 | **高** | AGENTS.md §5 | FIX-V33-02 验收标准为"新增 3 条 AG 规则",实际只落 AG-211。缺:**AG-507**(外部标准文件不得作为事实源,导入必须解析为内部对象/字段/关系/映射——F.5/7.15.3)、**AG-508**(C 级标准/工具不得作为 MVP 依赖,引入须先过 ADR-009 流程——F.4-3);另缺 §5 的"A 级标准适配器必须声明支持子集并维护 golden files,CI 跑样本回归"(F.4-1/4) | 在 §5 追加 AG-507、AG-508(各带 CI 检查方式:代码扫描"标准文件直读为事实源"模式;allowlist 比对 C 级清单),并补 golden files 条款(可并入 AG-508 或单列) | **是** |
| P-03 | 低 | AGENTS.md AG-502 | 仍写"由 ADR-001~008 派生",应为 ADR-001~009(ADR-009 已批准) | 改一处数字 | 否 |
| P-04 | 低 | ADR-007 标准对齐声明 | "消息通道与协议直接兼容"中 AMQP 表述为 A 级合理,但 CloudEvents 写"A/B"与 ADR-009 矩阵"事件契约 A/B"一致——无错;建议补一句"信封对外转换见契约 §10.1"形成互引 | 可选,不强制 | 否 |
| P-05 | 低 | 仓库根目录 | 无 git 仓库/无 tag——评估计划第六节建议的 `v0.1-baseline-v32` tag 与 `chore/v33-alignment` 分支均无法核验,Codex 变更不可回溯 | 用户侧确认是否在外部托管;若仓库即工作副本,建议立即 `git init` 并按计划打基线 tag(属流程项,不阻断本轮) | 否 |

## 四、架构红线检查表

| 红线 | 结果 | 依据 |
|---|---|---|
| AG-100~110 包依赖/import 禁令 | **通过** | 代码零变更;`check-architecture.mjs` 实测通过 |
| AG-201 命令事务禁重操作 | 不适用(命令处理器未实现) | 契约 §1.2 未被修改 |
| AG-202/203 视图全量查询/关系查询约束 | 不适用(无视图实现变更) | — |
| AG-204 AI 事件因果链 | **通过**(契约层) | 契约 §3.10/信封 causationId 说明未动;envelope schema 原样 |
| AG-205~209 仿真/深解析/快照/选择联动 | 不适用 | 无相关实现 |
| AG-210 消费者幂等 | **通过**(契约层) | 契约 §7.4 未动 |
| AG-211 重型求解/转换/推理禁入热路径(本轮新增) | **通过** | 已正确落入 AGENTS.md §2,含 CI 检查方式;无任何重型实现入仓 |
| AG-301 命令/事件名注册集 | **通过** | schema 命令枚举与事件枚举与基线逐项一致 |
| AG-501 禁改 /docs/spec | **通过** | 说明书 docx 未被改动 |
| AG-502 依赖准入 | **通过** | pom/package.json/pnpm-lock 零变更,无新依赖 |
| AG-503 权限旁路 | 不适用 | 无代码 |
| AG-504 测试 sleep | **通过** | 测试文件未变,无 sleep |
| AG-505/506 公网 URL/直连基础设施 | **通过**(行为)/**警告**(文本) | 无新 URL;但 AG-506 条文本身被截断丢失(P-01) |
| 关系一等对象原则 | **通过** | ADR-004 标准声明显式重申"关系继续作为 PG 中的一等事实对象" |
| 命令事件闭环(D.1) | **通过** | 契约 §10.4 明示零结构变更;schema 实测未动 |
| AI ChangeSet 确认机制 | **通过** | ConfirmAIChangeSet 契约与 AG-106/204 未触碰 |

## 五、给 Codex 的二次修复指令(仅以下两项,禁止其他任何改动)

**FIX-V33-02b(对应 P-01+P-02,单 PR,只改 AGENTS.md 一个文件):**

1. 恢复被截断内容:补全 AG-505 句尾;原文恢复 AG-506(PostgreSQL/OpenSearch/MinIO/RabbitMQ 驱动仅允许出现在对应封装层);恢复"附:提交前自检清单"6 条。
2. 在 §5 追加:
   - AG-507:**禁止把外部标准文件(ReqIF/XMI/STEP/FMU 等)作为事实源直读直写**;导入必须解析为内部对象/字段/关系/制品映射,输出由适配器生成(依据 F.5/7.15.3)。CI:适配器代码扫描"绕过内核命令直接以标准文件为读写目标"模式。
   - AG-508:**C 级标准/工具(以 ADR-009 §C 级清单为准)禁止进入 MVP 依赖与主流程**;引入前必须新增专项 ADR 并通过 ADR-008 准入。A 级标准适配器必须声明支持子集并维护 golden files,适配器变更必须跑样本回归(F.4-1/3/4)。CI:deps-allowlist 与 C 级清单比对;适配器目录存在 golden/ 样本集检查。
   - AG-502 中"ADR-001~008"改为"ADR-001~009"。
3. 验收标准:文件 ≥160 行且以自检清单结尾;`grep -c "AG-50[1-8]"` = 8;AG-211 不被改动;`pnpm verify` 通过。
4. 禁止事项:禁止触碰 AGENTS.md 以外任何文件;禁止改写既有 AG 编号语义;禁止再次调整术语表。

(P-03 已并入上述第 2.3 点;P-04/P-05 不下发 Codex。)

## 六、给用户的验收建议

- **功能**:本轮无功能代码,验收焦点是"约束文档的正确性"。重点抽查 ADR-009 的 C 级清单是否与你的商务/许可口径一致(Gurobi/CPLEX 被列为禁入)。
- **接口**:核对契约 §10.1 CloudEvents 映射表——确认你认可"信封 v1 字段不变、对外转换在网关层"(对应已确认的 Q3 口径);§10.2 通道命名 `workspace.{id}.events` 是否符合运维命名规范。
- **文档**:二次修复后,通读 AGENTS.md 全文(尤其 §5 八条禁止事项与文末自检清单是否完整);抽查任意两条 ADR 的"标准对齐声明"未改动其"决策"节。
- **测试**:验收时执行一次 `pnpm verify` 与 `node scripts/check-architecture.mjs`;两份 schema 用仓库内任一 JSON Schema 工具重验(本轮我已验证通过,验收复跑即可)。
- **演示**:无可演示功能;建议以"评审会过文档"形式验收:评估计划 → 本报告 → 二次修复 diff → backlog-v33.md 逐条确认。
- **风险**:①AGENTS.md 截断暴露了"无 git 历史"的流程风险——强烈建议验收前完成 git 基线化(init + tag `v0.1-baseline-v32`),否则下一轮 Codex 变更仍不可回溯、不可回退;②backlog 中 BL-05/BL-06 仍挂在你未答复的 Q2(原理.docx 来源确认)上,在答复前它们保持"禁止实现"状态,不影响验收但影响下阶段规划。
