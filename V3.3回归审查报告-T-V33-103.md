# T-V33-103 回归审查报告

- 审查对象:分支 `feat/T-V33-103-state-batch-commands`(10 个提交)
- 谱系核验:T-V33-102 分支是本分支祖先(`merge-base --is-ancestor` 实测)——批2 验收版(含 P-102-1 闭包主键修复与树形注释)完整在链上,无内容复制

## 一、审查结论

**允许进入用户验收**(实现层零阻断问题;一个流程发现见 §3 P-103-1,随合并动作一并解决;运行期证据待用户本机 verify 输出)。

## 二、逐项核验

| 核验项 | 实测 | 判定 |
|---|---|---|
| 谱系与范围 | 基于已验收的批2 分支续建(main 落后是用户未合并所致,Codex 选择正确);diff 中的 package.json/关系文件均为批2 谱系携带,非本批越界 | ✅ |
| ChangeState 状态机 | allowed() = 线性链下一态 + VOID,与契约 §3.3 逐字一致;fromState 双保险;409 含当前态与允许迁移集;CONFIRMED 规则前置留 TODO 挂点 | ✅ |
| TODO 边界 | fieldValue 状态显式拒绝(专项测试 `fieldValueStateIsExplicitlyDeferred`);artifact/快照引用按卡留 TODO,零偷跑 | ✅ |
| Archive 级联 [R-2] | reject 默认列清单;unlink≤50 同事务级联(测试断言 3 关系→4 事件同 correlationId);**51 条场景回滚零残留**;重复废止幂等零事件;hierarchical 终态清闭包(自纠提交 d8d599a + 专项测试) | ✅ |
| SoftDelete | DRAFT 创建者/admin 权限语义(`nonDraftCreatorStillRequiresAdminPermission`);relationPolicy 复用 | ✅ |
| BatchCommand [R-3][R-8] | **完全委托既有 Handler**(switch 分发,零校验复制);嵌套/超限拒绝;all_or_nothing 单 TransactionTemplate,失败整体回滚(专项测试);partial 逐项独立事务;子幂等键在 Controller 派生 `key#index`;**partial 重试只补失败项**(契约 §9 #4 专项测试);批事件 causationId 经 ThreadLocal 注入(自纠 79f49dc)且重放时序号正确(自纠 2249cef) | ✅ |
| 终态写入闭环 | FILED/VOID/DELETED 目标的 UpdateFields/CreateRelation 拒绝有回归断言(7b2bdc9) | ✅ |
| fixtures | Archived 信封样例已入(7917e23) | ✅ |
| AG 红线 | 零 sleep;零新依赖;contracts/ADR/docs 零改动;AG-405 十个细粒度提交含三个自纠 | ✅ |

三个自纠提交(批事件因果、重试序号、终态清闭包)说明 Codex 的自检在真实工作——这是三批以来首次零返工候选。

## 三、问题清单

| 编号 | 严重 | 问题 | 处置 | 阻断 |
|---|---|---|---|---|
| P-103-1 | 流程 | **批2 至今未合并入 main,v0.4 tag 未打**(上轮验收后合并步骤遗漏);本批分支已包含批2 全部内容 | 见 §4 合并方案,一次合并两批落账 | 否 |
| O-103-a | 观察 | 运行期证据(本机 `pnpm verify` 含 jacoco 段)连续两轮未随"完成了"附上 | 本轮验收**以贴出 verify 输出为生效条件** | — |

## 四、用户验收与收尾步骤

1. 本机跑 `pnpm verify`(分支上),**把完整输出发我**——含 jacoco 段与全部测试汇总;
2. 全绿后一次性落账(103 含 102,合并一次即可,补打两个 tag 保留批次追溯):
   ```
   git checkout main
   git merge --no-ff feat/T-V33-103-state-batch-commands -m "merge: relation + state/batch commands (T-V33-102, T-V33-103)"
   git tag v0.4-relation-batch2 feat/T-V33-102-relation-commands
   git tag v0.5-state-batch3
   ```
3. 里程碑意义:内核 8/10 命令完成,写路径除 AI 两条外全部闭环。下一批进入**阶段2(场景模板 M2 层)或阶段4 前置(Outbox 投递器)**——下发批4 任务卡前我会就先后顺序给你一个一句话建议。
