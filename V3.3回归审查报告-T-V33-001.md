# T-V33-001 回归审查报告

- 审查对象:分支 `chore/T-V33-001-v33-convergence`(commit 540b2d0),对照 tag `v0.2-baseline-v33`
- 审查方式:任务卡 7 条验收标准逐条机器核验(命令与输出见 §2),git diff 实测

## 一、审查结论

**不允许进入用户验收。**

本轮提交使仓库在分支上处于**损坏状态**:4 个文件被写入截断(含 package.json,JSON 已非法,整个 pnpm 工具链不可用),任务核心目标(AGENTS.md 修复)未达成。同时发现了贯穿两轮事故的**共同根因模式**,见 §3 P-T01——这是比单次返工更重要的发现,二次修复指令已针对性设防。

值得肯定:git 基线流程首次完整运转(tag/分支/提交规范),正因如此,本轮所有损坏**均可精确定位且可一键回退**——上一轮截断事故无法做到这一点。

## 二、逐项验收核验(实测)

| #   | 验收标准                                    | 实测结果                                                                                                                                        | 判定    |
| --- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| 1   | `wc -l AGENTS.md` ≥160 且以自检清单结尾     | **133 行**,仍止于 AG-505 句中(`${REGISTRY_PRE` 处),无自检清单                                                                                   | ❌      |
| 2   | `grep -c "AG-50[1-8]"` = 8                  | **6**(AG-506/507/508 缺失)                                                                                                                      | ❌      |
| 3   | 含"ADR-001~009"                             | 已更新(AG-502)                                                                                                                                  | ✅      |
| 4   | `node scripts/check-contracts.mjs` 退出码 0 | **退出码 1**:`ERR_INVALID_PACKAGE_CONFIG`——package.json 在第 24 行 `"eslint-plugin-react` 处截断,JSON 非法                                      | ❌      |
| 5   | error-codes.yaml 与契约 §8 对应             | 文件已建,26 个 code 条目、21 个 KERNEL-\* 唯一码;逐条比对待文件链路修复后复核                                                                   | ⏸ 暂缓 |
| 6   | CI 绿 / `pnpm verify` 全绿                  | 不可能绿:package.json 非法;且 ci.yml 的 buildx 行被截断(`dest=/tmp`,丢失 `/m-next-web.tar` 与 `-f Dockerfile.web .`)                            | ❌      |
| 7   | diff 仅含涉及文件清单                       | **超界**:packages/\*/pom.xml 与全部 Java 源文件被整文件重写——`git diff -w` 验证为**纯行尾变更(CRLF↔LF)**,内容未动,但违反"禁止格式化未涉及文件" | ❌      |

正确完成的部分:fixtures 14 个(10 正例命令+1 正例事件+3 反例,覆盖任务卡要求的反例场景);ajv/ajv-formats 按准入加入;**contracts/schemas、ADR/、docs/backlog-v33.md 实测零改动**;分支与提交信息规范。

## 三、问题清单

| 编号      | 严重         | 位置                                | 问题                                                                                                                                                                                                                                                                                                                                               | 阻断           |
| --------- | ------------ | ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| **P-T01** | **高(根因)** | 横切                                | **写入截断模式**:四个文件在互不相关的字节位置被切断——AGENTS.md 止于 `${REGISTRY_PRE`、package.json 止于 `"eslint-plugin-react`、ci.yml 止于 `dest=/tmp`、README 止于 `Architec`。这与上一轮 AGENTS.md 截断(同样止于 `${REGISTRY_PREFIX}` 附近)同模式。结论:**Codex 的整文件重写在长文件上不可靠**(疑似输出长度被截断后直接落盘),上一轮事故并非偶发 | 是             |
| P-T02     | 高           | package.json                        | JSON 非法,devDependencies 列表中断,所有 pnpm 命令不可用                                                                                                                                                                                                                                                                                            | 是             |
| P-T03     | 高           | AGENTS.md                           | 任务核心目标未达成:AG-506/507/508 与自检清单仍缺失                                                                                                                                                                                                                                                                                                 | 是             |
| P-T04     | 中           | .github/workflows/ci.yml、README.md | 两文件各自尾部/段落被截断,buildx web 镜像构建步骤损坏                                                                                                                                                                                                                                                                                              | 是             |
| P-T05     | 中           | packages/\*_(pom.xml、_.java)       | 全树行尾重写(CRLF↔LF),越出封闭文件清单;根因是无 .gitattributes 且执行环境行尾策略不一致                                                                                                                                                                                                                                                           | 是(随回退消除) |

## 四、架构红线检查

contracts/schemas 零改动 ✅;ADR/backlog 零改动 ✅;无新依赖越界(仅 ajv/ajv-formats,已准入)✅;无 backlog 条目被实现 ✅;AG-401 分支规范 ✅。红线层面无违规——本轮问题全部是**工程可靠性**问题,不是边界问题。

## 五、给 Codex 的二次修复指令(T-V33-001-fix,同分支继续)

**第 0 步(强制,先回退再重做):**

```
git checkout v0.2-baseline-v33 -- packages/ .github/workflows/ci.yml README.md package.json AGENTS.md
```

保留:scripts/check-contracts.mjs、tests/contracts/fixtures/、packages/shared/contracts/error-codes.yaml(这三者是本轮合格产出,无需重做)。

**第 1 步(防截断写入纪律,本次及今后所有任务强制):**

1. **禁止对 >50 行的文件做整文件重写**;修改既有文件一律用最小补丁(定位锚点行,只替换/插入目标段落);
2. 新增长内容采用**分段追加**:每段 ≤40 行,每段写完立即执行核验命令(见下)再写下一段;
3. 每个文件写完必须立即核验并把输出贴入 PR:文本文件 `wc -l <file> && tail -3 <file>`;JSON `node -e "JSON.parse(require('fs').readFileSync('<file>'))"`;YAML 同理;
4. 在仓库根新增 `.gitattributes`(本指令追加批准该文件入封闭清单):`* text=auto eol=lf`,杜绝行尾重写复发。

**第 2 步(重做被回退的四项小改动,全部是小补丁):**

1. package.json:仅插入 `"contracts:check"` script 一行 + devDependencies 两行(ajv、ajv-formats);
2. ci.yml:lint job 仅追加 `- run: node scripts/check-contracts.mjs` 一行,**不得触碰 buildx 段**;
3. README:仅追加一行 contracts 检查说明,**不得改写既有段落**;
4. AGENTS.md:按任务卡子任务 1 的 1a~1e 执行,但**逐条分五次追加写入**(1a 一次、1b 一次、1c 两条各一次、1e 一次),每次后跑 `wc -l` 与 `grep -c "AG-50[1-8]"` 核验递增。

**第 3 步:重跑任务卡全部 7 条验收命令,逐条贴输出入 PR。禁止事项与封闭清单维持任务卡原文(新增 .gitattributes 除外)。**

## 六、给用户的建议

- 本轮不要合并;让 Codex 按 §5 执行 T-V33-001-fix。
- 这次 git 基线证明了自身价值:四处损坏全部被 `git diff` 精确捕获并可单文件回退。请保持"先打 tag 再开工"的节奏。
- P-T01 的写入纪律(小补丁、分段追加、写后即验)建议固化为 AGENTS.md 的新规则(AG-403 补充或新增 AG-405),待本任务验收后由我提出文档修订——本轮不扩范围。
