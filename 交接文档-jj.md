# M-Next 项目交接说明

## 一、项目定位

M-Next 不是单纯的 AI 项目，也不是电脑采购 ERP。

核心定位是：

> 以统一数据源维护结构化事实，再通过 Grid、Canvas、Matrix、Doc、BI、ANA 等不同表达方式使用同一批数据。

电脑采购 `pc_procurement` 是第一个领域插件，用来验证这套架构是否真的可复用。未来室内设计、技术方案、器材采购等领域都应通过 Profile 和插件注册机制接入，不能复制一套独立系统。

主链路仍然是：

统一数据源 → 结构化模型 → 关系 → 派生字段 → 规则校验 → 多种表达 → 结构化文档 → 快照 DOCX

AI 不是当前主线。未来 AI 只能生成 AIChangeSet，经用户确认后走现有内核命令写入。

## 二、当前仓库状态

- 当前分支：`main`
- `main` 与 `origin/main` 一致
- 工作区干净，无未提交文件
- 当前提交：`6d06bd9b 电脑配件`
- `docs/ai-memory/02-current-state.md` 停留在 7 月初，已经严重过期，不能作为当前事实来源
- 最近提交名称存在大量“大卡”“电脑配件”等模糊名称，后续应改善提交可追溯性

## 三、电脑采购领域模型

Profile 位于：

`packages/domains/pc-procurement/profile.manifest.json`

已定义 6 类对象：

- `procurement_requirement`：采购需求
- `hardware_product`：硬件配件
- `supplier`：供应商
- `supplier_quote`：供应商报价
- `build_plan`：装机方案
- `build_plan_item`：方案明细

已定义 6 类真实关系：

- 方案满足采购需求
- 方案包含明细
- 明细选择硬件配件
- 明细使用供应商报价
- 报价对应硬件配件
- 报价由供应商提供

已定义 15 个后端派生字段，包括：

- 需求总预算
- 明细选中单价、总价、功耗、库存、性能分
- 方案总价、总功耗、性能分
- 电源容量
- CPU/主板平台范围
- 内存平台范围
- 关联需求预算和最大总功耗

已定义 7 条后端规则：

- 预算
- 最大总功耗
- 电源安全余量
- CPU/主板兼容性
- 内存平台兼容性
- 报价库存
- 明细产品/报价完整性

## 四、Demo 数据来源

真实 Backend 模式下，电脑采购主数据由后端 `PcProcurementDevSeeder` 创建，不是前端假数据。

Seeder 已包含：

- 4 个采购方案：入门、标准、高级、反例
- 采购需求
- 多种 CPU、主板、内存、显卡、电源、机箱、显示器等配件
- 供应商和供应商报价
- 方案明细及完整关系
- 正常方案与预算/兼容性/功耗反例

Seeder 通过 KernelCommandService 创建主数据和关系，并支持幂等启动。

前端 `pc-procurement-preset.ts` 主要保存表达配置和插件动作，不是主数据事实源。Mock 模式仍保留测试用途。

## 五、已经实现的产品能力

1. 根路径工作空间入口

访问 `/` 会进入工作空间选择界面，可以搜索、选择和创建工作空间，不再自动进入电脑采购空间。

2. Data Catalog 数据源树

已实现只读目录树、Profile 默认目录和记录库 placement。电脑采购按采购中心、需求、方案、配件、供应资源组织。

目前目录主要是导航元数据，不是 DataObject，也不是关系模型树。

3. 数据源记录维护

Grid 数据源支持真实后端记录读取、新建和弹窗编辑。电脑采购插件补充了领域关系和生命周期动作。

4. 方案与明细维护

已支持：

- 新建采购方案并绑定采购需求
- 新增方案明细
- 修改明细数量
- 更换配件和供应商报价
- 从方案解除明细
- 维护报价与配件、供应商关系
- 维护方案与采购需求关系
- 复制方案及其明细关系
- 归档记录

所有主数据写入均走 WorkspaceStore → KernelWriteBridge → KernelGateway → 内核命令。

5. 多表达

已实现电脑采购的：

- Grid：方案清单和派生字段
- Canvas：方案、明细、配件、报价等真实关系图
- Matrix：多方案比较
- BI：后端真实指标与校验统计
- ANA：预算、兼容性、功耗、性能比较和风险下钻
- Doc：结构化采购方案说明书

这些表达读取同一套工作空间对象、字段和关系，不在各页面复制主数据。

6. 结构化文档

已实现：

- 模型驱动的大纲和章节
- 方案、需求、明细和派生字段引用
- Tiptap 正文编辑
- 字段编辑走真实后端命令
- Grid/Canvas/Doc 的 SelectionRef 选择联动
- 从不可变快照生成 DOCX

文档不是完整 Word 克隆。章节结构仍由模型驱动，Tiptap 只负责正文字段。

7. 表达创建

“新建表达”已接入交互和后端持久化，可以创建表达及首个 View，不再只是无响应按钮。

8. 校验

已接入后端 CheckResult：

- BLOCK/WARN/PASS 汇总
- 当前方案和全工作空间范围
- 数据变更后标记结果过期
- 手动重新校验
- BI/ANA 使用同一批真实校验结果
- 校验过期时阻止误导性的文档输出

9. 写入一致性修复

已引入 `committed-pending` 状态：

- 后端命令已经提交但读模型尚未追平时，不再回滚真实写入
- 多步骤流程会停止后续命令
- 不使用临时 ID 继续创建关系
- pending 后按钮锁定，避免重复创建、绑定、归档
- 提示用户重新加载工作空间确认

## 六、Data Catalog 当前边界

已完成只读查询和树形导航。

尚未完成：

- 新建用户目录
- 目录重命名
- 目录移动
- 删除目录
- 移动 Record Library
- 用户 placement override 的完整写入链路

T-CAT03 的 MVP 决策已经由维护者确认，但 Catalog 专用命令实现目前不应视为已完成。代码中没有批准的五组 Catalog 命令实现。

电脑采购闭环完成前，建议继续暂停 Catalog 写功能。

## 七、当前仍缺少的内容

P0：

- 需要做一次真实运行环境的完整人工验收，而不是只看单元测试
- 需要验证“校验问题 → 定位对象/字段 → 修改数据 → 重新校验 → 所有表达更新”的完整修复闭环
- `committed-pending` 已有逻辑测试，但真实组件连续点击的回归测试仍偏少
- `docs/ai-memory/02-current-state.md` 必须更新，否则新会话容易重新偏向过期的 AI 主线

P1：

- 表达目前主要支持创建，重命名、删除、排序和完整配置管理仍不完善
- 文档不支持通用块增删、拖拽排序、完整富文本和复杂表格编辑
- DOCX 需要继续验证格式保真、数据引用、校验结论和多方案输出
- Data Catalog 仍是 Profile 默认布局，用户目录写入和 placement override 未完成
- Ant Design 只通过受控 adapter 用于少数组件，没有全面迁移；不应为了统一样式直接重写整个 UI
- 移动端/窄屏不在当前范围

P2：

- 第二领域插件尚未正式落地验证
- AI 草稿抽取和 AIChangeSet 不是当前闭环的优先任务
- 仿真、复杂协同、批量导入和插件市场仍属于后续能力

## 八、建议后续执行顺序

1. T-PC42：校验问题定位与修复闭环

点击校验结果后准确定位对象和字段；可跳转 Grid/Canvas/Doc；完成编辑后结果标记过期；重新校验后结果和 BI/ANA 更新。

2. T-PC43：电脑采购全链路 E2E 验收

真实后端完成：
创建需求 → 创建配件/供应商/报价 → 创建方案 → 添加明细 → 查看派生值 → 触发规则 → 修复问题 → 多表达同步 → 编辑文档 → 生成 DOCX。

本卡应以发现和修复真实阻断 Bug 为主，不增加新概念。

3. T-DOC 后续：文档交付质量

完善数据引用块、表格块、章节操作和 DOCX 格式保真，但继续坚持“模型控制结构、正文编辑器控制正文”。

4. T-CAT03：Catalog 用户目录写入

电脑采购闭环稳定后，再实现已批准的 Catalog 专用命令和 placement override。

5. 第二领域插件

选一个较小领域验证 Profile、Catalog、表达和文档注册机制是否真正通用，禁止复制电脑采购页面。

6. AI

最后接入“自然语言 → AIChangeSet → 人工确认 → 内核命令”，不能绕过确认直接改主数据。

## 九、接手人的第一步

先不要继续扩功能。启动项目并人工走一次电脑采购标准方案：

工作空间入口 → 数据源 → 方案 → 明细 → 派生值 → 校验 → Canvas/Matrix/BI/ANA → 文档 → DOCX。

记录每一个无法完成、显示错误或数据不同步的位置，再决定 T-PC42 是否直接实现，避免继续堆叠未经真实验收的大卡。

开始开发前运行：

- `git status`
- `corepack pnpm dev:up`
- `corepack pnpm verify:web`
- `node scripts/check-no-skipped.mjs`

当前没有必要重新启动 AI 方向，也不要优先做第二插件。
