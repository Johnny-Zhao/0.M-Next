# V3.3 接续工作计划 — MVP-0 对齐（上下文恢复与续接）

- 模式:上下文恢复 + 续接判断(只做定义、判断、计划,不写代码)
- 日期:2026-06-13
- 基线:《统一数据驱动办公平台技术说明书_修订版V3.3》;延续 `V3.3后续工作总控与阶段计划.md`
- 输入:仓库实物(git 历史 28 提交 / 4 tag)、AGENTS.md、ADR-001~009、contracts v1.1、docs/、prototype 线框、回归审查报告 ×5
- 结论先行:**不重定基线、不推翻、不返工。** 已有内核成果可全部继承;需修正的是"计划编排对 MVP-0 的覆盖"与"docs 上下文包的落点方式";唯一需明确废弃的是"另起 01–10 编号文档作为新事实源"这一动作。

---

## 0. 继承 / 修正 / 废弃 总账(本次最重要的判断)

| 类别 | 内容 | 处置 |
|---|---|---|
| **继承(原样)** | 仓库脚手架与 CI(`pnpm verify`)、ADR-001~009、AGENTS.md(已补全 AG-100~508+自检清单)、contracts v1.1 + 两份 Schema、error-codes.yaml、kernel 批1–3 实现(10 命令 + 信封 + 闭包表 + 字段级乐观版本 + 审计 + Outbox 写入)、docs/backlog-v33、阶段5 线框、9 阶段计划框架、变更影响分级 | 全部保留,作为续接地基 |
| **修正(增量,不返工)** | ①9 阶段计划需显式补 **评审阶段** 与 **树视图/图谱视图/JSON 轻交换/基础 diff** 的任务卡,以覆盖用户 MVP-0 的 15 项;②用户拟建的 `docs/01–10` 编号包 → 重映射到现有单一事实源文件,只补真实缺口文档;③"MVP-0"定义对齐为**一条贯通的薄垂直切片** | 通过新增任务卡 + 文档落点重映射实现,零既有代码改动 |
| **废弃(明确不做)** | 不另建 `01_PRODUCT_CONTEXT.md … 10_ACCEPTANCE_CRITERIA.md` 作为**并行新事实源**(会与 AGENTS.md/ADR/contracts 形成双源,违背单一事实源纪律与 AG-501 精神);V3.2 基线已废止(早前确认) | 用映射表替代;原理.docx 出处问题(旧 Q-6)已在 backlog 关闭 |

---

## 1. 我对项目的理解

这不是 CRUD 后台,而是面向复杂工程(MBSE / 需求 / 模型 / 评审 / 成果交换)的**统一数据源平台**。我的理解与已固化设计一致:

- **唯一事实源**:对象(`DataObject`)、字段(`Field`)、关系(`DataRelation`)、状态、来源、版本统一管理于内核;一切写入只经命令入口(`POST /workspaces/{id}/commands`),一切外溢经事件信封(Outbox 至少一次)。
- **多视图是投影,不是副本**:表格/树/图谱/文档视图只读读模型,**不得复制主数据、不得持久化主数据**(AG-101/102)。同一 `objectId` 在各视图保持同一身份,跨视图选中/高亮/追踪经 `SelectionRef`(纯前端态,零写、零 storage,AG-209)。
- **评审是带定位的结构化数据**,不是普通评论:必须能锚定到字段级 / 对象级 / 关系级,且挂在版本化的数据上。
- **diff 是多维的**:对象 diff / 属性 diff / 关系 diff / 版本 diff / 制品 diff,不是文本 diff。
- **成果交换是双向的**:解析→映射→diff→冲突→回写,全部经内核命令落地;外部标准文件(ReqIF/XMI/STEP/FMU…)**永不作为事实源直读直写**(AG-507)。
- **热路径必须薄**:命令事务内禁止 AI / 全量检查 / 渲染 / 深解析 / 仿真(AG-201/211),重任务一律 `TaskHandler` 冷路径。

技术形态已定:Java 21 + Spring Boot 模块化单体,PG16 主存 + 闭包表存关系(不引图库),OpenSearch / MinIO / RabbitMQ + PG Outbox,前端 React + Vite,六包依赖红线由 `architecture:check` 门禁。

## 2. 当前阶段判断

**不是起点,而是"内核已成型、读侧与视图为零"的中段。**

已完成并打 tag:

| 阶段 | 任务 | 产出 | tag |
|---|---|---|---|
| 阶段0 | T-V33-001 变更收敛 | AGENTS.md 补全、error-codes.yaml、contracts 进 CI、git 基线 | `v0.2-baseline-v33` |
| 阶段1 批1 | T-V33-101 | CreateObject / UpdateFields + 幂等 + 字段级版本 + Outbox 写入 + 审计 | `v0.3-kernel-batch1` |
| 阶段1 批2 | T-V33-102 | CreateRelation / Unlink / UpdateRelation + 闭包表 + 基数/防重 | `v0.4-relation-batch2` |
| 阶段1 批3 | T-V33-103 | ChangeState / Archive / SoftDelete / BatchCommand + 状态机 + 终态写保护 | `v0.5-state-batch3` |

阶段1 **未完(批4 残项)**:Outbox **轮询投递器/Relay**(目前只有写入,无发布器与 RabbitMQ 投递)、命令 API 的 **OpenAPI 3.1 生成**(契约 §10.3 仅定约定未实现)。

尚未开始:阶段2–9 全部。`packages/views`、`packages/web` 仍为壳;无读模型;无评审实现;无交换适配器。阶段5 线框已作为设计输入就位。

一句话:**写侧深度已达,读侧/视图/评审/交换尚未起步。** 这正是 MVP-0 想验证的"贯通性"目前缺口所在。

## 3. 核心技术与产品难点

1. **跨视图同一身份 + 选择联动且零副本**:难在读模型投影的幂等/乱序(AG-210)、最终一致延迟必须**对用户可见**(线框 W-1.2 三态),以及 `SelectionRef` 协议在表格/树/图谱/文档间复用而不退化为各自高亮逻辑。
2. **评审的可定位性**:字段级/对象级/关系级评审必须锚定 `(targetType, targetId, fieldCode?, dataVersion)`,否则数据一改批注就失锚。当前契约与计划对评审的定义**偏薄**,是 MVP-0 的真实设计缺口。
3. **多维 diff**:对象/字段/关系/版本/制品 diff 各有结构;MVP-0 的"基础 diff"应先落"对象+字段+关系的版本间结构化 diff",文本 diff 不在其内。
4. **双向交换的回写边界**:解析产物必须经命令回写、冲突走确认流;MVP-0 阶段先用 **JSON 轻交换**验证"解析→映射→diff→回写"骨架,再到阶段7 的 ReqIF/docx。
5. **热路径纪律与过早抽象**:命令事务零重操作(已由 AG-201/211 拦住);解释器/转换框架由 backlog 触发条件治理,MVP-0 **不抽象**。

## 4. MVP-0 范围(用户 15 项 × 现状对齐)

| # | MVP-0 能力 | 现状 | 落点 / 缺口 |
|---|---|---|---|
| 1 | 创建对象类型 | 缺 | **阶段2(M2 元模型)** — 待设计 |
| 2 | 创建属性定义 | 缺 | 阶段2 |
| 3 | 创建关系类型 | 缺 | 阶段2 |
| 4 | 创建对象实例 | ✅ 已实现 | 阶段1 CreateObject |
| 5 | 创建对象关系 | ✅ 已实现 | 阶段1 CreateRelation + 闭包表 |
| 6 | 表格视图 | 缺(线框已就位) | 阶段5 |
| 7 | 树视图(分解关系) | **缺,且未在阶段计划显式排期** | **需新增任务卡(挂阶段5,复用闭包表)** |
| 8 | 图谱视图(关系) | **缺,且阶段5"不做"中后置** | **需新增任务卡或显式纳入 MVP-0 切片** |
| 9 | 多视图选中同步 | 缺(协议线框已定) | 阶段5 SelectionRef |
| 10 | 字段级评论 | **缺,评审在计划中偏薄** | **需先出 06 评审模型设计 + 新增评审阶段** |
| 11 | 对象级评审 | 缺 | 同上 |
| 12 | 关系级评审 | 缺 | 同上 |
| 13 | JSON 导入导出 | **缺,阶段7 首例是 ReqIF/docx 非 JSON** | **需新增"JSON 轻交换"任务卡(前置 ReqIF)** |
| 14 | 版本记录 | ✅ 已实现 | 阶段1 字段级版本 + 事件 |
| 15 | 基础 diff | **缺,未显式排期** | **需新增"结构化 diff(对象/字段/关系/版本)"任务卡** |

**判断**:已实现 4/5/14;阶段2 与阶段5 覆盖 1/2/3/6/9;**树视图(7)、图谱视图(8)、评审(10/11/12)、JSON 轻交换(13)、基础 diff(15) 五块未被现计划近期排期覆盖。** 这是现 9 阶段计划(内核优先、视图靠后、评审折叠)与用户"MVP-0 薄切片"定义之间的真实张力,需把这 5 块显式排进近期任务卡,使端到端环路尽早可演示(可以浅,但要贯通)。

## 5. 不进入 MVP-0 的内容

用户列出的 8 项排除与现有 backlog/ADR-009 完全一致,**继承不动**:复杂权限体系、完整 Office 插件、ReqIF/XMI/STEP/FMU 深度适配、AI 自动建模、大规模协同编辑、企业级流程引擎、高级图库优化、完整 PLM/Teamcenter 集成 → 对应 BL-01~09、ADR-009 的 B/C 级、各阶段"不做"边界。无新增、无松动。

## 6. docs/ 上下文包建设顺序(映射优先,只补缺口)

**不另建并行编号包。** 用户拟建的 01–10 功能,大部分已有更成熟的单一事实源承载;重复建会造成双源漂移。映射如下:

| 用户拟建文档 | 现有承载(继承) | 缺口 / 动作 |
|---|---|---|
| 01_PRODUCT_CONTEXT | 说明书 + 本文件 §1 | **缺**:补一份仓库级精简产品上下文(派生自说明书第2章) |
| 02_CORE_SCENARIOS | 说明书 11.2 闭环用例 | **缺**:抽取为 `docs/core-scenarios.md`(MVP-0 演示脚本口径) |
| 03_DOMAIN_MODEL | AGENTS.md §3.1 术语对照 + contracts | 部分:补一份领域模型概览(对象/字段/关系/状态/版本/评审/制品的关系图) |
| 04_DATA_MODEL | 迁移 V1/V2 + 说明书 4.7 | 部分:阶段2 落 M2 表时同步补"数据模型现状"汇总 |
| 05_VIEW_MODEL | 阶段5 线框 | 阶段5 实做时补读模型/SelectionRef 协议文档 |
| 06_REVIEW_MODEL | **几乎空白** | **首要缺口**:需新设计评审模型(字段/对象/关系级 + 锚定版本) |
| 07_EXCHANGE_MODEL | contracts §10 + 阶段7 计划 | 部分:补 JSON 轻交换的解析/映射/diff/回写约定 |
| 08_UX_PROTOTYPE_SPEC | `docs/prototype/阶段5-线框-v1.md` | ✅ 已有,后续按阶段追加 |
| 09_API_CONTRACT | `contracts/数据内核命令与事件契约.md` | ✅ 已有(事实源,受 AG-501 保护) |
| 10_ACCEPTANCE_CRITERIA | 各任务卡验收 + 阶段9 + 说明书11章 | **缺**:抽一份 MVP-0 验收清单(15 项逐条可演示判据) |

**建设顺序(只做缺口,按依赖排)**:
1. `docs/01-product-context.md`(精简产品上下文)— 让所有工具有统一定位口径(0.5 天)
2. `docs/06-review-model.md`(评审模型设计)— MVP-0 最薄环节,且阻塞 10/11/12(1 天,Claude 设计)
3. `docs/10-mvp0-acceptance.md`(MVP-0 验收清单)— 把 15 项变成可验收判据,作为切片的出口(0.5 天)
4. `docs/02-core-scenarios.md` + `docs/07-exchange-json.md` + `docs/03-domain-overview.md`(随对应阶段任务卡产出)

## 7. Claude / Codex / 人工 分工(继承 AGENTS.md 治理)

| 事项 | Claude(设计/审查) | Codex(实现) | 你(确认/验收) |
|---|---|---|---|
| 产品定位 / 边界 / 优先级 | 提议 | 禁止决定 | **最终裁定** |
| 核心数据模型 / 元模型 / 契约 | **设计** | 按契约实现,禁改契约(AG-501) | 确认 |
| 评审模型 / 视图协议 / diff 结构 | **设计** | 实现 | 确认 |
| 任务卡拆分 | **出卡** | 领卡执行 | 批准开工 |
| 代码 / 测试 / 修复 / diff | 审查 | **写** | 验收 |
| ADR / 依赖准入 | 提议 ADR | 禁止自引依赖(AG-502) | 批准 ADR |
| 文档事实源(AGENTS/ADR/contracts) | 提议变更 | 禁止触碰(AG-501) | 批准 |

工作流固定:**Claude 定义 → 你确认 → Codex 实现 → Claude 审查 → Codex 修复 → 你验收**。每任务一分支(AG-401),PR 带 `Spec-Ref:`,合并前 `pnpm verify` 全绿。

## 8. 下一步任务清单(可直接执行,按序)

**T1(Codex,收尾阶段1)— `T-V33-104` Outbox 投递器 + OpenAPI 生成**
- 内容:PG Outbox 轮询发布器 + RabbitMQ 投递(幂等消费框架挂点)、命令/查询端点 OpenAPI 3.1 生成并进契约测试。
- 出口:阶段1 完整;事件可被下游(阶段5 读模型)消费。
- 我先做:此卡可在现有内核上直接开;无需新依赖审批以外动作。

**T2(Claude,设计)— 阶段2 元模型设计稿(M2)**
- 内容:`object_type / field_def / relation_type` 的定义/发布/版本化 + 实例化 + 兼容校验(新增兼容/收紧阻断两档)。解锁 MVP-0 第 1/2/3 项与表格列驱动。
- 出口:设计稿 + Codex 任务卡 `T-V33-201`。

**T3(Claude,设计)— 评审模型设计稿(补 MVP-0 最薄环节)**
- 内容:`DataReview` / `Annotation` 锚定 `(targetType,targetId,fieldCode?,relationId?,dataVersion)`;字段级/对象级/关系级三类;失锚策略(数据改版后批注状态)。
- 出口:`docs/06-review-model.md` + 评审阶段任务卡(建议编号 `T-V33-25x`,挂在阶段5 之后或并行)。

**T4(Claude + 你,决策)— MVP-0 薄切片再编排**
- 内容:确认是否把 树视图 / 图谱视图 / JSON 轻交换 / 基础 diff 四块作为显式任务卡纳入近期,使"类型→对象→关系→表格+树+图谱→选中同步→评审→JSON 往返→版本→diff"端到端贯通(可浅)。
- 出口:更新版阶段计划(在总控文档追加 MVP-0 切片视图,不改既有阶段定义)。

**T5(Claude,产出缺口文档)** — 按 §6 顺序补 01 / 06 / 10 三份上下文文件。

依赖:T1 与 T2 可并行;T3 依赖 T2 的类型定义口径;T4 依赖 T2/T3 设计成型;T5 中 06 即 T3 产物。

## 9. 建议你下一条消息给我的指令

**推荐(先定义、不写码,与你"先恢复上下文再接续"的意图一致):**

> "确认上述继承/修正/废弃总账。先执行 T2 + T3:产出阶段2 元模型设计稿与评审模型设计稿(`docs/06-review-model.md`),并给出对应 Codex 任务卡 `T-V33-201` 与评审阶段卡。同时把 MVP-0 薄切片再编排追加进总控文档。暂不下发 T1 给 Codex,等我看完设计。"

**备选 A(优先把阶段1 收口,推进度):**

> "先出 `T-V33-104`(Outbox 投递器 + OpenAPI)的完整 Codex 任务卡,我批准后让 Codex 开工;阶段2 设计稿随后给。"

**备选 B(先补上下文文档):**

> "先按 §6 顺序产出 01/06/10 三份缺口文档,其余暂不动。"

我建议走推荐项:它修补 MVP-0 当前最薄的两环(元模型 + 评审),且不消耗你尚未审阅的实现工作。
