# T-V33-102 回归审查报告

- 审查对象:分支 `feat/T-V33-102-relation-commands`(main 之上 9 个提交),对照任务卡封闭清单
- 审查方式:分支内容静态核验(diff 范围、V2 迁移、三 Handler 逐项契约比对、测试清单);运行期以用户本机 `pnpm verify` 输出为准(待补)

## 一、审查结论

**修改后进入用户验收**——1 个一行级实质问题(P-102-1),其余全部通过。实现质量延续批1 水准,闭包表插入(祖先×后代笛卡尔合并 + depth 取最小)和环检测方向(pathExists(target→source))都是教科书级正确。

## 二、逐项核验

| 核验项 | 实测 | 判定 |
|---|---|---|
| diff 范围 | 仅 kernel/server/tests/package.json,零越界 | ✅ |
| jacoco 接电 | package.json test script 已改 `run-maven.mjs verify`(0f31d43) | ✅ |
| V2 迁移 | relation_type(cardinality/hierarchical)、data_relation 双向索引 + **部分唯一索引 WHERE status='ACTIVE'**、relation_history、relation_closure、种子 2 类型 | ✅(除 P-102-1) |
| 锁序 [R-5] | lockEndpoints `ORDER BY id FOR UPDATE`,两事务锁序一致,死锁防护成立 | ✅ |
| 防重并发安全 | 预检 + insert 失败(唯一索引兜底)→ KERNEL-409-DUPLICATE-RELATION 携已存在 id,竞态正确 | ✅ |
| 端点校验 [R-9] | 自环/不存在/VOID/DELETED/类型不匹配统一 ENDPOINT-INVALID,不泄露存在性 | ✅ |
| 基数 | one_to_many 拒绝第二父节点 → KERNEL-422-CARDINALITY-VIOLATION | ✅ |
| 环检测 | hierarchical 时查 target→source 既有路径,命中 409-CYCLE;闭包插入同事务 | ✅ |
| Unlink | 重复解除幂等(零事件零副作用);版本检测;`// TODO 阶段3 规则引擎接入派生判定` 如卡所限;闭包行同事务清理 | ✅ |
| UpdateRelation | 端点变更走 validateCandidate 整体重校验(查重/基数/环,excludedRelationId 排除自身) | ✅ |
| 测试 | 8 个场景测试齐全:防重携 id、二父拒绝、**成环回滚零残留**、缺失与 DELETED 同错误码、并发恰一胜出、Unlink 幂等+闭包清理、端点变更重校验、幂等重放零副作用;**零 sleep** | ✅ |
| fixtures | RelationCreated 信封样例已入(3326c7a) | ✅ |

## 三、问题清单

| 编号 | 严重 | 位置 | 问题 | 修复 | 阻断 |
|---|---|---|---|---|---|
| **P-102-1** | 中 | V2__kernel_batch2.sql + RelationRepository.insertClosure | `relation_closure` 主键为 `(ancestor_id, descendant_id)`,**缺 relation_type_id**——未来出现第二个 hierarchical 关系类型时,不同类型的闭包行会互相碰撞、depth 跨类型污染(insertClosure 的 `ON CONFLICT (ancestor_id, descendant_id)` 同病)。当前仅一个种子层级类型,测试全过,属埋雷 | PK 改 `(relation_type_id, ancestor_id, descendant_id)`;ON CONFLICT 目标同步加 relation_type_id。**V2 尚未合并入 main,允许直接修改迁移文件**(迁移不可变原则只约束已合并迁移) | 是(一行级) |
| O-102-a | 观察 | 闭包算法 | insertClosure/deleteClosure 的算法**仅对树形(每节点 ≤1 父)正确**;若未来定义 many-to-many 的 hierarchical 类型(DAG 共享路径),删除会过删。当前种子 decomposes_to 为 one_to_many(树),正确 | 随 P-102-1 修复在 V2 或 RelationRepository 加一行注释声明该限制;DAG 支持需要时另立任务(计数法闭包) | 否 |
| O-102-b | 观察(环境) | 本地 .git | 多条 git 命令输出 `error: improper chunk offset` 警告——commit-graph 辅助文件疑似损坏(不影响对象数据)。建议用户执行 `git commit-graph write --reachable` 重建,或删除 `.git/objects/info/commit-graph` | 用户侧维护项 | 否 |

## 四、架构红线检查

AG-100~110/201/203(关系查询带类型+方向+索引)✅;AG-405 写入纪律(9 个细粒度提交、零截断)✅;关系一等对象原则(独立表/版本/历史/事件)✅;无新依赖、无 backlog 误实现 ✅。

## 五、给 Codex 的修复指令(T-V33-102-fix,同分支,仅此一项)

1. `V2__kernel_batch2.sql`:relation_closure 的 PRIMARY KEY 改为 `(relation_type_id, ancestor_id, descendant_id)`;
2. `RelationRepository.insertClosure`:`ON CONFLICT` 目标改为 `(relation_type_id, ancestor_id, descendant_id)`;
3. 在 V2 该表 DDL 上方加一行 SQL 注释:`-- hierarchical 类型当前仅支持树形(one_to_many);DAG 需改计数法闭包`;
4. 本地跑 `pnpm verify` 全绿,贴输出(含 jacoco 段——本批起门禁真实生效)。禁止其他任何改动。

## 六、用户验收要点

1. Codex 修复后,本机 `pnpm verify` 全绿输出发我(本轮尚未收到运行期证据,且这是 jacoco 门禁接电后的首次完整验证);
2. 通过后合并 → tag `v0.4-relation-batch2`;顺手执行 `git commit-graph write --reachable` 修复 O-102-b;
3. 至此对象-字段-关系三件套齐全,内核写路径完整;下一批为批3(ChangeState/Archive/SoftDelete/Batch,状态机与级联语义)。
